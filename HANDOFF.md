# Kaggriculture 项目交接（截至 2026-09-23）

这是一份给后续 agent 的工作交接，不是比赛提交说明。请先完整阅读，再运行实验。

## 0. 任务边界与当前授权

- 用户目标：继续以公开 V45 为冻结父本，**冲 Kaggriculture 前 10%**。2026-09-20 用户否决止损，要求继续尝试。
- 用户明确授权过“开始，但先不报名”。本地写代码、安装依赖、跑模拟已获授权。
- **禁止事项（仍有效）：**不得替用户点击/接受比赛规则、加入比赛、上传或提交 agent、创建 Kaggle API 凭据、消耗用户的提交配额。冲前 10% **不等于**报名授权。新参赛截止 `2026-09-23 23:59 UTC`（北京时间 9 月 24 日 07:59）；若要保资格，用户必须在此之前单独说“报名”。
- 用户担心“参赛但最终名次普通”的个人展示风险。本地开发集未过门前不建议报名；过门后仍须用户确认才提交。
- 请不要把公开代码直接包装成用户原创提交。必须保留授权、出处与上游 notice；获奖方案还会有开源/可复现义务。
- **上传（2026-09-23 用户要求）：**每次改完代码、交接或测试，提交并推到 `https://github.com/waterpure/farm` 的 `main`。不报名、不提交比赛。根目录 `README.md` 只留在本地，不提交、不推送；GitHub 上的项目说明已按用户要求删除。
- **评测习惯（2026-09-20 用户要求）：**第 14 条及后续本地试打，不要再打官方弱对手；直接对冻结 V45，默认只打 **1 局**。打完必须列出**逐日收工对照表**（每天 hour 23，不是早上 hour 1）：第几天、我方/对方现金、地、种植、养殖、雇工。对方常在 hour 1 才雇人，hour 1 快照会把对方雇工误记成 0。
- **先看第 11 天（2026-09-20 用户要求）：** 先把第 0–11 天现金差磨平，再谈第 12 天之后的 75 格和终局。打完对照表要能单独讲清第 11 天为什么还差、第 10 天倒瓜那几小时谁先卖掉。

### 对用户的解释方式（长期约定）

用户明确要求：后续沟通优先使用游戏里的白话。先说“这一局实际发生了什么、谁少了什么、为什么少、结果怎样”，再按需补充代码名、回合号、state、worker、commit 等内部术语。不要把实现层术语当作前提，也不要只报指标而不说明它在游戏中的含义。

## 0.1 2026-09-21：经营平衡方案已执行，局部通过、整项目标未通过

用户重新钉死的目标：**第 30 天已卖货后的现金尽可能多**。买种、买畜、买地时，必须一起考虑现有及新增生产的浇水、喂粮、护理、收获、搬运、每日雇工与现金续航；不能买得起种子却雇不起浇水的人，也不能只因买得起土地就扩张。用户看过具体方案后说「开始」，授权本轮修改代码/协议/交接并做本地 seed 0 对 V45 一局验证；没有报名或提交授权。

**本轮落地：**`docs/ROUTE14_PROTOCOL_V1.md` 原计划内增加经营平衡修订；新建 `lab/route14_capacity.py`，用已有 `lab/task_router.py` 为现有照料、新种地的「种+当天浇」、下一天及作物/动物首次产出日的照料/收获构造带坐标走路见证，并计算下一笔可能收入前的现金预留。`lab/route14_hub.py` 把见证用于买种、买畜、买地：已有照料先占用工资/饲料；新种按可执行格数缩量；动物须能安排放置和未来照料；土地至少要负担下一块地的 4 格实际选种、工钱、维护预留和回本。动物流动资金预留到第一批产出，整季饲料仍在 ROI 中扣除；历史 `empty_slot_plan` 的落格筛选仍要求能支付动物整季饲料，是保守的二次闸，后续应审计两闸是否一致。`lab/route14_agent.py` 不再用同小时的预计卖货额提前买新东西；同一小时订单优先保留雇工与饲料；已种地浇水优先于再下种；饲料从仓库四个门口分给多名工人并行去喂，连续一天未喂的牲畜优先获喂。买下一只动物前先等仓库/工人手里的上一只落地，且新买动物须有同类设施或已规划的同类设施。没有改冻结 V45。

**验证：**`lab/test_route14_economy.py` 从 36 增至 42 个针对性测试，全通过。新增检查种后浇水路线、低现金保留照料、最后一小时不白雇人、现有收获拥堵时不买地、12 只牲畜由 4 名工人分领饲料、已有待落地动物时不继续买。最终代码对 `route14` vs `v45_base`、seed 0、720 回合本地对局：`DONE/DONE`，终局现金 **35,035 : 174,073**；第 0 天未浇格 **0**、现金 541、2 只动物；第 10 天 **12,841 : 17,004**；第 11 天 **12,176 : 16,877**；第 13 天买到 50 格；第 28 天仍有 12 只动物、草地 0，终局仓库没有未安置动物。该局约 23 秒，明显慢于旧执行器。

**不要过度解读：**这只证明本轮 seed 0 冒烟中开局照料、喂粮分工和待落地动物清零；仍大幅输给 V45，既没有通过开发集，也没有跑 holdout，更不能推断前 10%。中间试验曾达到 41,429 现金，但终局仓库剩 1 只买来未落地的鹅；放宽动物匹配还曾剩 5 只。为消除这种明确浪费，最终版先等上一只落地，终局现金降至 35,035。这是 **安全性与本局收益的真实取舍**，不应把更安全描述为更赚钱。第 10 天收工仍有 2 格未浇，且任务见证未记录实际工人路线；某些未浇可能是已成熟待收而非需要继续浇，须逐格核实。走路见证并不精确模拟执行器正在搬货、仓库门口争用、共享市场成交与未来对手动作，所以它**不能保证**每笔通过的投资都成功照料或一定增收。试过两种更严格的开种限制，同局终局现金分别降至约 1.8 万和 3.2 万且留出大量空地，已撤回。没有为了单局分数修改 70/30、50/50 或卖货规则。

**下一道未完成的验证门：**先做只读的后期未浇/搬运/喂养因果审计，分清哪批作物真正因缺水而死亡、哪些已成熟田不需浇，并复核动物「中枢第一批产出流动资金」与「落格整季饲料闸」的冲突；据此提出可审核的执行排程和投资闸修正，不能直接扩大到七对手联赛。只有修正后的一局逐日表显示照料损失被控制、仓库无搁置动物，且终局现金没有因保守停产大幅下降，才申请多 seed/多对手开发集。应同步审计约 23 秒/局的运行成本。继续遵守本文件 §0 的单局试打与不报名边界。

## 0.2 2026-09-22：细粒度规划已确认，第一阶段路线已落地，尚未打整局

用户给出 `/Users/pure_water/Desktop/竞赛/kaggle方案设计.docx`，并钉死挑选规则与三阶段顺序。第一阶段已实现为独立执行器 `route14_phase1`（`lab/route14_phase1.py`，runner 同名）。没有改 `route14`，没有改冻结 V45，没有报名，没有打 seed 0 整局。

**状态先于路线。** `lab/route14_state.py` 把一小时的局面拆成空地、杂草、棚舍、作物、动物、仓库、工人，以及对方看得见的地和牲畜。空地记录能不能种、能不能盖棚、离门口几步。作物记录还有几天能收、几步后开始掉产量、今晚会不会变草、今天浇过没有。动物记录下次产货、格子上的货、连续没喂、今晚会不会跑。仓库记录 100 的容量、要留给断粮牲畜的麦、手上还没进仓的货。必须浇水和必须喂粮从这些状态读，不再直接扫原始格子。

**仓库和市场是两堆货。** `InventoryState` 是自家仓库，容量 100，卖掉才从这里扣。`MarketState` 是全镇共用的库存：我方卖掉、对方卖掉都会加进去，镇上的店和镇中心买走才减少，价格由这一堆决定。市场状态还记着现在有哪些店、各几家、商店下一次买货还要几个时辰、镇中心下一次买货还要几个时辰、下一间新店还要几个时辰。每种货还有今天剩下每个时辰的价格；这份预测只算镇上会买走的，不算我们和对方还没卖掉的货。

**引擎实走已核对。** 用官方引擎从开局走完，不手改格子。空地排上种麦后仍是空地；下一小时看见麦苗才变成作物，种麦任务才完成。刚种下的麦有 1 份产量、还没熟、今晚不浇会死，倒计时和引擎格子一致。浇水排进去时还是干的，浇完才消失。没浇的胡萝卜过夜变成杂草，不是荒地；挖开后是普通空地。第 2 天的麦是熟的，收完变成荒地，1 份麦先在农夫手里，过夜后进仓库。荒地上盖出空牧场，放进羊；羊第二天早上必须喂，喂完任务才完成。照料当晚写在羊身上，加成要过夜才变成 1，同时出现粪肥。再空喂一天后羊跑掉，牧场还在并标成跑过。番茄第 8 天早上挂 1 个果，收完仍是番茄、产量归零，不会变成荒地。鸡舍、鹅、拾粪、给麦施肥的字段也和引擎一致。照料、施肥、拾粪按用户说明不设待办，待办只放在土地、作物和牲畜上。第一阶段的一天安排只重写两次：新的一天，以及对方雇工数量变了（通常是第 1 时辰）。它现在不安排挖地。挖杂草的完成标记要靠「当时还有负责人」；这是核对时手动把挖地从安排里去掉才出现的，不是现在的安排自己改的。种、盖、放养、收、浇、喂不靠这个条件，格子变了仍能记成完成。

**任务池。** `FarmState.task_pool` 是一张清单，不是三类活。每一格、每一种需要各占一行，行上写坐标、站着的作物或牲畜、要做的事。例如 `(2, 3) STRAWBERRY WATER`、`(2, 4) STRAWBERRY WATER`、`(2, 5) WHEAT HARVEST` 是三行。牲畜收获这一行站着的是羊，带走的货在 `goods`（羊毛）。今晚不浇会变草的是 `DIES_TONIGHT`，今晚不喂会跑的是 `ESCAPES_TONIGHT`，已经有货要赶快收的是 `RIPE`。同一格同一件事只进池一次。排班按行领还没派人的，领走之后变成 `SCHEDULED` 并写上谁。昨天浇过、今天还能活的地，和今天没断粮的牲畜，不进池。

**二维任务池是排班的任务源。** `build_task_grid` 只读已解析的局面，得到 `TaskGrid[x][y]`。第一阶段路线从格子里取还没派人的必须浇水、必须喂粮和收获，不再读线性 `task_pool`。浇水只要今天没浇过、而且浇了有用，就在格子上：今晚会变草是 `mandatory`，只增产则不是。`yield_gain` 仍按官方窗口算，可以是 0、1、2。熟了的一次性作物即使今晚会变草，路线仍去收、不改去浇。喂粮、关怀、拾粪、施肥的生成规则不变，路线先不领它们。杂草有挖地；空棚不生成拆棚。种、盖鸡舍、盖牧场、放牲畜只在已经派了人时出现。已派的活在下一小时重算格子时还在，要等局面证实才完成。

**单人全天路线。** `lab/worker_route.py` 的 `plan_worker_route` 只读一个人现在的坐标、当前时辰、和 `TaskGrid`。参加搜索的只有还没派人的必须浇水、必须喂粮、收获。走一格、浇、喂、收各用一个时辰。时辰 1 做到时辰 23，共 23 步，没有时辰 24。人已经站在那一格就直接干活。同一格的浇水和收获两种顺序都会试；一次性作物收掉以后，模拟状态把这一格剩下的浇水判成不能再做，所以不会出现先收后浇。搜索不是“先做离我最近的那件”：只朝还没做完的任务走，或者做脚下合法的那一件，并用“剩下的活最早也得几点做完”剪掉当天做不完的分支。先要全部必须活做完，再让走路步数最少。步数一样时，没有空等，结束时辰自然更早。直线上的几块地会一路做完，不送回仓库。这一版不雇人、不改市场、不改多人分工，`route14_phase1` 还没有改用这条路线。只为增产的浇水、已经派给人的活、挖地、照料、施肥都不进这条搜索。

**这十三件事，格子上有的和没有的。** 浇水、喂粮、收获能从作物和牲畜直接开行，池子里已经有。杂草的 `needs_dig`、空棚的 `can_dig`、牲畜的 `fertilizer_ready`、作物的 `fertilized_today` / `fertilizer_days_left`、牲畜的 `cared_today` / `care_bonus` 都在状态上，但池子不会因为这些字段自动开行。空地的 `plantable`、`can_build_coop`、`can_build_pasture` 只说明能种、能盖，不说明该种什么、该盖哪一种。空棚的 `accepted_animals` 和仓库里的 `unplaced_animals` 能对上“有空棚、手里有羊”，选哪一座棚仍是安排。拿货和放货不在土地、作物、畜身上：仓库和手里的货在 `InventoryState`，人的位置在 `WorkerState`。第一阶段的排班仍然只领浇水、喂粮、收获。

**格子跟着局面换形态。** 同一格不会同时是空地、作物和动物。空地派了人去种、去盖棚或去挖，格子仍是空地或杂草，但标成 SCHEDULED，别人不能再规划这块。下一小时局面看见苗，空地换成作物；看见棚，空地换成棚；棚里放进羊，棚不再空，并多出动物状态。一次收完的作物消失后，这格变回空地并标成 FALLOW（荒），直到再种上或盖上东西。动物连续没喂跑掉后，动物状态消失，棚还在，标成 ESCAPED。杂草挖开后是普通空地，不是荒。第一阶段的路线仍然不种、不盖、不放动物；这些流转只在局面或任务写明时发生。

**排进路线不等于已经做成。** 浇水、喂粮、收获各记各的，每件活只有三种状态。PENDING：今天要做，还没有人。SCHEDULED：已经写上谁、写上哪一时辰。例如门口那块瓜由农夫第 1 时辰收、第 3 时辰进仓并卖掉；会渴死的麦由 Hand2 第 6 时辰浇；断过粮的羊由 Hand1 第 8 时辰喂。这时瓜还在地里，麦还没浇，羊还没吃上。COMPLETED：下一小时的局面证明做成了。发出 HARVEST、WATER、FEED 的那一小时仍然是 SCHEDULED。下一小时看见瓜地空了才把收获记成完成；看见今天浇过才是浇完；看见今天喂过才是喂完。地变成杂草不算收获完成。同一块地上浇水和收获不会挤成一个状态。

**第一阶段只做：**现有作物和动物的必须浇水、必须喂粮、可收的货。每人一条全天路线。每批货写下收割时辰、进仓时辰、可卖时辰。卖出时辰等于进仓时辰，因为引擎先卸货再开市。估价用官方价格曲线，计入镇上收购、我方这一天的进仓，以及对方当前可见工人能把地里熟货送进仓库的时辰。没卖掉的货不算现金。雇人取「必须活干完」里的最少人数；多雇只有在更早卖出的现金超过工钱时才发生。

**明确不做：**新种、新动物、买地、照料、施肥、拾粪。昨天浇过的地今天不浇。没断过粮的牲畜今天不喂。会渴死的地、已经断过一天粮的牲畜，先排进路线。

**验证：**`lab/test_worker_route.py` 7 个测试通过：直线不回仓、相邻地连着做、同格浇水加收获、一次性作物收完不再浇、远处任务不会被最近的挡住、23 步刚好做完、24 步做不完。`lab/test_route14_state.py` 与 `lab/test_route14_phase1.py` 先前已通过，这次没有改它们。四格同样远的熟瓜，一个人进仓是第 5、11、17、22 时辰；加上三个雇工是第 5、5、7、7 时辰。没有对手抛售时不为这四格加人。对手有一大批瓜会在第 6 时辰进仓时，雇 2 人，三格都在那之前卖掉。仓库里的瓜当天开市就卖，空地不买种。喂牲畜的麦留下，别的货照卖。对方早上雇来的人出现时，路线按新时辰重排；涨价本身不重排。门口那块瓜排好以后是 SCHEDULED，农夫第 1 时辰发出 HARVEST 时地还在，收获仍未完成；下一小时地空了才是 COMPLETED。浇水和喂粮同样要等局面写上浇过、喂过。杂草不会被记成已经收完。第 2 天第 10 时辰有两家毛线店时：商店再过 2 个时辰买货，镇中心再过 14 个时辰买货，下一间新店也要 14 个时辰。毛在第 12 时辰卖掉仍是当下的价，第 13 时辰才变成店买走 4 份之后的价。这两家店不买瓜，瓜价今天剩下的时辰不动。空地排上种麦、盖牧场、挖杂草后仍是原地，状态是 SCHEDULED；局面变成麦苗、空牧场、空地后，任务才完成。牧场放进羊后出现动物；羊跑了棚还在并标成 ESCAPED。麦收完那格是 FALLOW。杂草盖住荒地后不再算荒。

**不要把这些针对性测试当成一局赢了 V45。** 官方开局是空地，第一阶段不会种，整局现金必然很差。等第三阶段能种能养之后，才按 §0 打 seed 0 一局。

**已知限制：**新雇工要下一时辰才能动，出生门口按雇人那一时辰开始时的站位估计，没有算农夫在同一时辰先走一步。对方模型只用已经在场的人，不算他们下一时辰才会雇到的人；所以每天第 1 时辰看见对方新人后会重排一次。`route14_phase1` 仍是一人一轮只收一格再送回仓库，不会顺路连收。单人全局路线已经能在格子上把一条直线收完，但还没接进这个执行器，也不送货、不进门。一次收完就消失的瓜，完成与否靠「地空了」来认；第一阶段不会挖地，所以不会把挖掉误认成收完。变成杂草的地会从待办里拿掉，不记完成，也没有单独的失败状态。必须活如果多到几十件，单人搜索会变重；当天做不完时，留下的是尽量多做完的一条，不保证那条残路步数最少。

**下一阶段：**人数外层已经能回答「最少雇几个人才能当天干完必须活」，见 §0.3。它还没接到 `route14_phase1`，也不送货进门。第二阶段才把照料和施肥按「多出来的货 × 那个时辰的售价 / 多走的步」插进已有路线。第三阶段才按用户的边际现金规则逐项加入 1 格麦、胡萝卜、番茄、草莓、鹅、牛、羊或买地，并且每加一项就重新扣钱、工时、空地、仓库和饲料。

## 0.3 2026-09-23：最少雇几个人，每换一次人数就重排全队

用户要求先回答：今天至少雇几个帮手，才能把眼前必须干的活在当天干完。不能先画好一条路再反推人数。每试一个人数，农夫和这些帮手的活都重新分一遍。

**第 0 时辰只雇人。** 这一小时最多 10 笔市场单，所以新帮手不超过 10 个。刚雇的人第 1 时辰才走。路线不猜他们是不是都出生在同一格门口。每个人的起点是第 1 时辰局面里这个人真正站着的坐标：`WorkerState.coord`（原来的 `position`）。`plan_minimum_hands` 要调用者按雇人顺序交出这些坐标；模块自己不挑仓库门。

**全队一起分。** `lab/team_route.py` 的 `plan_team_routes` 只领今晚会渴死的浇水、今晚会跑的喂粮、以及已经能收的货。只为增产的浇水、照料、施肥、拾粪不进这轮。先要全部做完，每人不超过第 23 时辰；做完以后走路步数尽量少。同一格上的浇水和收获交给同一个人，到了连着做。步数一样时，相邻格子尽量不拆开。不是「谁离得近就给谁」：两个人都靠近同一侧时，若一个人把两头走完总步数更少，就让另一个人空等。

**人数停在第一个干得完的。** 从 0 个帮手试起，干完就停。这一版不算多雇一个人能不能多卖钱，也不改物价、买种、买畜、买地。

**验证：** `lab/test_team_route.py` 8 个测试通过，同时 `lab/test_worker_route.py` 与 `lab/test_route14_state.py` 仍通过。0 个帮手能收完相邻两格就返回 0，不再试后面的人。农夫走不完三个角、站在第三个角上的 1 个帮手能收完，返回 1，帮手的起点是给他的那一格，三件活不重复领。第 21 时辰起每人只剩 3 步时，1 个帮手不够、2 个够，返回 2，三个人的起点各不相同。同一格的浇水和收获由站在那格的人连续做完，另一个人不领。只增产的浇水、照料、施肥、拾粪不进人。满地 100 格又要浇又要收时，试到 10 个帮手仍干不完，`feasible=False`；多给的第 11、12 个坐标不会变成第 11 个帮手。传入第 0 时辰时，走路从第 1 时辰开始。

**不要当成已经打赢。** 这层还没接到执行器。`route14_phase1` 仍是收一格送回仓库，雇人仍按旧的门口出生来估。必须活少的时候，人数是逐格分完再挑步数最少的那一队。格子多到逐格分配太重时，先用「这些活最少也要走多少步」判断连 10 个人都干不完；若步数下限说也许干得完，就按行切成几段交给各人，这时不保证步数最少。单人那条搜索在一个人分到很多活时仍然会变重。

**下一位：** 不要报名，不要打 seed 0 整局。人数这层先不要接进执行器。5×5、1～4 人的当天路线改看 §0.4 的 `plan_region_routes`，不要再把 `plan_team_routes` 的按行切段当成最短路线。仍不要改市场预测、买种买畜买地和照料/施肥策略，除非用户另说。第二阶段才把照料和施肥按多出来的货和多走的步插进已经排好的路线。

## 0.4 2026-09-23：5×5 里一块地一个人包干，排出当天最短路线

用户要求先把一小片地的当天路线做稳，再谈雇人和接进执行器。一块地或一只动物这一天要干的必须活，交给同一个人，走到那一格后连续做完。浇水、喂粮、收获不再拆给两个人，也不再按行把地切开。

**看的是哪一片。** `lab/region_route.py` 的 `plan_region_routes` 默认看左上角 5×5，也就是坐标 0 到 4。整张 10×10 上别的五格见方，用 `origin` 指出那一片的左上角。这一层不雇人，也不猜人从哪扇门出生。每个人已经站在自己的格子上：传入 `id` 和 `coord`。只领还会渴死的浇水、还会跑掉的喂粮、以及已经能收的货。只为增产的浇水、已经派给人的活、照料、施肥、拾粪都不进这一版。

**一块地是一站。** 格子上这几件必须活合成一次到访。走一格用一个时辰，干一件也用一个时辰。从旁边一格过去再浇再收，是三步，不是一步到位。路过别的地不算把那块地做了。麦、胡萝卜、瓜今天要收时，不再因为「今晚会变草」而必须浇：苗当天就会被收掉。浇水若还能把今天的产量加上去（例如麦现在 4、上限 6、浇了变 5），格子上仍留着这次浇水，标成非必须，记着 `yield_gain`。必须活排完以后，如果这一格本来就要去、而且多一个时辰还赶得及回仓，才顺手浇。已经顶到产量上限、浇了不加货的，格子上不再放浇水，只收。还没熟、今晚会渴死的麦仍然必须浇。番茄、草莓收完苗还在，浇水仍排在收获前面。一站里如果同时有浇水和收获，一次性作物必须先浇后收；排顺序的时候不删浇水。一站内部的顺序不参加「先去哪块地」的搜索。

**人怎么分。** 人数固定为 1 到 4。每一站只归一个人。比较的是这条路线重新排好之后多走的步，加上这一站要干的件数，不是插进旧顺序某个空档的步数；那个空档排完会被整条路线重排掉，不能拿来打分。当天排不下的地先搁着。之后每轮先试着把搁下的地塞回某个人；塞回去并且剩下的地变少，就收下，哪怕多走几步。然后再把一块地从甲挪给乙，或者两人对换。先要剩下的地更少，再让总步数更少，步数一样则收工更早。一个人分到的地不超过 10 块时，访问顺序精确算完；超过 10 块改用「先插最近、再两两对调」，只换算法，不因为地多就判这一天干不完。不再做「每件活一个开关」的整表搜索。

**时间。** 从第 1 时辰做到第 23 时辰，最多 23 步。路线展开成一步一步的 NORTH / EAST / SOUTH / WEST，以及 WATER / FEED / HARVEST。缺麦时，进门拿麦也占一个时辰，动作是 `["PICKUP", "WHEAT", n]`。有收获时，田间全部做完后再走回一扇门，按产品各卸一次，动作是 `["PLACE", 产品, 数量]`。没有「直接走到 (2,3)」这种一步。做不完时 `feasible` 为假，剩下的地在 `unfinished_visits`。先要必须活全部做完，每人不超过第 23 时辰，再让总步数最少，步数一样则全队最晚收工更早。

**喂粮要先有麦。** 人走到动物跟前直接喂，身上没麦这一下是空的。一个人当天负责几只必须喂的动物，缺多少就在开始跑自己的地之前去仓库门口拿一次：`max(0, 要喂的只数 - 身上已经带着的麦)`。一次拿够，不喂一只回一趟仓。身上已经够，就完全不进门、也不拿麦。四扇门用现成的 `shed_doors`，选的是「从现在的位置走到那扇门、拿麦、再把后面的动物走完」整段最便宜的门，不是离起点最近的那扇。仓库里的麦读这一小时局面上的真实库存，不用预测库存；几个人要拿的加起来不能超过这个数。不够喂完的那些动物留在未完成，`feasible` 为假，不会排出必然喂不成的 FEED。同一格上的喂和收仍是一站、一个人；拿麦不是格子上的任务，也不占一站。分给谁时就把这段进门算进成本：离动物更近但要绕远去拿麦的人，可能不如身上已经有麦、稍远一点的人。第 1 时辰按真实坐标、真实库存排完，第 1 到 23 时辰仍按这条固定队列走。人站错格子时原有的兜底重排还在，不为喂失败再加一套每小时重排。卖麦不再看旧的一天路线留了多少。第 1 时辰以后，已经排出来的路线里，当前时辰之后还要 `PICKUP WHEAT` 的份数留下来；这一时辰当场拿走的不重复留，因为引擎先拿货再卖。第 0 时辰新帮手还没出现，不按「现在这几个人谁去喂」留麦，也不猜他出生在哪扇门。这一片 5×5 里有几只必须喂的动物，就留几份麦，不扣掉农夫身上已经带着的。多留的最多晚一个时辰再卖。这一片以外的喂粮不留。

**验证（回仓之前）：** `lab.test_task_grid` 29、`lab.test_region_route` 19、`lab.test_team_route` 8、`lab.test_worker_route` 7，合计 63 个测试通过，约 0.33 秒。当时直线三格收完只走 2 步、不送回仓库；这一条已经被下面的一次回仓替换。每一步都是真动作。产量 4、上限 6 的熟麦，格子上有收获，也有一次非必须的浇水（`yield_gain` 为 1）；这一阶段的人只收。产量已经是 6 的熟麦只有收获，不再浇。若一站里同时写了浇水和收获，麦先浇后收。还没熟的麦仍然浇。番茄仍是先浇后收。同一格浇水和收获只有一个主人。两个人都离右侧不远时，右侧两格仍由已经走到这边的那个人顺路做完，总步数是 5，不是各自奔向离自己起点更近的那一格。四个角上的人各自留下脚下那一格，中间那格只多走 4 步。一个人走五个角加中心要 16 步，两个人更少。11 块地超过精确算法的门槛，当天仍排得完。18 块地一个人排到第 23 时辰仍有剩，`feasible` 为假，已经排上的动作不超过第 23 时辰。满满 25 格、四个人，能做完，连跑两次结果相同。`origin=(5,0)` 只看那一片，不把左上角的地算进去。第 6 时辰截止时，先按顺序塞地会把 `(0,1)`、`(0,2)` 搁下；把横排上的地挪给站在 `(4,0)` 的人之后，这两格重新排进站在 `(0,0)` 的人的路线，当天能做完。另一处：甲的旧路线某个空档看起来最近，但重排之后乙的真实多走步更少，分地时选乙。

**已经能指挥人走路。** `lab/region_phase1.py` 的 `make_region_phase1_agent`（runner 名 `region_phase1`）是单独的一条执行路径，没有改掉旧的 `route14_phase1`。第 0 时辰仍用原来的雇人和卖货，农夫和帮手都站着不动，也不按猜出来的门口去排全天路线。第 1 时辰从局面里读农夫和已经出现的帮手的真实坐标，以及各自身上的麦、仓库里的麦，最多取前 4 个人，建好任务格子，再调用 `plan_region_routes`。这一片的左上角取必须活的最小坐标，并保证五格见方落在棋盘里。之后每个时辰按工人名字取出那一小时已经展开的动作：走路是 `["WEST"]` 这种，拿麦是 `["PICKUP", "WHEAT", n]`，喂是 `["FEED"]`，没有动作就 PASS。不再另算一条走向目标的路。人还站在计划里的格子上，就继续执行；人不在了，就用现在的坐标和最新格子重排，不把旧队列里的下一步盲目发出去。排上的浇水、喂粮、收获写成 SCHEDULED；下一小时局面证明做成了才是 COMPLETED。拿麦和卸货不写进任务格子。收成的卸货和同小时卖掉由路线里的 `PLACE` 带出来，执行器不再自己找仓库。也没有和 V45 打整局。旧执行器仍是收一格送回仓库。

**不要当成已经打赢。** 人数外层 `plan_team_routes` 还在，格子一多仍可能按行切段，那不是这条最短路线。单个格子的活多到任何一个人都做不完时，这一版仍不拆给两个人，直接留在未完成里。必须活如果散出这一片 5×5，这一步只做这一片。喂粮只在出门前补一次，不会走到一半发现麦不够再折返。仓库麦不够时，那一格的喂和收一起留下，不把收拆给别人。

**验证（2026-09-23，喂粮补麦之后）：** `lab.test_region_route` 27、`lab.test_region_phase1` 12、`lab.test_task_grid` 29、`lab.test_team_route` 8、`lab.test_worker_route` 7、`lab.test_route14_phase1` 11，合计 94 个通过，约 0.5 秒。站在门口、身上没麦、要喂 `(2,4)` 和 `(2,3)`：第 1 时辰拿 2 份麦，再两步向西、喂、向北、喂，一共 6 个时辰，走路 3 步。三只动物只拿一次、数量是 3。身上已有 2 份就不进门。身上有 1 份、要喂 3 只，只再拿 2 份。不算拿麦刚好排到第 23 时辰的十格，加上拿麦就做不完。仓库 3 份麦、两人一共要 3 份，三只都喂完；仓库只有 2 份时不能把三只都排完，发出的拿麦数量不超过 2。离羊只差一步但要绕去门口的人，羊分给身上已经有麦、稍远的人。官方引擎里仓库有麦、人站在门口：拿麦、向西、喂，之后那只羊的 `fed_today` 为真。农夫身上有 1 份麦、旧路线因此一份都不留，羊却分给还要先走到门口的帮手时：仓库 4 份只卖 3 份，那 1 份留到第 2 时辰再拿；拿到的那一时辰不再重复留。第 0 时辰帮手还没出生、农夫身上已经有 1 份麦、仓库有 4 份、这一片有 1 只必须喂的羊：早上只卖 3 份并雇人；这一片外面的另一只羊不占这份留粮。第 1 时辰帮手出现在 `(4,3)`，羊分给他，路线里有一次 `PICKUP WHEAT 1`，当天排得完。第 1 时辰仍只留这 1 份还没拿的，第 2 时辰人已经站到门口、这一小时就拿，不再留，4 份都可以卖。

**收完只回一次仓。** 一个人把自己分到的地全部收完，货一直拿在身上，最后才回仓库。不是收一格送一趟。卸货用 `PLACE`，同一种货合成一次：三块麦分别是 3 和 2，卸成一次 `PLACE WHEAT 5`；羊卸毛、牛卸奶、鹅卸蛋，不把牲畜的名字当货。身上还留着喂牲畜的麦不卸。只有浇水和喂粮、没有收获的路线不额外回仓。结束时从最后一块地挑离它最近的那扇门，田间先后顺序不为此重算。回仓的走路和卸货占的时辰算进能不能做完，也算进分给谁：收完离仓远、要单独开一趟回程的人，可能不如已经在往回走的人顺路把下一格捎上。卸货如果会落到第 24 时辰，这一条旧规则已经被后面的「回不来就留在地里」改掉：收获本身当天做完就留下，不再整格删掉。执行器不另写找仓；路线里这一小时已经是 `PLACE`，卖货单就按这一小时进仓的数量卖掉，不等下一小时。

**卸货时辰写回这一格。** 路线本身没有改。每一次收获已经知道最后那一次 `PLACE` 在第几小时。这个时辰同时记成 `planned_drop_hour` 和 `planned_sell_hour`，写到这块地的任务、作物或牲畜上。同一个人收的麦共用卸麦的那一小时；奶用卸奶的那一小时。浇水和喂粮没有卸货时辰。地收完、格子空了以后，这条收获任务仍留着原来的收、卸、卖三个时辰。门口熟麦第 1 时辰收、第 2 时辰卸，记的是 `(1, 2, 2)`；下一小时地空了，任务完成，这三个时辰还在。农夫从门口出发，第 2、第 7 时辰收的两块麦都记第 12 时辰卸麦，第 4 时辰收的奶记第 13 时辰卸奶；顺路喂的羊只有第 9 时辰的喂粮，没有卸货时辰。

**验证（2026-09-23，连收一次回仓之后）：** `lab.test_region_route` 33、`lab.test_region_phase1` 15、`lab.test_task_grid` 29、`lab.test_team_route` 8、`lab.test_worker_route` 7、`lab.test_route14_phase1` 11，合计 103 个通过，约 1.1 秒。`lab.test_route14_state` 10 个也通过。从 `(4,4)` 收 `(1,1)`、`(1,2)`、`(2,2)` 三块麦，三笔收获都做完才有一次 `PLACE WHEAT 3`，中间没有卸货。麦 3 加麦 2 再加 4 份奶，卸成 `PLACE WHEAT 5` 和 `PLACE MILK 4`。羊、牛、鹅分别卸毛、奶、蛋。喂完没有收获的路线没有 `PLACE`。身上带着 3 份喂粮麦、只收了 1 份毛，只卸毛，不卸麦。第 21 时辰在 `(2,4)` 收完，回 `(4,4)` 要再走两个时辰，卸货会落到第 24 时辰。当时这一格留在未完成；这一条已经改成收获留下、人不回仓，见后面「回不来就留在地里」。同一块地若从第 1 时辰开始、截止放宽到第 4 时辰，则是收、向东、向东、`PLACE WHEAT 1`。站在 `(1,2)`、离 `(0,2)` 只有一步的人没有领到那块麦，因为站在 `(0,0)`、已经要往回走的人顺路捎上更便宜。农夫站在门口的熟麦上，第 1 时辰收，第 2 时辰 `PLACE WHEAT 6`，同一时辰的卖货单是 `SELL WHEAT 6`，不是下一时辰，也不把身上另带的麦算进去。

**顺手的照料和增产浇水。** 必须做的浇水、喂粮、收获和回仓卸货先排完，这一遍没有改。排完以后，只在已经要去的那一格上，再看能不能加上照料，或加上还能增产的浇水。只有照料、没有喂或收的格子，这一步不去。只有增产浇水、路线本来不会经过的格子，也不去。加进去仍各占一个时辰：喂完再照料再收；浇水仍在收成前面。每加一个都把整段路重新算一遍。回仓来不及时，顺手的浇水或照料仍然可以留下，人停在地里；只有田间本身排不下，这个顺手动作才不要。剩下的时间只够一件时，增产份数多的先于照料加成少的。施肥和拾粪仍不做，因为肥料在人身上，不在仓库自动扣。

**验证（2026-09-23，顺手照料和增产浇水之后）：** `lab.test_region_route` 41、`lab.test_region_phase1` 17、`lab.test_task_grid` 29、`lab.test_team_route` 8、`lab.test_worker_route` 7、`lab.test_route14_phase1` 11，合计 113 个通过，约 1.2 秒。`lab.test_route14_state` 10 个也通过。时间够时，熟麦先浇再收，这一站是 2 个时辰。第 22 时辰才开始、只剩两个时辰时，当时为了卸货只收不浇；这一条已经改成浇水和收获都做、当天不卸，见后面「回不来就留在地里」。羊先喂再照料。喂、照料、收在同一只羊上是 3 个时辰，同一个人做完。只有照料的羊不进路线。加上照料后，收成和卸货都晚一个时辰，作物或牲畜上记下的卸货时辰跟着变。增产 2 份和照料加成 1，只剩一个时辰时选浇水。必须活没做完的格子不会因为这些顺手动作变多。官方引擎里先喂后照料，羊的 `fed_today` 和 `cared_today` 都变成真，各占一个时辰。

**空地也写进同一张任务格子。** 一块空地先把麦、胡萝卜、番茄、草莓、瓜、鹅、牛、羊八条线都算一遍，账用现成的金钱/天、季内能不能收完、买种或买畜要多少钱。不套旧的「一半养畜、七成三种作物」那道门。比完只留一条。瓜是先种再浇，羊是先盖牧场再把羊放进去。进门拿羊不是这块地的任务，和拿麦一样，由路线在出门前补上。新苗的浇水记在格子上，但不是地里已经长着的那棵苗的浇水；它标明依赖先种下，而且种下之后当天必须浇。种和浇、盖和放，当天排不下整段就整段先不要开始。一粒种子、一头牲畜、一笔钱只能给一块地。离仓门近的空地先挑，所以更赚钱的线落在近处。没钱、季内收不完、或者算下来不赚钱的，这块空地就仍然没有生产任务。已经长着的作物和已经在棚里的牲畜不另开一条生产计划。锁着的地也不开。

**验证（2026-09-23，空地生产计划写入任务格子之后）：** `lab.test_production_plan` 12，加上当时的路线和格子测试，合计 135 个通过。一块空地八个候选里只留下瓜。一粒种子、一头羊、一笔牛钱都只给一块地。季末瓜不写进格子。近门的地拿更赚钱的线。

**空地上已经定下的活，顺路才做。** 必须的浇水、喂粮、收获先排完，已经要去的格子上的照料和增产浇水也先排完。然后才看空地上那一条已经定下的生产链。还要花钱买种或买畜的，留在格子上，今天不走。手上已经有种子，就走到那一格先种再浇，种子不用进门去拿。仓库里或这个人身上已经有羊、牛、鹅，才盖棚再放进去。同一种牲畜只进门拿一次，数量写在同一次拿货上。麦和羊如果同一个人都要，只绕一次仓库门，但拿麦和拿羊各占一个时辰。羊在谁身上就是谁的，不能算成大家都能用。四扇门按整段路的总时辰来选，不是离人最近的那扇。多出来的走路加上干活，哪条链增加的时辰少就先塞哪条；时辰一样才看每天的钱。塞进去以后整段路重新算，必须活变多、或者任何已经接上的活过了第 23 时辰，这条链就整段不要。种了不浇、盖了不放，都不允许。没塞进去的空地不算没做完，当天路线仍然算完成。没有必须活的时候，这一片改看空地计划所在的位置，不再死守左上角。已经排上的种、盖、放，下一小时价钱变了也不改成别的。种下去以后那一格变成苗，原来排好的浇水照样发出去；盖好棚以后，放羊那一步也不丢掉。

**验证（2026-09-23，空地计划顺路执行之后）：** `lab.test_region_production` 18，连同原有路线、格子、局面测试合计 153 个通过，约 1.3 秒。从 `(0,0)` 去 `(0,2)` 浇水，中间 `(0,1)` 种瓜再浇，走路仍是两步，只多两个干活时辰。只剩 1 个时辰时，种和浇都不出现。发出的动作是 `PLANT MELON` 然后 `WATER`。还要买种子的瓜计划不执行，必须活照样完成。仓库一头羊：先到门拿 1 只，再盖牧场再放羊，每步一个时辰。羊已经在身上就不进门。仓库没有羊、农夫身上有一只、帮手没有，这格归农夫。仓库只有一头羊、两块空地，只做一块，路线仍然算完成。两只羊是一次 `PICKUP SHEEP 2`。牛和羊各拿一次。喂两只羊再放一只羊，只进一次门，先拿 2 份麦再拿 1 只羊。再加一段会超过第 23 时辰的种瓜，必须浇水保持原样。路上的空地比绕远但更赚钱的空地先做。5 块必须浇水都做完、10 块空地只种了 2 块时，没有未完成的必须活。价钱改成麦更赚钱之后，已经排上的瓜计划还在。官方引擎里先种瓜再浇，苗在，而且当天浇过；先拿羊、盖牧场、再放羊，棚里是羊，不是空棚。派工记录上种的是瓜、放的是羊；格子上从待做变成已排，局面证实之后变成已完成。

**收完回得来才回仓，回不来就留在地里。** 时间够，还是连收、最后回一次仓、按货卸下、同一时辰卖掉。回仓会拖过第 23 时辰时，田间的活照做，人停在最后一块地上，不再为了卸货把最后一两笔收获删掉。身上的货不在第 23 时辰的市集卖掉。天黑之后，引擎会把农夫和帮手身上的东西自动放进仓库，仓库一共只能装 100。计划必须保证：这一天主动卸下、又不能确认同一时辰卖掉的货，加上每个人天黑时还背着的东西，再加上仓库里没被拿走的东西，加起来不超过 100。主动卸下的收获只算进仓库一次，不会再算进身上。进门拿走的羊、麦，用掉或放进棚里的不再回来；没喂完的麦、没放完的羊，天黑会回到仓库，要算进去。全队一起算，不是每个人单独有一个 100。哪一笔后加上的收获会撑破 100，这一笔就不接；必须收的如果自己就会撑破，留在未完成，不另写一套提前卖货。取消回仓省下的路程，不算这个人更便宜，免得站在门口的人被远处的人抢走。

**验证（2026-09-23，回不来就留在地里之后）：** 上述路线、空地、格子、局面测试合计 167 个通过，约 0.3 秒。第 18 时辰站在门口旁边收麦，第 19 时辰回门，第 20 时辰卸下。第 23 时辰已经站在熟瓜上，只收，不卸，卸货时辰和卖货时辰都空着。第 21 时辰收一块、走一步、第 23 时辰再收一块，两笔都在，没有卸货。仓库 80 再背 15 是 95，可以；85 加 15 正好 100，可以；86 加 15 是 101，这一笔不收。三个人背上 10、15、5，仓库 70，一共 100，可以；其中一人改成 16，合起来 101，不能全部收下。仓库满 100、里面有 2 只羊，两只都拿出去放进牧场，仓库变成 98。拿 2 只只放 1 只，剩下那只算回仓库。拿 3 份麦只喂 2 份，剩下 1 份算回仓库。收 6 个瓜再卸下，只算 6，不会变成 12。必须活做完以后仓库会到 98 时，再加一笔不必须的 5 个瓜就不接，必须的路线仍然做完。种了必须浇、盖了必须放，没有拆开。

**第 0 时辰就把当天的路和市集写完。** 地里要做什么，仍然只看任务格子。第 0 时辰先沿用当天旧路线已经算好的雇人数量，再按官方规矩猜新帮手站哪扇门：四扇门谁人少去谁，一样少就按西北、东北、西南、东南。不把第一名帮手写死在某一扇门。猜好的人从第 1 时辰排到第 23 时辰。还没买的种子也可以排进去，种子不用进门去拿，只要在种下去之前的市集买到。还没买的羊、牛、鹅也可以先排进去，但只指望第 0 时辰买到，这一步不写中途回仓去拿后来才买的畜。

买什么从写完的路线反推，不从格子上全部空地反推。格子上有十块空地、路线只接了三块，就只给这三块买。手里的种子先给最早要种的那一格，缺的才买，同一种合成一条订单。这条订单的截止时辰，是最早一次真正缺种的种植时辰减一。同一时辰买的种，赶不上同一时辰种。羊、牛、鹅也按缺的头数合成一条，截止只能是第 0 时辰。仓库里的和这个人身上已经抱着的，不再买。雇人是几个人就写几条，不能写成一条里面雇三个。

这些单子放进当天的市集队列，从截止时辰往前找第一个还有空位的时辰。每个时辰连同已经要卖的货、要买的麦，最多 10 条。第 0 时辰先给出货、买麦和雇人，种子不能把雇人挤掉。羊买不下，或者第 0 时辰没有位置，就删掉多走路、每天钱又更少的那一整条牧场，再重排。种子塞不进截止时辰，种和浇整段拿掉，再重排。必须的浇水、喂粮、收获不拿来给空地腾位置。发出买种、买畜并不改手头库存，下一小时看见的才算数。真要种的时候没有种子，或者要拿的羊不在仓库里，这一步改成停着，下一小时再按人实际站的地方重排，必须的活不拆。

第 0 时辰人还是停着。市集才雇人、买畜，种子只在还能放进截止时辰时才买，能晚就晚。第 1 时辰先核对人是不是站在早上猜的门口。对上了就照早上那份路线走，不再重排。对不上，才用原来的偏离重排。

**验证（2026-09-23，第 0 时辰写完全天市集之后）：** `lab` 下全部测试 239 个通过，约 0.7 秒。农夫站在西北门，三个新帮手依次出现在东北、西南、东南。手里有 1 粒草莓种子、路线要种两次，只为晚的那次买 1 粒，截止是那次种植的前一个时辰。三块缺瓜合成一条 `BUY_SEED MELON 3`。仓库已有的羊、身上抱着的羊，都不再买。第 0 时辰只剩 4 个位置时，4 条雇人占满，羊和种子都不挤进去。种子在截止前每个小时都满了，种和浇整段消失，旁边必须浇的麦还在。口袋只够一只羊时，留下每天钱更多的那块牧场。路线没走到的空地不买种。第 1 时辰出生点和早上猜的一样时，用的还是早上那一份路线。观察里的种子和仓库数量，发出购买之后保持原样。

**第 0 时辰自己比要雇几个人。** 旧路线的 `target_hires` 不再决定这一片雇几个人。农夫加上已经在场的帮手，人数还没到 4 个时，从再雇 0 个试到再雇满 4 个。每一个人数都从空的剔除名单开始，猜好官方出生点，把当天路线、要买的种子和牲畜、市集队列排到稳定。钱不够雇这么多人，或者第 0 时辰的市集位置放不下这么多条雇人，这一套直接作废，不能按三个人排路却只雇两个人。雇人的钱按官方递增价一笔一笔加，不按人数乘一个固定工钱。比的时候先看必须的浇水、喂粮、收获还剩几笔没排上，少的赢。都排完了，再看空地计划里真正种下去或放进棚的那些，把它们的每天收入加起来，减去这份雇人的钱。已经长在地里的收获不算进这笔收入。净收入一样时，人少的赢，所以多雇一个却种不出更多的，不会因为收工更早就雇。卖货和买麦仍暂时问旧路线，但雇人只看这份比较的结果。

**验证（2026-09-23，第 0 时辰自己选人数之后）：** `lab` 下全部测试 255 个通过，约 1.3 秒。一个人浇得完、又没有空地可种，雇 0 个，市集里没有雇人。13 块干麦一个人浇不完、两个人浇得完，雇 1 个，不雇第 2 个。20 块干麦加 5 块每天 10 块钱的瓜，第 2 个帮手多出来的瓜超过他的工钱，雇 2 个；第 3 个帮手没有新的瓜，不雇。第 0 时辰只剩 2 个市集位置时，雇 3 个的方案作废。口袋只够付 2 个人的工钱时，雇 3 个的方案作废。一个人来不及种的瓜，换两个人仍能看见，格子上的计划没有被删掉。旧路线写着雇 3 个、地里却没有额外的活，结果是雇 0 个。农夫站在西北门、这份方案雇 3 个时，帮手依次出生在东北、西南、东南。第 1 时辰人站在早上猜的门口，不再重排。

**下一位：** 不要报名。不要把 `region_phase1` 当成已经替换 `route14_phase1`。第 0 时辰已经自己比较 0 到还能塞进 4 人的每一种人数，写完路线和市集再选。不要改回用旧路线的 `target_hires` 拍板。完整 30 天的对照账在下一节：下一刀只处理第 0 天把整袋钱买成喂不上的羊。不要为了后来才买的羊，中途再写一条回仓去拿。不要另写种子路线或牲畜路线。施肥、拾粪、肥料怎么拿到人身上，仍未做。不要做提前卖货腾仓，也不要做买地、对手建模，或一套新的统一边际分数。已经拥有的羊在金钱/天里仍按买一头来扣成本。下一格也还看不到上一格计划收成会把市价压下去。

## 0.41 2026-09-23：完整 30 天对照账，策略没改

这一步没有改雇几个人、当天路线、买种买羊的次序、浇水、收获。只加了一本书：同一颗种子、都坐左边、对手都是起步那一家、都走 720 个时辰（30 天），让现在这块 5×5 的早班和旧的第一阶段各干一整季。

**账从官方成交里抄。** 卖出、买种子、买牲畜、买货、雇人、买地，钱都在官方引擎里改。评测时暂时包住这几个入口，只记下真正扣成或加上的金额，不改任何动作，一局结束把原函数放回去。卖货按每一单位当时成交的价钱累加，不用数量乘一个手头价。没做成的单子不进收入，也不进支出。开局的钱加上卖货、减去各项支出，应等于终局口袋；对不上就留下差额，不拿一个未知数去填平。这 20 局里差额都是 0。官方终局奖励就是终局口袋里的钱：引擎在收工时把奖励写成 `farm["money"]`，这 40 局里奖励和口袋逐局相等。

**20 颗种子（1 到 20）全部正常收工。** 没有崩溃，没有非法动作。结果每局一样，所以没有单独的最好或最差种子；汇总里这两项都写成第 1 颗，只是因为数字打平时留下编号最小的那颗。先不要为了凑 50 颗再跑，下一刀改完再用这同样的 20 颗对照。

| | 5×5 早班 `region_phase1` | 旧第一阶段 `route14_phase1` |
| --- | --- | --- |
| 平均终局现金 | 0 | 3000 |
| 平均差额 | -3000 | |
| 中位数差额 | -3000 | |
| 胜 / 平 / 负 | 0 / 0 / 20 | |
| 卖货 | 0 | 0 |
| 支出 | 3000，全是买羊 | 0 |
| 雇人 | 0 | 0 |
| 失败动作 | 每局 1 笔没买成的麦 | 0 |

**第 1 颗种子上实际发生的事（其余 19 颗相同）。** 第 0 时辰市集只有一条：买 6 只羊。每只 500，刚好把开局的 3000 花光。农夫当天从仓库一次抱出这些羊，盖了 6 座牧场并放进去，中间走了 6 步。当天收工现金已经是 0，地上 6 只羊，仓库是空的，仍然只有开局那 25 格。没有买到麦，也没有买种子。之后有一笔买麦，标价 26，口袋是 0，没做成，也没有因此再扣钱。羊连续两天没吃上，第 1 天收工全部跑掉，牧场空着。接下来大约 700 个时辰农夫站着不动。整季没有播种、没有收获、没有卖出、没有雇人，局面变了也没有重排。

旧的第一阶段在这块空地上什么都不买，719 个时辰都停着，3000 还在。对照的结论是：早班终局现金更少，少掉的就是这 3000。

**下一刀只修这一处。** 第 0 时辰不要在饲料还没留出来之前，把整袋钱换成羊。买不起喂到羊还在的麦，就少买，或者不买。修完用同一本账再打种子 1 到 20，看终局现金是否不再掉到 0。不要加肥料、照料、买地，不要重写卖货，不要做对手建模，也不要另做一套统一的每天收入分数。不要报名。

账本在 `experiments/region_phase1_season_v1.jsonl`（每局一行）和 `experiments/region_phase1_season_v1_summary.json`。入口是 `python -m lab.season_eval`。`lab` 测试 263 个通过。

## 0.5 第 14 条路线：给下一位 agent 的完整脉络（2026-09-20 晚）

**只读冻结 V45。** 不要改 `third_party/v45/main.py`。当前候选是独立执行器 `route14`：`lab/route14_economy.py`、`lab/route14_hub.py`、`lab/route14_agent.py`、协议 `docs/ROUTE14_PROTOCOL_V1.md`。runner：`route14` vs `v45_base`。默认 seed 0、一局、hour 23 逐日对照表。不报名、不 112 联赛，除非用户另说。

### 用户钉死的现行规则（已实现部分）

- **70/30 与 50/50（2026-09-21 已改）。** 空地最多一半畜牧，买得起就养，买不起退回种植 70/30。第 10 天前也可以下畜。空棚不算已经养了，有空棚继续买。买一只畜只扣这只的买价和它自己剩下天数饲料 + 200 垫。种植内部 70% 赚钱作物、30% 麦/萝。正在长的不拔，已放下的畜不拆。
- **拓地（2026-09-21 已改）。** 现有空地先填。买地只付新 25 格：地价、当天多雇的人、买完还够先种几格。买完后口袋还要养得住现有工钱和口粮。人手上限 12 忙不过来就不买。
- 能收就收：官方 `first_yield_day` 到了、地里有产量就 HARVEST，**不要为凑满 6 个再浇一天**。
- 货进仓库的那个时辰就卖，不等第二天早上。同一小时卸完就挂卖。门口 4 格尽量各占一格；已经站在任一门口、手里有货立刻卸，不许改道。前 15 天有货就卖；第 15 天起才观望 90%。第 10 天熟瓜先收仓库 5 步内，近圈没清完不去角落。
- 雇人、买地、买畜由中枢按当天活量、剩钱、回本天数一起定；不写死第几天雇几人。扩地后按新地活量加人，上限 12。
- **畜牧日常：**有畜就必须买粮、喂、CARE，和作物每天浇水同一档。手里拿着饲料去喂，不送回仓库。连续两天不喂会跑。没有当天饲料就不新买畜。
- **先卖再补种：**早上还有一茬熟瓜时先不买种。拿货的人只送仓库。空地 ≥4 补种优先于浇草莓。

### 2026-09-20 晚：60% 一包再收完一包（seed 0）

用户确认进仓就卖不如对方打包卖，改为收到约 60% 先卖、收完再卖余下。单测 25/25。

seed 0 第 11 天 **10964:16626**（上一刀抢收进仓就卖是 13782:16700）。终局 8657:135988。

**复盘到第 11 天：** 第 10 天 14 点仓库攒到 42 个瓜才触发 60% 一包，对方 11–12 点已经倒完。我们把高价窗口让出去，42 个砸进已经跌过的价（14 点 932 → 15 点 9679）。地里清完后仓库还剩 18 个，23 点没卖掉（手里可能还拿着，第二包没挂上）。这刀把第 11 天差又拉大了。未报名。

### 2026-09-20 晚：对方并不是真的「攒满一包再一次倒完」

用户问：对方怎么能一次性倒完，是不是没种那么多。对着 route 0 tape 核对：

- **种得少是事实。** 对方开局 12 格瓜（另 7 格麦），我们 17 格瓜。每格收 6，对方一共 72 个；第 10 天卖 60、第 11 天早上卖手里剩的 12。
- **不是一单砸 60。** 第 10 天卖瓜是跟着进仓走的：9 点卖 6（刚卸 1 趟）、10 点卖 24（4 个人同时卸，门口 4 格占满）、11 点 12、然后 6/6/6。现金在 11–12 点跳，是因为那两小时到货最多，不是仓库里先囤到 60% 再挂单。
- **门口照样一小时最多 4 趟。** 对方 10 点 4 人各卸 6 个=24，已经是物理上限。他们能「看起来一次倒完」，是货少、人按 tape 对准门口、到了就卖；不是有第二条卸货通道。
- **先前把对方理解成打包卖，是看错了。** 他们每趟 6 个进仓那个时辰就卖掉。我们改成攒到品类约 60% 才卖，等于自己把 9–11 点高价让出去。未改代码；须用户说开始才动卖法。
- **跟我们「进仓就卖」也不一样。** 引擎是先卸货再挂卖，对方同一小时卸 24 就卖 24。我们决策时只看见仓库里已经有的，这小时刚卸的要等下一小时才卖；而且是仓里有多少卖多少，常是 5、10 零卖，没有把 4 个门口凑成一笔。已按用户要求改成到了就卖。

### 2026-09-20 晚：到了就卖 + 门口分格（seed 0）

用户要求学对方：货到仓库那个时辰就卖，门口合理安排。已去掉 60% 囤货；引擎先卸再卖，所以同一小时 PLACE 的数量立刻挂 SELL；拿货的人按距离分到 4 个门口，先到的先卸。单测 26/26。

seed 0 第 11 天 **13156:16946**（60% 那刀 10964:16626；再上一刀进仓隔夜卖 13782:16700）。第 10 天 **12677:17480**。终局 24853:160637。未报名。

**复盘到第 11 天：**

1. **同小时卖已经对上。** 第 10 天每次卸多少就挂多少：5 点 5、9 点 9、11 点 12、13 点 13、14 点 8。不再出现仓里压着不卖。
2. **10 点我们对着空门口。** 对方 10–11 点 4 人同时卸，现金 2437→8656→11419。我们 10 点和 12 点 PLACE 都是 0，人还在路上或还在地里收。到了就卖做到了，人没有凑成一波 24。
3. **9 点我们还领先（2842:1846），11 点就被倒过去。** 差不在早卖那几颗，在对方那一波 24 个砸高价、我们大头还在后面零卖。
4. **第 9 天已经落后约 900（1884:2806）。** 对方 50 格上的牛羊莓，我们仍 25 格无畜。第 11 天对方买第三块地现金落到 16946，我们 13156，仍差约 3.8 千。
5. **比 60% 囤货好，没有超过隔夜进仓就卖。** 60% 那刀把高价让光了；这刀吃到了 9 点前的价，但 10 点没人在门口，第 11 天比隔夜卖那刀还少约 600。未报名。

### 2026-09-20 晚：为啥对方 10 点四人同时卸、我们门口空（seed 0 复盘）

用户问差距到底在哪，不是说了模仿对面吗。对着同一局第 10 天工人账核对，不是门口物理上限不同：

1. **模仿的是卖法，不是他们写死的走路日程。** 对方 12 格瓜全在仓库 3–5 步内；6–8 点一波收完，一起往门口走，10 点正好 4 人站在门口卸 24。这是 tape 排好的，不是看见货再反应。
2. **我们 17 格有角落 6–8 步。** 谁近谁先收、谁先送到谁先卖。5 点近的已经卸 1 趟，10 点远的还在路上（当时 9 人拿着瓜、8 人还在走），到不齐。
3. **这刀「四门各站一格」还把人带走了。** 8 点和 10 点都有人已经站在门口、手里拿着瓜，任务却指定另一个门，下达 SOUTH 而不是 PLACE。10 点门口其实有货，一趟都没卸成。这是自己的分门规则，不是学到了对方。

未改代码。若要四人一波，须另说：站在任一门口就卸、远近分开走还是收齐再一起送。

### 2026-09-20 晚：门口就卸 + 第 10 天近圈先收（seed 0）

用户说「开始」。已改：站在任一扇门口、手里有货立刻 PLACE，指定另一扇门也不许从门口走掉；第 10 天熟瓜按离仓库近到远收，先收 5 步内，近圈没清完不去角落，清完再收下一圈（最近剩余距离 + 1）；到了就卖保持，不等 4 人；这茬瓜不拔。单测 28/28。

seed 0 第 11 天 **12853:15464**（上一刀到了就卖+分门 13156:16946）。第 10 天 **13055:16518**（上一刀 12677:17480）。终局 17490:152632，按用户要求不当本刀目标。未报名。

**复盘到第 11 天：**

1. **门口不再走过。** 第 10 天 24 小时 `walk_off_door` 全是 0。8 点卸 5、10 点卸 5，上一刀这两小时有人站在门口拿着瓜却 SOUTH。这刀那两趟卖掉了。
2. **近圈先收做到了。** 开局近圈 11、角落 6。近圈 9 点清完；角落一直挂到 16 点以后才动，收工还剩 1 格熟瓜。空闲的人没有去 (0,0) 抢收。
3. **10 点仍然不是四人同时卸。** 我们 5/8/9/10 点各卸 5，11 点才 14。对方 11 点一波把现金 2342→8434。近圈格子本身距离 2–5 步，到了就卖，人还是一趟一趟到，凑不成对方那种 10 点 24。
4. **第 11 天现金差收窄了，没有磨平。** 12853:15464，差约 2.6 千（上一刀差约 3.8 千）。9 点我们 4106:1769 仍领先；11 点被对方那一波倒过去。第 9 天已经 1851:2717，对方 50 格牛羊莓，我们 25 格无畜。第 11 天对方买第三块地落到 15464。
5. **比分门那刀：第 10 天我们多卖到一点（13055 vs 12677），第 11 天我们少约 300（12853 vs 13156），对方少约 1.5 千。** 共享市场里我们 11 点砸了 14 个，把双方报价都压了一截。未报名。

**卖瓜卖不过的真实原因（seed 0 对账单）：** 不是挂单方式，也不是瓜少。双方都是卸完就卖；第 10–11 天我们挂卖 76 个、对方 72 个。差在高价窗口里谁把货凑到门口。对方 12 格全在仓库 3–5 步，10 点 4 人一次卸 24（当时价约 249），11 点再卸 12（约 235），60 个里的大头砸在高价上。我们 17 格里 6 格在 6–8 步外，近的 11 格也是 2–5 步混着，收到就往回走、到了就卖，所以 5/5/5/5 零卖；10 点人家卸 24 时我们门口只有 1 趟 5 个。11 点才凑出 14 个，价已经被砸到 235，角落那批更是 174→140→第二天早上 73。多种的 5 格瓜卖进了砸完的价，没有多赚钱。未改代码。

### 2026-09-21：第 10 天差距的下一刀方案（未改代码，待用户说开始）

用户问：用我们自己的方法（金钱/天 70/30，进仓就卖，不抄 V45 tape）第 10 天跑赢或把差收小。静态核对种地顺序后，近圈 11 / 角落 6 不是收获规则造成的，是**开局填空地的方向反了**。

1. **70/30 种对了，格子派错了。** 25 格地，赚钱作物 17 格瓜、平稳 8 格麦/萝。空地列表按行扫描，从最远的 (0,0) 开始。前 17 个槽给瓜，于是角落 6 格（离仓库 6–8 步）全是瓜，靠仓库的 8 格反而种了麦/萝。上一刀「近圈 11、角落 6」正好等于这 17 格瓜的距离分布，不是巧合。
2. **用户当场纠正：不能只把扫描顺序倒过来写成「瓜近麦远」。** 70/30 仍只决定这一茬种什么；往哪一格放按金钱/天，价值高的贴门口，价值低的放远格。哪一种眼下更赚钱，哪一种就更近，不写死作物名。

### 2026-09-21：按金钱/天贴门口（seed 0）

已落地 `place_plan_by_value`：空地按离仓库门口从近到远，计划按金钱/天从高到低，一一配对。作物和畜同一把尺。单测 30/30，含开局瓜价贴门口、草莓临时更赚钱时草莓换到门口。不报名。

seed 0 第 10 天 **12897:14664**（上一刀门口就卸+近圈先收 13055:16518）。第 11 天 **11354:13739**（上一刀 12853:15464）。终局 36492:142539。按用户要求不当本刀目标。

**复盘到第 10 天：**

1. **远瓜没有了。** 第 10 天 24 小时 `ripe_far` 全是 0。开局 17 瓜在近圈、8 萝在角落。上一刀近圈 11、角落 6。
2. **9 点前已经倒过去、一直领先到 15 点。** 3 点卸 5、6 点 10、7 点 5、9 点 9。4 点 2537:1418；10 点 8224:1938 仍大领先。对方 11 点那波 1938→7366，15 点我们 12841:11961 还没被追上。
3. **收工还是落后约 1.8 千。** 18–19 点对方 12807→14586，牛羊莓在 50 格上收工进账，我们倒瓜已经结束。差从上一刀约 3.5 千收到约 1.8 千，没跑赢。10 点仍不是四人同时卸（5/10/5/9 零卖），只是货更早、对方被我们提前砸了价所以对方收工也少了约 1.9 千。
4. **早上只有 13 格熟瓜。** 开局 17 瓜，第 1 天表上掉到 14，第 3 天补到 16。晚种的那几格第 10 天还没熟，所以 1 点不是 17 格一起收。
5. 第 9 天 1429:2820，对方 50 格牛羊莓的领先还在。未改拓地。

下一刀若做（须说开始）：倒瓜窗口已经能领先到下午，收工被 50 格畜莓翻盘。是继续凑四门同时卸，还是动第 6 天拓地补第 9 天的 1400，要另说。

### 2026-09-20 晚：先磨第 11 天（seed 0）

用户要求先别看终局 75 格，把第 11 天现金差磨平。上一局第 10 天小时账：我们 9 点 2923:1913 还领先，对方 11–12 点一次倒完跳到 8690/11502，我们地里还挂 10 格熟瓜往下卖。已改：熟瓜收割优先于麦、有熟瓜时不建棚不补种。单测 24/24。

seed 0 第 11 天收工 **13782:16700**（改前 11260:16770）。第 10 天 **13229:17526**（改前 10788:17404）。终局 24425:152645，按用户要求不当本刀目标。

**复盘到第 11 天：** 第 0–8 天现金我们不落后，常领先。第 9 天对方 +1188（50 格上的畜和莓），我们 1769:2805。第 10 天倒瓜我们 +11460、对方 +14721；11 点对方一次进账，我们当时还剩 5 格熟瓜。仓库门口只有 4 格，一小时送不完 17 格瓜。第 11 天对方买第三块地现金回落到 16700，我们 13782，仍差约 3 千。第 10 天收工 24 格空（熟瓜清完才补种）。未报名。

代码曾是植物+动物一张榜、第一名 70%、第二名 30%。CARE 入账后前二是羊和牛，第 10 天收瓜后空地全铺畜。用户否定后已改：第 10 天起空地 50% 畜牧 / 50% 种植（空棚也占畜牧地）；种植内部才是赚钱作物 70% + 平稳麦萝 30%；买畜扣整群剩余天数饲料。单测 24/24。

seed 0：`10908:147942`。开局瓜 17 萝 8，无畜。第 10 天卖瓜 `10788:17404`。此后 5 羊 + 莓麦茄，不是全场畜。喂 91、照料 81。地仍 25 格未拓。未报名。

**复盘（这一局为什么是这个分）：**

1. **分差主要在地，不在第 10 天倒瓜。** 收瓜日我们 10788、对方 17404，只差约 6 千。终局差约 13.7 万，几乎全是第 11 天之后对方 75 格莓麦工厂 + 牛羊鹅每天进账，我们卡在 25 格。对方第 6 天 50 格、第 11 天 75 格；我们 `land_orders=0`。
2. **没买地，是拓地闸自己卡死。** 第 10 天收工还空 19 格，规则是现有空地先填，当天不能买。第 12 天填满、口袋约 1.1 万时，闸按「旧地+新地 50 格再铺一轮」计价：一半畜就要约 25 只 ×500，再加 20 天饲料，远远超过 1.1 万，所以永远不买。第一块地只要 1000，但养护准备金按 50 格全场重铺来算。
3. **畜牧一半只落到 5 只羊。** 建了 12 个棚（占满 50% 地），但买畜要预留「现有+新的整群 × 麦价 × 剩余天数」和种植那半的种子，5 只之后口袋过不了闸。7 个空棚占着地，既没有羊也不种。
4. **收瓜后没有第二茬瓜。** 双方一起倒瓜把瓜价砸下去，「眼下赚钱」从瓜换成莓/茄/麦。种植那半变成莓麦茄轮种，一茬钱远不如再种 10 天瓜。对方 tape 收完瓜改铺莓麦，而且是 50–75 格在转。
5. **第 10 天前没有畜，是规则不是漏做。** 第 10 天前空地只准种作物，对方开局就有牛羊，第 6 天起羊毛牛奶已经进账。
6. **不是畜又跑了。** 喂 91、照料 81，5 只羊养到收工。也不是 25 格又全变成畜。

下一刀若做（须说开始）：拓地准备金按「新 25 格」而不是把旧地当空地再买一遍；空棚要么放下畜要么不要占种植地；收瓜后「赚钱作物」是否仍允许再种一茬瓜，要另说。

### 2026-09-20 晚：喂粮+CARE 按浇水同一档（seed 0 一局）

用户指出作物有浇水，畜牧也该有喂粮和 CARE。原先任务写过，但拿着麦会被当成卸货送回仓库，饲料还常被雇工单挤掉，等于没喂。已改：缺粮就买麦（可花到现金缓冲以下）、手里的麦去喂不入库、CARE 压过普通浇水、建棚/放畜优先于多种两格瓜。单测 22/22。

seed 0：`7:165782`。羊从第 0 天养到第 20 天，喂 92 次、照料 49 次。第 10 天卖瓜 `14390:16512` 后把钱砸进牛羊，第 15 天 25 格全是畜、现金 186，之后买不起麦，第 21 天畜全部跑光。未报名。

### 2026-09-20 晚：CARE 入账 + 拓地要养得住整场（seed 0 一局）

上一刀把动物按「每次只出 1 个、不算 CARE」记账，开局底价下羊每天约 18、瓜约 142，所以 30% 轮到萝/莓/麦。那笔账是错的：官方每天喂+CARE 时羊/牛/鹅 30 天约 38/39/56 个，底价下羊约 212/天、牛约 170/天，都压过瓜。对方第 10 天领先不是因为「畜不如菜」——seed 0 收工对方已经 50 格、牛 6 羊 10，地里瓜已收完改种莓麦，现金 16102；我们若真只种更赚的瓜，第 10 天不该被这样一局甩开。

已改：`animal_money_per_day` 计入 CARE 产量；空地计划按口袋能买几只畜就铺几只；拓地要付得起整场（旧+新）再铺一轮，以及剩下天数整场工钱和饲料，人手忙不过来不买。单测 20/20。

seed 0：`1942:152685`。开局按新账去买牛羊，钱瞬间掉到约 200，畜没养住（第 5 天起表上又没畜），第 10 天 `308:16102`，地一直 25 格到第 25 天才买到 50。比错账那局更差。未报名。

### 2026-09-20 晚：种养同一账 + 拓地（已落地，seed 0 一局）

用户授权开始。`rank_lines` / `empty_slot_plan` 把作物和动物排在一起；`land_expansion_plan` 要求现有地无空格、买完后雇得动、现金够买地+雇人+把新 25 格按 70/30 填上、净值仍正。单测 18/18。

seed 0 对 V45：`11543:143714`。第 10 天仍约 `12028:16928`。第 13 天买到 50 格并雇 12 人，第 18 天买到 75 格。畜牧仍 0：开局价下瓜金钱/天约 142，羊/牛/鹅按官方底价算不过第二名作物，30% 轮到萝/莓/麦而不是畜。后期 75 格常常种不满、现金被买地抽走。未报名。

### V45 第 10 天雇 11 人是不是「每条路线抢瓜」？

是，而且写在 tape 里，不是看见瓜才反应。静态读 41 条 `_ROUTES`：

- **第 10 天 HIRE：39 条雇 11、2 条雇 12。没有例外。** 瓜 `first_yield_day=10`，这是收瓜倾销日。
- 第 11 天仍雇 10 或 11（24 条 10 人、17 条 11 人），因为第二天还要倒剩瓜（tape 上第 10 天卖 60、第 11 天再卖 12）并且地已扩到 50/75、牛羊还在。
- route 0 雇人日程：5,3,4,5,4,4,7,7,8,8，**第 10 天跳到 11**，此后长期 9–11。第 10 天 hour 0 一次挂 9 个 HIRE，hour 1 再挂 2 个，凑满 11（昨天的人已解散）。
- 因此「11 人」首先是 **第 10 天抢共享市场高价瓜**，不是第 11 天才开始；第 11 天高雇工是大农场常态 + 扫尾。

### 我们怎么输在收瓜波（seed 0）

对方开局 12 瓜。我们改金钱/天后是 17 瓜 + 8 萝。引擎两边第 10 天早上地里都是产量 5。共享市场谁先把货送进仓库谁吃高价。

演进分数（对 V45 seed 0）：店缺口种法 `8769:125276` → 金钱/天铺满仍旱死 `11823:144682` → 浇地也雇人+隔夜卖 `19109:153408` → 第 10 天能收 `26234:140327` 但第 11 天才卖完 → 当天卖 `26182:128280` 我们 8 点先卖第一笔 6 个，对方 10 点起一次卖 24 个、当天倒完 → 两倍雇人+能收就收，半场 360 步 `13495:28707`，第 10 天 `8027:17265`（我雇 6 对方 11，收工还剩 13 格瓜没收完），第 11 天 `12550:16851`。

卡点：HIRE 上限 8、市场 10 单、第 10 天仍收不完 17 格；对方 tape 预写 11 人从 hour 0 就位。

### 2026-09-20 晚：只改第 10 天雇 11 人（seed 0 一局）

用户授权「单单在第 10 天多雇人抢收」。其它天仍上限 8、倾销两倍。实现：`hire_target` 第 10 天固定对准 11；`pack_market_orders(..., hire_first=True)` 让雇工单挤掉买种；hour 1 起用 `remaining_hires` 补到 11。单测 13/13。不报名。

seed 0 对 V45 整局 720 步：`20830:161571`，`DONE/DONE`。第 10 天收工雇工 **11:11**（改前 6:11），地里 0 空、不再剩 13 格熟瓜；作物变成瓜 18 + 莓 7（当天收完又补种了）。现金第 10 天 **5959:17338**（改前 8027:17265），第 11 天 **12160:17040**（改前 12550:16851）。加人把瓜收完了，但当天卖货现金更少，对方仍一次倒完吃高价。加人本身不是赢面。

### 2026-09-20 晚：先卖再补种（seed 0 一局，第二次才真正补上）

用户明确说代码要改成先卖再补种。第一刀把 `planting_allowed = not dump_push`，草莓一直能摘就把 17 格空地锁到第二天，用户看到仍空地会认为没改。

第二刀：`planting_allowed` 只在 hour 0 且地里还有一茬熟瓜时禁买种；拿货的人只 PLACE；仓库门口允许多人同去；空地 ≥4 时 PLANT 优先级 90 压过浇水 80。单测 16/16。

seed 0 对 V45：`22260:141870`。第 10 天 **11891:17137**，雇 11:11，收工 **3 空 / 瓜16 莓5 麦1**（上一刀是 6174:18600、17 空）。第 11 天 **12320:16096**。当天卖掉后把瓜田补回去了，倾销现金差从约 1.2 万收到约 5 千。整局仍大败在地和畜牧规模。未报名。

### 下一位可以动的方向（须用户说开始再改）

1. ~~第 10 天对准 11 人。~~ **已做。**
2. ~~收瓜日先卖再补种。~~ **已做。**
3. ~~种养 70/30 同一账本 + 拓地按买完后能否填上。~~ **已做，后又加 CARE 与整场养护。** 错账那局买到 50/75 格、没买畜。CARE 入账后 seed 0 畜没养住、整局 `1942:152685`。
4. ~~收瓜后 50/50 种养 + 种植内部赚钱 70 / 平稳 30。~~ **已做。**
5. **第 11 天差：** 进仓就卖 13782:16700；60% 打包 10964:16626；到了就卖 13156:16946；门口就卸+近圈先收 **12853:15464**。差从约 3.8 千收到约 2.6 千，没磨平。10 点仍不是四人同时卸（各小时 5，11 点才 14）。
6. **第 10 天按金钱/天贴门口（2026-09-21 已做）：** seed 0 第 10 天 12897:14664，差约 1.8 千（上一局约 3.5 千）。15 点前领先，收工被对方 50 格畜莓翻盘。远瓜 0。
7. **买地养畜三处一起改（2026-09-21，用户说开始）：** 第 10 天前可下畜；空棚不算已养、有棚继续买；买地只付新 25 格。买一只畜只扣这只的买价+它自己余天饲料+200，不再每只都锁种植种子钱。畜牧名额买不起就退回 70/30 种菜。单测 31/31。seed 0 第 10 天 6294:18100，开局雇 10、14 瓜，第 13 天 75 格口袋 24，羊第 17 天跑光。
8. **经营中枢（2026-09-21）：** 雇人/买地/买畜走同一剩钱账本，不写死第 10 天 11 人。单测 36/36。seed 0 第 10 天 7472:16495，开局 16 瓜+1 羊雇 4 人；羊养到收工；地只买到 50。倒瓜窗口仍落后。用户追问种养两不落：开局瓜没浇活、第 10 天只有 7 格熟；买第二只羊被整季口粮门槛卡住。未改代码。

### 2026-09-21：经营中枢（用户不要写死第几天雇几人）

用户明确要求：不要针对性改「第 10 天雇 11」「空地×2 雇人」这种补丁，而要一个中枢，买地看钱和人手，买羊看人手和剩下天数。

已落地 `lab/route14_hub.py`：先扣现有工钱、口粮、手头活（浇/收/喂/扛货步数）；剩钱按回本天数决定补种、买畜、买地。盖棚只盖这小时买得起的那一只。单测 36/36。不报名。

seed 0 第 10 天 **7472:16495**（上一刀写死买地养畜 6294:18100；再上一刀只种菜贴门口 12897:14664）。第 11 天 **3848:16180**。终局 6365:157307。未报名。

**这一局地里发生了什么：**

1. **开口不再雇 10 人砸钱。** 第 0 天雇 4、种 16 瓜 8 萝、1 只羊，口袋还剩 997。上一刀开局雇 10、只种 14 瓜、第 9 天只剩 935。
2. **羊没跑，地也没买到破产。** 喂 135、照料 104。第 11 天买到 50 格，没有再买 75 格把口袋掏到 24。羊从第 0 天养到收工还在（第 28 天仍 7 只）。中枢把买地后的口粮/工钱留住了。
3. **第 10 天倒瓜仍落后。** 收工 7472:16495，差约 9 千。早上只有 7 格熟瓜（开局 16 格里不少中途空掉/晚种），中枢按扛货活量只雇了 4 人，对方 11 人。10 点我们 4721:2111 还领先，11 点对方 2111→8241。差仍在对方 50 格畜莓和第 10 天熟瓜数量，不在「有没有写死雇 11」。
4. 比写死买地那局：第 10 天我们多约 1.2 千，对方少约 1.6 千；比只种菜那局仍差一截（12897:14664）。中枢把破产堵住了，倒瓜窗口还没磨平。

### 2026-09-21：为啥第 10 天熟瓜少、畜牧也少（用户问种养两不落）

对着 seed 0 每天早上/收工的地块核对，不是「钱不够所以必然种不好养不好」，是中枢把钱和人手拆开用了，两头都没铺上。

1. **熟瓜少：开局那茬没浇活，地里后来的瓜年纪不够。** 第 0 天中枢买了 24 包种、1 只羊、雇 4 人。18 点才种满 16 瓜 8 萝；23 点还有 5 格当天没浇。引擎规定连两天不浇就变草。第 1 天早上 3 格草、开局瓜只剩 13。第 3 天早上 13 格已经一天没浇，第 4 天早上瓜掉到 8、草 10 格。第 10 天早上地里仍有 16 格瓜，但年纪是 10 天的只有 **7 格**（另外 3/5/1 格分别才种了 4/6/8 天）。收工表上的「16 瓜」是补种晚茬，倒瓜窗口用不上。
2. **人手按「种得下」雇，没按「种完当天必须浇上」雇。** 第 0 天 4 人要盖棚、放羊、种 24 格再浇 24 格，走不完。第 2 天萝熟了人去收货，23 点 16 格没浇。旱死 → 空地/草 → 再补种晚瓜，恶性循环。
3. **畜牧少：不是 50% 名额没用，是买第二只的门槛把剩钱锁死了。** 中枢先把空地种子钱花光，再买羊还要一次性扣「这只羊剩下所有天的口粮」。第 0 天买完 16 瓜+8 萝+1 羊口袋约 1000。第 1–9 天口袋 244–1278，第二只羊账上要 500+25×剩余天数，再加 200 垫，还要求扣完后还能撑到第一袋毛（6 天）。所以第 1–9 天 `animal=None`。第 10 天早上 1278 仍因「扣完口粮撑不到第一袋毛」不买；倒完瓜晚上 7472 才买第 2 只。一天最多买 1 只，倒瓜前不可能铺成对方那种 16 只。
4. **空地一直在，地也没买。** 中枢规定有空地不买地。第 1–9 天几乎每天 1–6 格空或草，所以卡在 25 格。对方第 6 天已经 50 格牛羊莓。我们不是「种养各半」失败，是 25 格里菜没浇活、羊买不起第二只、地也加不了。

未改代码。若要种养两不落，须另说：浇水算进当天必做活、买畜不要一次性锁整季口粮、空地/草清完再谈买地。

### 2026-09-20 晚：用户三问（畜牧 / 拓地 / 第 9、11 天雇工）

1. **畜牧代码写了，但这局一只没买成。** `choose_next_animal`、建棚、买畜、放畜、喂、CARE 都在 `route14`。买畜门槛是：70% 高价作物已经铺满、动物金钱/天高于第二名作物、空地补种后 ≤2 格、地上已经有空棚、人手还管得过来。建棚还要求口袋里没有种子。实际长期在种瓜、口袋常有种子、也从没建棚，所以表上一直「无畜」。对方开局就有牛羊，路线表写死的。
2. **拓地规则自己打架，这局不可能买地。** `should_buy_land` 要求「现有地一块空地都没有」同时又要求 `plantable_today > 0`；而 `plantable_today` 在没有空地时恒为 0。两个条件互斥，买地单永远不会发出。对方第 6 天 50 格、第 11 天 75 格是 tape 预写的。即便第 10 天我们有 1.2 万现金，也买不成第二块地（第一块 1000）。
3. **第 9、11 天我们雇 2 人，是按 25 格浇水活量算的，不是漏雇。** 每人按 12 个活，25 格浇水大约农夫+2 雇工就够；第 10 天才因为抢瓜对准 11。对方第 9 天已经 50 格 + 牛羊鹅要喂要收，路线表雇 8；第 11 天 75 格雇 10。他们不是突然加班，是大农场日常人手。

### 2026-09-22：接手体检（只读诊断，未改代码）

用户要求「看看现在这个程序」。先说交接账本本身已经落后于代码：上一位 agent 改了东西但没写 HANDOFF。实测差异如下，本节只做诊断，**一行代码都没改**。

**代码现状与上一节记录不符的地方：**

- 单测是 **42 个**全绿（上一节写 36）。
- 新增了 `lab/route14_capacity.py`（300 行，HANDOFF 的「关键文件」没列）：里面是 `day_schedule`（当天路线能不能排完）、`future_care_hires`（未来关键日要几个人）、`affordable_care_reserve`（养到第一次卖货要留多少钱）、`proposed_crop_slots`、`new_land_crop_slots`。中枢的买种/买畜/买地现在全部先过这三道容量门。
- `format_daily_compare` 从 `route14_economy.py` 搬到了 `route14_agent.py`。照旧命令 `from lab.route14_economy import format_daily_compare` 会 ImportError。
- 项目仍然**不是 git 仓库**，所以没有 diff 可查，只能靠跑和读。

**seed 0 一局（route14 vs 冻结 V45，720 步，DONE/DONE）：**终局 **35035:174073**。第 10 天收工 **12841:17004**，第 11 天 **12176:16877**。对比上一节的中枢那局（第 10 天 7472:16495、第 11 天 3848:16180、终局 6365:157307）：这批未记录的改动把终局从 6365 拉到 35035，第 11 天从差 1.23 万收到差 4.7 千，是真实进步。但和「只种菜贴门口」那局（第 10 天 12897:14664）比，我方几乎一样、对方反而更高，倒瓜窗口没有再进步。

**这一局地里实际发生了什么（逐日收工表 + 中枢每天 hour 0 的决策理由）：**

1. **第 1–6 天，13 格空地一动不动。** 第 0 天买了 1 牛 1 羊、种 10 格瓜，口袋剩 541。此后六天中枢每天的理由都是 `keep_only`：`affordable_care_reserve` 要求「把现有 10 格瓜和 2 头畜养到第一次能卖货那天」的全部工钱口粮，这个数比口袋里的 239–541 还大，于是买种、买畜、买地全部停摆。13 格好地空了六天，对方同期 25 格早已种满。开局把钱买成牛羊，反过来锁死了种地。
2. **整季 29 天里，中枢只有 5 天真的买了种**（第 0、9、11、15、29 天）。其余全是 `land_empty`——意思是「有空地所以不买地」，但同一天也没补种，空地就这么挂着。
3. **卡点一：熟瓜整天封印种地。** `planting_allowed` 只在 hour 0 拦「地里还有熟瓜就先别买种」，但执行器 `route14_agent.py` 第 438 行另写了 `if planting_allowed(observation) and not dump_melon:`，`dump_melon = oneshot_harvest_pending(observation)` 是**全天**生效的。我们整季主种瓜，瓜熟了会在地里挂好几天，于是全局 **177/719 小时**禁止下种。第 28 天 23/24 小时被封、38 格空地。这条规则本来只为第 10 天「先卖再补种」而写，现在变成常态封印。
4. **卡点二：`future_care_hires` 一返回 None 就连 1 格都不种。** 第 22 天口袋 1.8 万、18 格空地、当天路线排得开（`day_schedule` witness 通过），中枢仍然 `filled=0`。实测原因：`future_care_hires` 对 18 格返回 None，**对 1 格也返回 None**。这道门问的不是「多种这一格会不会照料不过来」，而是「我现有这摊子将来每个关键日 12 个人能不能排完」；现有摊子（11 头牛每天喂+照料+收货+扛货，加 26 格瓜浇水）本身就排不完，于是门永久关闭、再也不准扩张。第 19 天同样是 5 格空地、返回 None、种 0 格。
5. **后果：买了 50 格地，后期一半是空的。** 第 23–28 天收工空地 32/33/34/34/17/20 格，同期口袋 2.2–3.0 万现金闲置。第 13 天买地后到第 20 天现金从 12462 一路掉到 5451——12 个人的工钱加 11 头牛的口粮，比 26 格瓜的产出还多，那七天是净亏的。
6. **最后一天才种 38 格。** 第 29 天 `filled=38`，种下去当季收不了，纯亏种子钱。
7. **畜养成了 11 牛 1 羊。** 不是之前记录的羊群。第 11–18 天几乎每天买一头牛（`animal=COW`），把钱持续抽走，而牛的产出没能覆盖它带来的人手挤占。

**结论（给下一位）：**这套「订死照料才准种」的三道容量门方向没错，但当前实现把「现有摊子照料不完美」当成「禁止一切新增投资」，所以 50 格地长期半空、两三万现金躺着。三个候选修法各自独立、**须用户说开始才改**：(a) `dump_melon` 只在 hour 0 生效，和 `planting_allowed` 对齐；(b) `future_care_hires` 改成比较「加这一格的增量活」而不是要求现有摊子完美可排，或者至少在现有摊子本身就 None 时不要连带否掉新增；(c) 剩余天数不够作物长成时不买种（第 29 天那 38 格）。不要三个一起改，否则无法归因。不报名、不提交。

### 2026-09-22：来不及长成就不买种（用户选了这一刀，已落地）

用户从上节三个候选里选了 (c)。**只改这一处，另两处（熟瓜整天封印、`future_care_hires` 连 1 格也否）没动**，方便归因。

**改了什么。** 病根在 `stable_crop_name` 最后一行 `return STABLE_CROPS[0]`：那是个无条件兜底，不管剩几天都回一个小麦，`choose_next_line` 就照着下种。所以第 29 天（最后一天）还种了 38 格，当季根本收不了，纯亏种子钱。

新增 `crop_finishes_in_season(crop, days_left)`：官方规定到 `first_yield_day` 就能收（可以收不满），最后一个能动的日子是第 29 天，所以判断是 `CROP_FIRST_YIELD_DAY[crop] <= days_left - 1`。`choose_next_line` 的三条出口（紧俏配额、轮种兜底、按金钱/天兜底）现在都要过这道门，全都过不了就 `idle`，不买种。没有写死作物名，也没有写死第几天。

**怎么验的。** 单测 42 → **43**，全绿。新测 `test_no_seed_for_a_crop_that_cannot_reach_harvest`：最快的麦/萝需要 2 天才第一次能收，所以第 27 天仍下种、第 28 天起 `choose_next_line` 必须 `idle`、`empty_slot_plan` 必须空。（第一版测试断言第 27 天种小麦，实测是萝卜——同样来得及且金钱/天更高，是测试预期写错，不是代码错，已改成断言性质而不是作物名。）

**seed 0 一局（route14 vs 冻结 V45，720 步，DONE/DONE）：**终局 **35414:174073**，改前 35035:174073，**+379**。买种市场单 27 → 24。逐日收工表第 0–27 天与改前逐行完全一致，第 28 天 33136 → 33265，差异全部来自省下的种子钱。中枢第 29 天的理由从 `fill,land_empty` 变成只有 `land_empty`，不再规划那 38 格。

**边界：**这是一刀纯粹的止损，不是增长。它只把本来必然打水漂的种子钱收回来，量级几百。上节诊断的两个大洞仍在：50 格地第 23–28 天仍空 32/33/34/34/17/20 格，口袋 2.2–3.0 万现金仍然闲置；第 13–20 天买地后现金仍从 12462 掉到 5451。下一刀要真的把空地用起来，得动 (a) 熟瓜封印只在 hour 0 生效，或 (b) `future_care_hires` 改问增量活。仍未报名、未提交。

### 2026-09-22：解开熟瓜封印 —— 三种配置全部变差，**已完整退回**

结论先写：**这一刀失败，代码已回到解开之前的状态**（`35414:174073` 与改前逐位一致，43 单测全绿）。下面三节是过程记录，`lab/` 里已无残留。

**seed 0 三组干净对照（route14 vs 冻结 V45，720 步，全 DONE/DONE）：**

| 配置 | 我方 | 对方 | 差距 |
|---|---:|---:|---:|
| A 原样（补种和盖棚都全天封） | **35414** | 174073 | **约 13.9 万** |
| B 全解开 | 33319 | 199231 | 约 16.6 万 |
| C 只解开补种、盖棚仍全天封 | 32732 | 179371 | 约 14.7 万 |

**用户提的「按播种日预算成熟时刻、清晨先分好收货队和补种队」已实现并实测，是 no-op，不是方向错。** 做法：新增 `harvest_haul_turns`（按 `planted_day` 而不是 `yield_units` 算当天该熟的格子，每格计一趟 `2×门口距离+2`）和 `delivery_crew_size`（当天成熟活量 ÷ 剩余小时 = 该留几个人），执行器按 `人数 − 留守人数` 给补种设并发上限，并在果子陆续成熟、上限收紧时把走在半路的补种工**放回**收货链。插桩确认这道门 719 小时全部被调用、其中 56 小时收紧到 0，**但终局与不加它一字不差**。原因：补种同时占用的人数本来就很少（12 格空地也只有两三个人在种），并发人数从来不是瓶颈。这段代码已随退回一起移除，思路记在这里备用。

**这里有个必须纠正的认知（上一节我判断错了）：**上一节说失败原因是「补种把人从收货—扛货—挂卖链上抽走」。**这是错的。**C 配置（只解开补种）反而把 `place_commands` 从 320 提到 **425**、`sell_units_requested` 从 666 提到 **865**——送货和卖货都比原样**多**了一大截，终局却更低（32732）。所以人手从来没被抢走，问题在**卖的价钱**。

**真正的机制，seed 0 每天 12 点的市场报价（C 配置）：**

- **西瓜价被我们自己砸穿。** 第 9 天 271 → 第 10 天 200 → 第 11 天 **129** → 第 20 天 **91** → 第 29 天 **4**。我们整季主种瓜，第 12 天起地里长期 MELON22–27，卖进的是自己砸出来的地板价。多种多卖只是把单价压得更低，`sell_units` 865 > 666 而现金反而更少，就是这个原因。
- **小麦价被我们自己抬高，对手靠它赚钱。** 第 0 天 28 一路爬到第 29 天 **47**。我们买小麦是为了喂牛；对手地里长期 WHEAT24–38，第 23 天 WHEAT38。我们越养畜、越买粮，越是替对手把卖价顶上去。B 配置盖棚不受限、牛涨到 16 头、买粮最多，对方终局就最高（199231）。

**所以「熟瓜封印」根本不是主要矛盾。** 它拦住的那 177 小时补种，拦掉的是一批卖不出好价的瓜，所以拦着反而更好——原样最优不是因为封印聪明，是因为**我们下游的卖价已经崩了，上游多产等于白干**。

**下一刀应该打的地方（须用户说开始，不要再动封印）：**

1. **不要整季只种一种货。** 第 11 天起瓜价就 129 了，金钱/天的排序应该早就把瓜换掉，但 `rank_crop_lines` 用的是**当前**报价，没有算「我自己再卖 20 格进去会把价压到多少」。缺的是「我方供给对价格的自压效应」。
2. **喂畜的粮不要全靠买。** 自己种麦喂畜，既省钱又不替对手抬价。现在是一边买粮抬价、一边让对手卖粮。
3. 第 1–6 天 `keep_only` 冻结 13 格（开局买牛羊掏空现金）和 `future_care_hires` 连 1 格都否，这两个洞仍未动。

未报名、未提交。

#### 过程记录（已退回，仅供追溯）

**改了什么。** `lab/route14_agent.py` 原本写 `if planting_allowed(observation) and not dump_melon:`，其中 `dump_melon = oneshot_harvest_pending(observation)` 是全天生效的；而 `planting_allowed` 只在 hour 0 拦。同一份代码里 `assignment_is_current` 校验 PLANT 时只看 `planting_allowed`，所以两处语义本来就不一致。这一刀删掉 `and not dump_melon`，让「地里还有熟瓜就先别下种」只在早上生效，并清掉随之悬空的 `oneshot_harvest_pending` import。单测 43 → **44**，全绿；新测 `test_ripe_melon_only_holds_seeds_back_at_dawn`：地里挂着一颗熟瓜、口袋有种、雇工就站在空地上时，hour 0 不许下种、hour 1 必须下种。

**seed 0 一局（route14 vs 冻结 V45，720 步，DONE/DONE）：终局 `33319:199231`，改前 `35414:174073`。我方 −2,095，对方 **+25,158**，差距从约 13.9 万扩大到约 16.6 万。这一刀应当判为**失败**。**

**为什么变差，telemetry 说得很清楚（改前 → 改后）：**

- `plant_commands` 99 → **122**：种得更多了，改动本身生效了。
- `place_commands` 320 → **216**（−104）：往仓库送货的次数掉了三分之一。
- `sell_units_requested` 666 → **539**（−127）：少卖了 127 个货。
- `harvest_commands` 159 → 159：**收的量一模一样**。

所以不是没收成，是**收下来的货没送进仓库、没卖掉**。补种把雇工从「收货—扛去仓库—挂卖」这条链上抽走了。任务优先级里 PLACE 是 97、PLANT 是 75，本该卖货优先；但任务分配是**跨小时持续**的，雇工一旦被派去种远处那一格，就会一路走过去，`assignment_is_current` 还会让它保持。于是倾销窗口里人不在门口。

`animal_orders` 12 → 16 是连带副作用：补种和买畜共用 `choose_next_line`，解开封印同时也放开了买畜，牛从 11 头涨到 14 头，每天口粮把第 12–19 天的现金进一步抽干（第 12 天 9437，改前 12462）。

**结论修正（下一位必读）：**上一节体检把这条规则判为「本来只为第 10 天写、现在变成常态封印」，方向对，但把它当成**冗余**是错的。它实际在干活：拦住补种去抢倾销窗口的人手。真正的毛病是这条规则**太钝**——它按「地里任何地方还有一颗熟的一次性作物」一刀切全天禁种，把整季 177 小时都封掉。

**下一刀的候选（须用户说开始）：**

1. 把封印条件从「地里还有熟瓜」换成「**手上/仓库里还有没卖掉的货**」（`carrying_sale_goods` 一类）：只在真的有货待卖时保护卖货链，地里挂着熟瓜但货已清完就允许补种。
2. 让 PLANT 任务**不跨小时保持**：有更高优先级的收货/扛货出现时立刻放弃补种，避免雇工走一半被锁住。
3. 把买畜从这一刀里摘出去，单独定畜群上限，避免解开补种顺带把牛买到 14 头。

第 0–11 天对照（用户要求先看这段）：第 10 天 `13995:17169`（改前 `12841:17004`），现金差从约 4,163 收到约 3,174，**这一段是改好了的**；第 11 天 `11933:17095`（改前 `12176:16877`），差从约 4,701 扩到约 5,162。所以早期倒瓜窗口确实受益，亏在中后期卖货链被抢人。未报名、未提交。

### 2026-09-22：把「自己砸价」算进金钱/天（用户说开始；**seed 0 大幅改善，已保留**）

用户从上节候选里选了「选种时算上我自己这么多货卡进去会把价压到多少」。**seed 0 终局 `60423:188901`，改前 `35414:174073`：我方 35414 → 60423（+25,009，+71%），差距从约 13.9 万收到约 12.8 万（改善约 1.02 万）。** 45 单测全绿。这是 route14 至今最大的单刀改善。

**先记住官方定价模型，这是整件事的根**（直接读本机引擎 `kaggriculture.py`，不是猜的）：`price = base ± amp × f(|库存 − 10000|)`，`amp = target × base / f(T)`。过剩侧各货的形状差别极大：

| 货 | 底价 | 过剩形状 | target | T | 卖光 T 个后的价 |
|---|---:|---|---:|---:|---:|
| MELON | 250 | **sq（平方）** | **3.60** | 300 | 250 → **地板价 1** |
| WOOL | 200 | sq | 3.20 | 105 | 崩 |
| MILK | 160 | linear | 1.60 | 122 | 崩 |
| STRAWBERRY | 120 | linear | 1.60 | 100 | 120 → 约 −72（地板） |
| TOMATO | 60 | sqrt | 0.60 | 200 | 60 → 24 |
| CARROT | 35 | sqrt | 0.70 | 450 | 35 → 10 |
| **WHEAT** | 25 | **log（对数）** | **0.20** | 400 | 25 → **20** |
| **EGG** | 50 | **log** | **0.20** | 332 | 50 → **40** |

西瓜是全场最禁不起过剩的货：`价 = 250 − 0.01 × (超出量)²`。卖 100 个 → 150；150 个 → 25；180 个 → 地板价 1。小麦和鸡蛋是对数，几乎砸不动。**所以对手整季种 24–38 格小麦不是笨，是小麦砸不下去；我们整季种 22–27 格瓜才是把自己的钱烧掉。** 这条表后续任何选种/选畜决策都应当先看。

**改了什么。** 从引擎 import 官方 `market_price`（加 `lru_cache`，纯函数不影响准确性；import 失败则退回原来的现价口径）。新增：

- `batch_sale_revenue(item, inventory, units)`：引擎卖货是**逐单位**结算、每单位按它落地前的库存报价，所以这里就按逐单位求和，和引擎完全一致（有单测对账）。
- `own_supply_map(observation)`：一次扫描算出每种产品我们**已经欠市场**多少单位 = 地里在长的（按 `CROP_YIELD`）+ 仓库里的 + 工人手上的。这些货会先砸进去，决定下一格真正能卖什么价。
- `money_per_day` / `animal_money_per_day` 增加可选 `inventory` / `own_units`：把这一格/这一只的产量排在自家已有存量**后面**定价，而不是信当前报价。
- 四个排序入口（`rank_lines`、`rank_crop_lines`、`rank_animal_lines`、`stable_crop_name`）、`slot_money_per_day`、`line_season_value`、以及中枢的买畜/买地估值全部改走这个口径。`line_season_value` 还按「第 2 格比第 1 格便宜」逐格走曲线，不再拿第一格的价乘以 25 格。

**这一局地里实际发生了什么（seed 0 逐日收工表）：**

1. **第 10 天倒完瓜后，地里自动从瓜换成草莓。** 第 10 天收工 `CARROT3 MELON5 STRAWBERRY1`，第 12 天起稳定 `STRAWBERRY26–28`，瓜降到 2–7 格。改前是整季 `MELON22–27` 一直砸自己的瓜市。没有写死任何作物名，是金钱/天自己算出来的。
2. **后期现金起飞。** 第 23 天起 36088 → 42488 → 45926 → 50872 → 55532 → 59968。改前同期只有 22948 → 33136。
3. **牛也被同一把尺压住了。** 牛停在 8 头（改前 11 头），因为牛奶是 linear 1.60、自家奶一多就不值得再买。
4. **种得更少但更值钱。** `plant_commands` 99 → 64，`seed_orders` 26 → 18。
5. **第 10 天略降、第 11 天略降：**第 10 天 `12281:17004`（改前 `12841:17004`），第 11 天 `11433:16877`（改前 `12176:16877`）。前 11 天这一段轻微变差约 600–700，收益全在第 12 天以后。若下一位要继续磨第 11 天，注意这一刀是拿早期一点钱换后期一大截。
6. 对方也涨了（174073 → 188901）：我们不再往瓜市倒货，瓜价没被砸穿，对手第 10–11 天的瓜卖得更好。差距仍然明显收窄，所以这笔交换是划算的。

**运行时间从约 23 秒涨到约 87 秒/局。** 已确认**不是**定价代码慢：给 `market_price` 加缓存、把 5 次全地块扫描合并成 1 次，终局都是一字不变的 `60423`，耗时也不变。真正原因是**农场变大了**——整季维持 28 格草莓 + 50 格地，原有的容量门（`day_schedule`、`future_care_hires`、`route_mandatory_tasks`）要排的活多得多，profile 显示时间几乎全在那三处。这是既有架构随规模上升的成本。**若后续要跑 112 局联赛，先解决这个，否则一轮约 2.7 小时。**

**边界：**只打了 seed 0 一局，不能当作泛化结论，也不是榜单预测。未报名、未提交。

**下一刀候选（须用户说开始）：**

1. **自己种麦喂畜。** 现在喂畜的粮全靠买，小麦价被我们从 28 抬到 47，而对手正靠卖小麦赚钱。自己种麦既省钱又不替对手抬价，而且小麦过剩曲线是对数的，多种也砸不动。
2. **卖货也该按这条曲线分批。** 现在是「到了就卖」，一小时把 24 个瓜倒进去等于自己吃平方衰减。可以按边际价决定这一小时卖几个。
3. 开局第 1–6 天 13 格空地被 `keep_only` 冻结、`future_care_hires` 连 1 格都否，这两个洞仍未动。
4. 容量门的性能，见上。

### 2026-09-22：剩下这 11.6 万差在哪（只读诊断，未改代码）

用户问：还是差一截，主要差在哪天、为什么对方赚到了我们没赚到，是人手不够、没卖好、还是种错养错。对 seed 0 当前这局（`60423:188901`）逐日算「我方当日净赚 / 对方当日净赚 / 当天花在种子·买粮·买畜·工钱上多少 / 当天卖出了什么 / 收工时空地·没浇水·草各几格」。**一行代码没改。**

全季我方净赚 56,770、对方 172,732，差 **115,962**。按时段拆：

| 时段 | 落后 | 占比 |
|---|---:|---:|
| 第 0–11 天 | 5,319 | 4.6% |
| **第 12–21 天** | **68,392** | **59.0%** |
| 第 22–28 天 | 29,099 | 25.1% |
| **第 29 天（最后一天）** | **13,152** | **11.3%** |

**第一主因：第 12–21 天十天空转，占差距近六成。** 这十天里第 13、15、16、17、18 天**一个货都没卖出去**，第 14 天只卖了 9 个奶 1 个麦。当日净赚是 −1327、+1754、−409、−704、−390、−481、+984——基本零，而对方同期每天稳定进账 3,379–11,190。原因不是人手也不是卖法：第 11–12 天我们把地改成了草莓，**草莓 `first_yield_day=10`，第 12 天种下要等到第 22 天才第一次能收**。这十天地里全在长、一分钱不产，而每天照付约 330 买粮和工钱。第 23 天起草莓开始产出（STRAWBERRY36/13/9/13/9/12），现金立刻起飞——晚了十天。

**第二主因（也是上一刀的直接续集）：自压修正没有在「同一批计划内」累计。** 第 12 天草莓看起来确实值：市场库存 9875 低于平衡点 10000、现价 **214**（`below_func=sqrt, below_target=0.70`，缺货时涨得很凶）。模型给第 1 格草莓 42.9/天，这没错。但手算同一时刻的边际值是：

| 第几格 | 1 | 5 | 10 | 15 | 20 | 26 |
|---|---:|---:|---:|---:|---:|---:|
| 金钱/天 | 42.9 | 40.5 | 37.6 | 33.9 | 27.7 | **14.0** |

第 26 格其实只值 14.0，是第 1 格的三分之一。**但 `empty_slot_plan` 一次排 26 格时，每一格都按 42.9 定价。** 根因：`choose_next_line(observation, counts=...)` 里 `counts` 只用来算 70/30 配额，定价走的 `rank_crop_lines(observation)` → `own_supply_map(observation)` **只读 observation，不读这一批已经排进去的计划**。所以刚落地的自压修正在跨天上生效了（第 10 天后瓜被换掉），在**同一批内没生效**，于是一次铺了 26 格慢货。

**第三：最后一天完全没清仓。** 第 29 天对方 +12,959，我们 −193，当天只卖出 1 个小麦。规则是仓库和手持都不计分、最后一天必须卖光。这一天白送 1.3 万，是单日最大缺口。

**第四：人手确实照顾不过来，但是次要。** 收工快照：第 21 天 9 格没浇、第 22 天 **19 格**没浇、第 23–28 天持续有 1–9 格草。旱死会变草、再变空地，属于持续渗血，但量级远小于上面三项。同期空地也一直挂着 6–19 格。

**第五：买粮整季约 9 千，全靠买。** 每天 300–530，而且把小麦价从 28 一路抬到 47，对手地里常年 24–38 格小麦正靠这个价卖钱。

**直接回答用户的问题：主要不是人手不够，也不是卖法不对，是「种错了时机」——在只剩 18 天时铺 26 格要等 10 天才开产的慢货，而且按第一格的价给全部 26 格估值；加上最后一天没清仓。**

**下一刀候选（须用户说开始，一次只改一个）：**

1. **让自压修正在同一批计划内累计**（最小、最直接、和已验证有效的那刀同源）：`choose_next_line` / `empty_slot_plan` 定价时把本批已排的同种货计入 `own_units`。第 26 格就会显示 14.0 而不是 42.9，慢货自然铺不了 26 格。
2. **最后一天无条件清仓**（独立、几乎无风险，对准单日 1.3 万）。
3. **按剩余天数卡首产**：只剩 18 天时，要等 10 天才开产的货应当明显降权，而不是只按占格天数平摊。
4. 自己种麦喂畜（省 9 千 + 不替对手抬价；小麦过剩曲线是对数的，多种也砸不动）。

未报名、未提交。

### 2026-09-22：批内累计自压 —— seed 0 变差，**已完整退回**（但挖出两个重要发现）

用户选了上节候选 1。实现：给 `rank_crop_lines` / `rank_animal_lines` / `hot_crop_names` / `stable_crop_name` / `choose_next_line` 加可选 `supply` 参数，`empty_slot_plan` 维护一本「本批已排的货」台账，每排一格就把这一格的产量记进去，下一格按更低的价估值。

**结果：seed 0 `42606:177479`，改前 `60423:188901`。我方 −17,817，判失败，已完整退回**（现回到 `60423:188901`，逐位一致，45 单测全绿）。

**发现一（结构性，独立于这一刀，下一位务必知道）：买畜完全不比价。** `choose_next_line` 里养殖分支是：

```
if occupied_livestock < animal_cap and allow_animal:
    for _kind, name, _value in rank_animal_lines(...):
        if can_afford_animal(...):
            return "animal", name, "livestock"
```

只要养殖名额（`livestock_tile_cap` = 已解锁格数的一半）没满、钱够，就**立刻**返回第一只买得起的畜，**从不和作物比金钱/天**。`rank_animal_lines` 只决定买哪一种，不决定该不该买。所以把畜的产量记进压价台账对结果毫无影响（实测加与不加终局都是 42606）。畜群规模现在由「50% 名额 + 现金」决定，不由经济性决定 —— 这是一个尚未评估的独立杠杆。

**发现二：草莓不是种太多。** 静态模拟第 12 天那批 25 格，批内累计给出的是 3 格瓜 + 14 格草莓 + 8 格萝卜，草莓的金钱/天从 47.1 只降到 41.0——因为 29 格草莓约 116 个，而当时市场库存 9875 低于平衡点 10000，**确实吸收得下**。种 29 格草莓的判断是对的。

**⚠️ 本节曾写过「真正的损失在卖法：第 23 天一天倒 36 个草莓砸价」，该结论已被实测推翻，见下一节「自砸损失只有 1,306」。不要据此去做分批卖。**

**为什么批内累计会变差（诚实说明，未完全定论）：**真实局第 12 天它把 **6–7 格番茄**放进了计划（番茄首产 8 天、占格 11 天、金钱/天只有 16–19，是最差的一档），这 7 格一直占到第 21 天；而且第 22 天起空地从 10–19 格恶化到 25–37 格。一个已确认的结构原因是 `HOT_CROP_RATIO = 0.70` 是**相对第一名**的门槛：压低第一名的同时门槛自己下降，本来够不上的慢货就挤进「赚钱作物」名单。若将来要重试批内累计，必须先给「赚钱作物」加一个**绝对下限**，而不是只用相对 70%。

**下一刀候选（须用户说开始）：**

1. **按过剩曲线分批卖（最高优先）。** 每种货一小时卖多少，按边际价决定：对数曲线（小麦、鸡蛋）可以一次倒完；线性（草莓、牛奶）和平方（西瓜、羊毛）要摊开。直接对准上面发现二，也顺带修第 10 天倒瓜砸价。
2. **最后一天无条件清仓**（对准单日 1.3 万，独立、低风险）。
3. **让买畜也过金钱/天**（发现一）。
4. 自己种麦喂畜。

### 2026-09-22：自砸损失实测只有 1,306 —— 「按曲线分批卖」这条路不值得做（只读，未改代码）

用户要求做分批卖，但同时提出一个关键质疑：第 10 天倒瓜砸价**是否必要**，因为我们不倒对面也一样倒，不如先倒赚启动资金。为了先验证「分批卖」到底值多少钱，对当前这局（`60423:188901`）逐笔 SELL 记录卖前市场库存，用引擎定价算出实得金额，和「假如每个单位都能拿到首单价」对比，差额就是自砸损失。**一行代码没改。**

| 货 | 卖出 | 实得 | 若都按首单价 | 自砸损失 | 实际均价 | 平衡价 |
|---|---:|---:|---:|---:|---:|---:|
| MELON | 75 | 14,839 | 15,625 | **786** | 197.9 | 250 |
| MILK | 151 | 36,856 | 37,174 | 318 | **244.1** | 160 |
| WOOL | 34 | 6,683 | 6,790 | 107 | 196.6 | 200 |
| STRAWBERRY | 92 | 23,414 | 23,509 | **95** | **254.5** | 120 |
| WHEAT | 36 | 1,273 | 1,273 | 0 | 35.4 | 25 |
| CARROT | 4 | 150 | 150 | 0 | 37.5 | 35 |
| **合计** | **392** | | | **1,306** | | |

**整季自砸损失只有 1,306，对 115,962 的差距毫无意义。分批卖的收益上限就是这个数，因此不做。** 全季单笔最大损失是第 20 天卖 25 个瓜、损失 661，也只有几百。

**更重要的是这张表推翻了「我们在砸自己的价」这个前提：实际成交均价普遍远高于平衡价。** 牛奶卖到 244（平衡价 160）、草莓 254（平衡价 120）、小麦 35.4（平衡价 25）。说明**市场是缺货的**——商店和 Town Center 每天的消耗比我们的供给快，库存长期在 10000 以下。只有西瓜是真过剩（卖前库存 10102、均价 197.9 低于平衡价 250），羊毛勉强打平。

**因此用户对第 10 天倒瓜的判断是对的，而且比原判断更强：** 瓜是全场唯一成交价低于平衡价的货，但即便如此，整个瓜季自砸也只损失 786。对手第 9 天的田里明明白白有 12 格 `planted_day=0`、`yield_units=5` 的瓜（对手地块字段完全公开，含 `crop`/`planted_day`/`yield_units`/`watered_today`），第 10 天必然倒货。**先倒拿启动资金是对的，不要为了护价而慢卖。**

**真正的缺口是产量，不是价格。** 整季我们一共只卖出 **392 个单位**，对手 75 格常年满种。既然实际售价都在平衡价之上，**多产一格就是净赚**，不存在「种多了卖不掉」的问题（西瓜除外）。当前这局第 12 天以后长期空着 6–19 格（50 格里），第 25–28 天空 17–19 格；加上第 12–21 天那十天零产出，和最后一天没清仓。

**下一刀候选（须用户说开始）：**

1. **把空地真正种上（对准产量，收益最大）。** 两个已定位的闸：开局第 1–6 天 `keep_only` 把 13 格冻死（`affordable_care_reserve` 要求的养护准备金高于口袋）；中后期 `future_care_hires` 返回 None 时**连 1 格都不给种**（它问的是「现有摊子将来每天 12 人能否全排完」，而不是「多种这一格的增量活」）。
2. **最后一天无条件清仓**（单日 1.3 万，独立、低风险）。
3. **让买畜也过金钱/天**（现在名额没满、钱够就买，从不比价）。牛奶均价 244、平衡价 160，畜其实很值，所以这一项可能是**放开**而不是收紧。
4. 自己种麦喂畜（省约 9 千 + 不替对手抬麦价）。

**不要再做：**分批卖（上限 1,306）、熟瓜封印（三种配置全部更差）、批内累计自压（−17,817）。

### 2026-09-22：人手上限改成经济上限 —— 全部更差，**已完整退回**（但结论很硬）

用户指出：`future_care_hires` 不该把人手当成上限 12，人可以多雇、只是更贵；而且每开一格都该做投资账——算清照顾到收获要多少人工、划出照顾资金、收获的钱能不能赚回来，都 OK 就种。

**先确认一个事实：引擎对雇人数没有任何上限。** `_do_hire` 只查 `farm["money"] < cost`，`cost = FARM_HAND_COST_MULT × fib(n)`，`FARM_HAND_COST_MULT = 1`。**12 完全是我们自己写死的常量。** 用户这一点是对的。

**但 fib 是指数的，这是这条路走不通的原因。** 当天第 k 个雇工的价钱和整支队伍的日工钱：

| 队伍人数 | 12 | 13 | 14 | 15 | 16 | 17 | 18 | 19 | 20 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 第 k 人单价 | 144 | 233 | 377 | 610 | 987 | 1,597 | 2,584 | 4,181 | 6,765 |
| **当天总工钱** | **376** | 609 | 986 | 1,596 | **2,583** | 4,180 | 6,764 | 10,945 | **17,710** |

我们整季净赚 56,770 / 29 天 ≈ **每天约 2 千**。所以第 16 个人一天就要 2,583，比农场一天挣的还多；20 个人一天 17,710。

**三组 seed 0 实测：**

| 人手上限 | 我方 | 对方 | 差距 |
|---|---:|---:|---:|
| **写死 12（原样）** | **60,423** | 188,901 | **12.8 万** |
| 只看现金够不够（`affordable_crew`，无 fib 预算约束） | 5,029 | 170,695 | 16.6 万 |
| 看「日工钱 ≤ 这块地一天能挣的钱」 | 36,447 | 171,973 | 13.6 万 |

第一种雇到 13–20 人，第 27 天一天亏 13,391，终局只剩 5,029。第二种雇到 12–14 人，仍差 2.4 万。**写死 12 反而是三者最好的。** 已完整退回（现回到 `60423:188901`，逐位一致，45 单测全绿）。

**这一刀最有价值的发现：那道闸打开了、地也真的种上了，但不赚钱。** 提高上限后 `future_care_hires` 从 `None` 变成了 12 和 7（第 19、24 天实测），当天和次日路线都 witness 通过，第 24 天空地从 hour 5 的 25 格种到收工 12 格。**所以「空地种不上」确实是这道闸卡的，抬高上限能解开。** 但多雇那几个人的工钱比多种那些格的收成更贵，净值为负。

**所以真正的约束不是「人手不够」，而是「人手太贵」。** 在我们约 2 千/天的收入水平上，第 13 个人以后的边际工钱就吃掉了边际产量。要靠填地翻盘，得先让**单格产出**值钱到能养得起第 13–16 个人，或者找到不加人也能填地的办法（比如少走路、活儿更集中）。写死的 12 虽然是拍的，但恰好落在经济上限附近。

**用户要的「每格投资账」只做了一半。** 已实现的是「日工钱不超过这块地一天能挣的钱」这个预算门（`farm_money_per_day`，空地按当前最好的线计价，否则开局地里没东西就永远雇不起人）。**没有实现**的是「这一格从种下到收获的完整回本账」——按用户描述应当是：这一格的预计收获金额 ≥ 种子 + 照顾它到收获期间分摊的人工 + 划出的照顾准备金。当前 `_fill_plan` 只有流动性检查（钱够不够），**完全没有回本检查**。若下一位要继续这条线，这才是该补的那一半，而且要配合「不抬人手上限」一起测。

### 2026-09-22：隔天浇水 —— 五种变体全部更差，**已完整退回**（但引擎事实非常重要，务必读）

用户提出：植物不必每天浇水、动物不必每天照顾，隔天可能综合更好，还能减轻人手压力；另外同一个人应该干相邻田地的活。**第一条已实现并测了五个变体，全部更差，已退回。第二条（相邻田地）尚未做。**

**先记住这几条官方规则（直接读引擎 `kaggriculture.py`，永久有用）：**

1. **变草条件是「连续 2 天没浇」**（`consecutive_unwatered >= 2`），而**播种当天就算 1 天没浇**（`_new_plant` 里 `consecutive_unwatered = 1`）。所以播种当天必须浇；之后浇一天隔一天，永远不会死。
2. **动物跑掉条件是「连续 2 天没喂」**（`consecutive_unfed >= 2`）。隔天喂同样不会跑。
3. **浇水对产量的作用分两种，这一点决定一切：**
   - **草莓、番茄（ongoing）**：产量在 `_daily_refresh_plants` 里按 `interval` 自动出，**完全不看浇没浇水**；只有施肥加成要求当天浇过。所以对这两种，浇水**纯粹只为防变草**。
   - **小麦、萝卜、西瓜（一次性）**：产量**只**来自 WATER 动作，且只在 `[(max_yield_day+1)//2, max_yield_day]` 窗口内 +1。窗口分别是麦第 2–4 天、萝第 2–3 天、**瓜第 6–12 天**。所以**瓜在第 0–5 天浇水对产量毫无贡献**。
4. **动物产量**：`base = 1` 每个 interval 必给，**不要求当天喂**；但 `pending_care_bonus` 只在「当天既喂又照料」时 +1，且只在**生产当天有喂**时才结算。所以喂食既为不跑、也为拿照料加成。

**实测浪费量（当前这局）：整季浇水 605 次，其中 541 次（89%）对产量毫无贡献**，纯为防变草。按作物：草莓 408 次全部无效，西瓜 165 次里 101 次无效，小麦 24、萝卜 8 全部无效。所以用户的观察是**对的**，浪费确实巨大。

**但五个变体全部更差（seed 0，基线 `60423:188901`）：**

| 变体 | 我方 | 对方 | 挖草次数 |
|---|---:|---:|---:|
| **基线（每天都浇）** | **60,423** | 188,901 | **3** |
| 只在「会死」或「能加产量」时浇，浇水优先级仍 80 | 44,395 | 173,958 | — |
| 同上，救命浇水优先级提到 102 | 35,859 | 156,583 | — |
| 同上，救命浇水降到 98（在喂食 101 之下） | 48,879 | 185,832 | 39 |
| 同上，再把浇水和收获解耦（不再是 elif） | 48,296 | 184,214 | 33 |
| 余量浇水降为优先级 20 的闲时填充活 | 48,211 | 183,471 | 24 |

**失败原因链（每一步都实测定位，对下一位有用）：**

1. **优先级排错了两次。** 原本浇水是 80，低于收货 95、送货 97、喂食 101。每天浇时漏一天只是变成「1 天没浇」还有余量；改成隔天浇后**没余量了，而「今天不浇就死」的地却还排在收货后面**，第 21→22 天草莓一天从 28 格死到 13 格。提到 102 又压过喂食 101，**牛从 9 头掉到 1 头**——牛奶本是最大进账（36,856，占我方总收入六成）。落在 98 最好，但仍不如基线。
2. **收获和浇水是 `elif`，草莓永远拿不到浇水任务。** 草莓每 2 天产一次，只要手上有果子就一直走收获分支。它需要浇水的那天恰好也是有果子的那天。解耦后挖草从 39 降到 33，仍在死。
3. **同步到期。** 所有地块同一天种下、同一天浇，于是会在**同一天一起到期**。隔天浇把负载从「每天匀着浇」变成「一天全要浇」，对贪心派工来说比原来更难排。
4. **余量浇水改成闲时填充后，浇水次数回到 595（≈基线 605）**，说明人手其实没紧到会跳过；但挖草仍 24 次，因为救命优先级 98 重排了工作顺序，`care_commands` 从 190 掉到 79、`feed_commands` 从 220 掉到 141。

**结论：基线那套「每天全浇」虽然 89% 是浪费，但它承担了一个隐性作用——给每块地留一天余量，把贪心派工的失误全部吸收掉。** 省下约 200 个工人回合，代价是死掉 20–36 株（每株草莓约值 1,000），净亏。要让这个想法成立，必须先解决**同步到期**：让地块错开到期日（例如每天只浇一半），而不是全场同时。这是一个新设计，未获授权。

**用户的第二条（同一个人干相邻田地）尚未实现。** 现在派工是 `min(-priority, distance, actor)`，已经按「离得最近的人」分配，但**没有**「一个人把附近几格连着干完」的成批逻辑；工人每小时都会被重新分派到全局最近的任务，可能来回跑。这是仍然开放的方向，而且它不像隔天浇水那样需要牺牲余量，风险更低。

### 2026-09-22：脚下有活就先干（用户第二条；**seed 0 改善，已保留**）

用户第二条：同一个人干相邻田地的活。**seed 0 终局 `62586:187896`，改前 `60423:188901`。我方 +2,163，对方 −1,005，差距从约 12.85 万收到约 12.53 万。** 46 单测全绿。这是当前最好版本。

**先量清了走路的代价（基线那局）：全季工人回合 5,911 个，其中干活只占 1,554（26%），走路 3,548（60%），闲着 809（14%）。而且有 1,068 次工人正踩在一块有活没干的地上却走开了**——因为它被派去了远处，中途路过也不停。

**改了什么（`lab/route14_agent.py` 的 `commands`）：**

1. **脚下有活就改干，但绝不为小活耽误大活。** 派工现在把优先级连同任务一起记在 `state["assignments"]` 里。每小时开头，若某工人**正站在一块有待办活的地上**、而它手上的任务在别处，**且脚下这活的优先级不低于手上那件**，就把它放回待分配池，让它当场干掉脚下的活。拿着货要去仓库的人不改道（`_carrying_goods`）。
2. **派工时零距离的活优先。** 选择键从 `(-priority, distance, actor, target)` 改为 `(min(distance,1), -priority, distance, actor, target)`：所有站着就能干的活先安排完，再考虑要走路的。

**归因（两条分别测过）：**

| 版本 | 我方 | 对方 |
|---|---:|---:|
| 基线 | 60,423 | 188,901 |
| 只加「脚下有活就改干」（不改排序键） | 62,078 | 188,901 |
| **两条都加** | **62,586** | **187,896** |

**中间踩过的坑，别重犯：**第一版没有「优先级不低于」这个保护，只要脚下有活就无条件改干，结果正在去收瓜的人被拉去浇草莓，终局掉到 **55,261**（比基线差 5 千）。加上保护后才转正。有意思的是，加保护后「踩着活走开」的计数反而从 579 升到 1,141——**这个计数不是目标，钱才是**；剩下的那些「走开」正是应该走开的（手上的活更值钱）。

**这一局的变化：**第 13 天起地里稳定 26 格草莓 + 萝卜 + 瓜，空地从基线的 6–19 格降到 4–14 格；第 19 天起现金明显走高（12,114 → 18,199 → 20,117 → 31,864 → 37,000 → 42,658 → 49,744 → 54,945 → 61,922），基线同期是 6,949 → 17,864 → … → 59,968。畜是 COW10 GOOSE1 SHEEP1。

**边界与仍然开放的方向：**只打了 seed 0 一局，不是泛化结论。走路仍占 59%，用户原话「同一个人干**相邻**田地的活」只实现了「脚下那一格」，**还没有实现「一个人把附近一片连着干完再走」的成批派工**——那是更大的一块，仍然开放。

### 2026-09-22：成批派工（一个人把附近一片连着干完）—— 结果分歧，**已退回上一版，需用户定夺**

用户要求做「一个人把附近一片连着干完再走」。做法是把派工目标从「优先级绝对压倒一切」改成**按每个回合换来多少价值**排序：到一块地要花 `distance` 回合再加 1 回合干活，所以 `value = priority / (distance + 1)`。零参数，用现有优先级。

**三个版本（seed 0，全 DONE/DONE）：**

| 派工方式 | 我方现金 | 对方现金 | 差距 | 干活占比 | 走路占比 |
|---|---:|---:|---:|---:|---:|
| **A 脚下有活优先（现行，已保留）** | **62,586** | 187,896 | 12.53 万 | 25% | 59% |
| B 纯按每回合价值 | 60,340 | 177,462 | **11.71 万** | 29% | 57% |
| C 先按优先级分档（`priority // 10`）、档内比价值 | 53,560 | 175,784 | 12.22 万 | 26% | 57% |

**C 明确淘汰。A 和 B 是一个真正的取舍，当前保留 A，理由写在下面，但这不是定论：**

- **B 的差距收窄（+8,188）完全来自对手掉钱**（187,896 → 177,462），**我方现金反而下降 2,246**。靠干扰共享市场得来的优势，换一个对手很可能不成立。A 那一刀是我方现金和差距**同时**改善，来源是自己多赚，更可信。
- B 的农场其实更满：第 13–22 天空地只剩 0–1 格（A 是 4–14 格）。但它把地填成了 **7 格番茄**（首产 8 天、金钱/天只有 16–19，最差的一档），干的活更多、赚的钱更少。
- 两者差 2,246（3.6%），**只有一个 seed，本身就在噪声量级内**。要在 A/B 之间做决定，应当先跑多个 seed。

**这一刀最该记住的结论：成批派工没有解决走路问题。** 走路占比只从 59% 降到 57%。原因是走路的大头不是派工顺序，而是**结构性的仓库往返**——每收一次货都要走回四个门口卸货再挂卖。要真正压下 59%，得动的是「货怎么回仓库」这一层（例如就近集中、减少空手往返），不是「先干哪件活」。

**仍然开放：**多 seed 验证 A vs B。仓库往返那一层已在下一节解决。未报名、未提交。

### 2026-09-22：扛着货顺路再收一格（成批派工的正确落点；**seed 0 双指标同时改善，已保留**）

按上一节结论去查走路到底花在哪。**关键引擎事实：工人手上拿多少货没有任何上限**（`_apply_unit_action` 的 `HARVEST` 直接 `_inv_add`，不检查容量；只有仓库有 `shedCapacity=100`）。

**走路拆解（版本 A 那局，3,718 个走路回合）：**

| 类别 | 回合 | 占比 |
|---|---:|---:|
| **空手 / 离开仓库** | **2,033** | **54.7%** |
| 扛着货 / 朝仓库 | 581 | 15.6% |
| 拿着饲料 / 离开仓库 | 488 | 13.1% |
| 空手 / 朝仓库 | 314 | 8.4% |
| 拿着饲料 / 朝仓库 | 223 | 6.0% |
| 手里抱着畜 / 离开仓库 | 55 | 1.5% |
| 扛着货 / 离开仓库 | 24 | 0.6% |

一半以上是空手从仓库往田里走。根因：代码规定「拿着货的人只能去仓库」（`if carrying and task.operation[0] != "PLACE": continue`），于是**每收一格都单独跑一趟**——出去 3 步、收 1 格、回来 3 步、卸货、再出去。而引擎根本不限制手上拿多少。

**改了什么（`lab/route14_agent.py` 派工循环）：**扛着货的工人现在也可以接 `HARVEST`，**条件是那块地的距离不超过它此刻到仓库门口的距离**——反正回家这段路已经欠下了，顺路捎一格不额外花钱。这一条是零参数的（用 `door_distance` 比较，没有拍任何半径）。同时给这种顺路收割的优先级设为 `HAUL_PLACE_PRIORITY + 1 = 98`，压过卸货 97，否则它会先回仓库。新增常量 `HAUL_PLACE_PRIORITY = 97` 取代原来散落的字面量 97。

**seed 0 结果：`63419:178435`，上一版 A 是 `62586:187896`。我方现金 +833，对方 −9,461，差距从约 12.53 万收到约 11.50 万（+10,294）。这是本轮唯一「我方现金和差距同时改善」的两刀之一，比 B 那种只靠对手掉钱的改善可信。** 47 单测全绿（新增 `test_loaded_worker_collects_a_ripe_tile_on_the_way_home`）。

**这一局的变化：**省下的人手转去养畜，**牛从 10 头涨到 14 头**；草莓稳定 26–27 格；空地降到 0–10 格（A 是 4–14）。扛货回仓库的走路从 581 降到 496，顺路绕出去的 `扛着货/离开仓库` 从 24 升到 110（这是预期的、便宜的绕路）。代价是第 10 天倒瓜略差（10,402 vs 12,202），收益全在第 20 天以后。

**走路仍占 52.7%（空手出门）。** 这一项的根因是**每天收工解散、次日重雇**，所有人都在仓库门口出生，必须走出去；12 人 × 29 天 ≈ 348 次出生，每次走 3–5 步就是约 1,400 回合，占了那 2,033 的大头。要再压缩只能减少「出门次数」而不是「出门距离」：例如把喂饲料（488 + 223 = 711 回合）也做成顺路捎带，或让高价值作物更贴门口。

### 关键文件

- 规则与单测：`lab/route14_economy.py`、`lab/route14_hub.py`、`lab/route14_capacity.py`、`lab/test_route14_economy.py`（47 tests）

### 2026-09-22：拿饲料出门也做成顺带 —— 两种变体都更差，**已退回**（瓶颈在投资端，不在人力端）

按上一节留下的方向做「拿饲料那 711 个回合顺带干活」。**已退回，当前仍是 `63419:178435`。**

**先量清了饲料行程的形态（737 个拿饲料走路回合）：**

- **265 回合（36%）脚下就有别的活**（浇水/收货/除草）却走过去了——和收割顺带完全同构。
- 293 回合离最近的饿畜只有 1 步，36 回合**正踩在一头饿畜上却走开**（那头畜被别人占了，`blocked`）。
- 61 回合手里拿着饲料但**根本没有饿畜**，纯浪费。

**两种变体（seed 0）：**

| 变体 | 我方 | 对方 | 差距 |
|---|---:|---:|---:|
| **不改（现行）** | **63,419** | 178,435 | 11.50 万 |
| 允许顺带，条件「不比去喂畜的剩余路程更远」 | 51,476 | 166,357 | 11.49 万 |
| 只允许脚下那一格（距离 0，完全不走路） | 50,946 | 165,858 | 11.49 万 |

**两种都掉约 1.2 万，而且距离 0 那版和宽松版几乎一样差——所以原因不是绕路。** 差距反而略好，但又是「靠对手掉钱」那种，不可信。

**真实链条（查逐日表定位）：畜没有跑掉，是买得更慢。** 第 13 天 5 头牛（好版本 7 头），收工 11 头（好版本 14 头）。而买种订单从 19 涨到 **29**，第 11 天草莓从 10 格涨到 **22 格**。省出来的人手让中枢多种了地，草莓种子 100 块一包把现金抽干，于是买不起牛——**而牛奶比草莓值钱得多**（牛奶成交均价 244、平衡价 160；草莓一株整季只出约 4 个）。

**结论：省人手本身是对的，但省下来的产能被中枢花在了更差的生产线上。** 这和之前两条发现对上了：(a) `choose_next_line` 里买畜**完全不比价**，只要养殖名额没满、钱够就买，所以现金一被种子占用就买不动；(b) 现金是买牛的唯一约束。**所以下一刀应该动投资端**——让买畜和买种在同一把尺上比金钱/天——而不是继续找人力可省的地方。在投资端修好之前，再省出人手大概还是会变成更多草莓。

### 2026-09-22：畜和种同一把尺、各自承担人工成本（用户说做；**seed 0 大幅改善，已保留**）

用户要求：买畜和买种在同一尺度比较，但成本要分开算清——畜要喂粮、照料，能一直活一直产；作物要浇水施肥；**两边的人力成本都要计入**。

**seed 0 终局 `75473:183641`，改前 `63419:178435`。我方现金 +12,054（+19%），差距从约 11.50 万收到约 10.82 万（+6,848）。两个指标同时改善。** 49 单测全绿。这是本轮最大的一刀。

**人工怎么定价（零参数，直接来自引擎）：**`labor_price_per_turn` = `fib_hire_cost(当前队伍人数) / 24`。理由：当天第 n 个雇工要 `fib(n)` 块、能干 24 个回合，所以一个工人回合的边际价就是这个。**队伍越大照料越贵**——

| 已有人数 | 0 | 4 | 8 | 11 | 12 | 15 |
|---|---:|---:|---:|---:|---:|---:|
| 下一人日薪 | 1 | 5 | 34 | 144 | 233 | 987 |
| 每回合价 | 0.04 | 0.21 | 1.42 | 6.00 | **9.71** | **41.12** |

**每条线要多少工人回合（剩 20 天，只算动作不算走路）：**

| 线 | 回合 | 构成 |
|---|---:|---|
| CARROT | 5 | 下种 1 + 浇水（占格 3 天）+ 收 1 |
| WHEAT | 6 | 下种 1 + 浇水 4 + 收 1 |
| MELON | 12 | 下种 1 + 浇水 10 + 收 1 |
| TOMATO | 16 | 下种 1 + 浇水 11 + 收 4 |
| STRAWBERRY | 21 | 下种 1 + 浇水 16 + 收 4 |
| SHEEP | 48 | 建棚取放 3 + **每天喂+照料 2×20** + 收 5 |
| COW | 50 | 同上 + 收 7 |
| GOOSE | 60 | 同上 + 收 17 |

作物照料到清地就结束；**畜是剩下每一天都要两个回合，不管那天产不产**——这正是用户说的「成本得分开算」。

**改了什么：**

1. `money_per_day` 增加 `labor_price` 参数，扣 `crop_labor_turns(crop, days_left) × 单价`。
2. `animal_money_per_day` 同样扣 `animal_labor_turns(animal, days_left) × 单价`（它本来已经扣了畜价和整季口粮）。
3. `crop_money_per_day` / `animal_line_money_per_day` 自动带上 `labor_price_per_turn(observation)`，所以所有排序入口都改口径了。
4. **最关键的一半：`choose_next_line` 现在真的比价。** 原来是「养殖名额没满、钱够，就直接返回第一只买得起的畜」，从不和作物比。现在分别算出「买得起的最好的畜」和「配额/轮种规则选出的作物」的金钱/天，**取高者**。

**新口径算出来的样子（第 6 天、剩 20 天）：**队伍 2 人时 羊 183 > 瓜 142 > 牛 120 > 鹅 45 > 萝 27 > 莓 23；队伍 12 人时变成 羊 160 > 瓜 130 > 牛 96 > 鹅 16 > 萝 11 > 莓 10。**吃人工多的线（鹅 60 回合、草莓 21 回合）掉得比省人工的线（羊、瓜）快得多**，这正是想要的效果。

**这一局的变化：**牛买得早得多——第 12 天 8 头（改前 6 头）、第 18 天 13 头并稳住；空地降到 1–9 格；第 23 天起现金 37,294 → 43,784 → 51,876 → 59,116 → 65,925 → 72,437。

**注意一个实现细节，写单测时会踩：**`animal_money_per_day` / `money_per_day` 的售价走的是**市场库存**那条官方曲线（`batch_sale_revenue`），**不看传进来的 `prices` 字典**（`prices` 只用于小麦口粮成本）。所以要在测试里让某条线变便宜，必须改 `market.inventory` 制造过剩，改 `prices` 没有任何作用。新测 `test_livestock_must_beat_the_crop_on_the_same_yardstick` 就是用库存超出 30 让羊从 183 掉到 55、低于瓜的 142，同时羊仍为正收益——证明这是**比价**而不是一刀否掉畜。

**边界：**只打了 seed 0 一局。人工成本模型**不含走路**（只算动作回合），所以它低估了远格子的成本；若以后要把走路也计进去，`door_distance` 是现成的。未报名、未提交。

### 2026-09-22：最后一天清仓（**已保留**）+ 对 V45 的剩余差距对账

用户要求先把最后一天全卖掉，再查还差在哪。**seed 0 终局 `79638:183574`，改前 `75473:183641`，我方现金 +4,165。** 50 单测全绿。

**清仓前第 29 天到底怎么亏的（逐小时实测）：卖货规则本身没错，`sell_quantity` 早就写了「最后一天全卖」。亏在货根本没收上来：**

1. **动物那段任务链是 `elif`：先喂食，喂过了才收货。**最后一天没粮可喂，于是永远走不到收货那一步——**地里 12 个牛奶 + 1 个草莓整天没人收**。
2. **最后一天雇 0 个人。** `hub_plan` 和 `market_orders` 各有一处 `if day >= SEASON_DAYS - 1: hires = 0`，本意是别浪费工钱，但 12 个人一天只要 376，而地里躺着约 3 千的货。只剩农夫一个，收不完。
3. **还在买小麦喂牛。** 现金从 75,866 一路倒跌到 75,522，整天工人只在 PICKUP 和 FEED。
4. **仓库那 14 个小麦因为「留作饲料」没卖。** 仓库不计分。

**改了什么：**新增 `liquidation_day(observation)`（`day >= SEASON_DAYS - 1`）。当天：动物直接走收货、不喂不照料；作物不浇水、不除草；`wheat_feed_order` 返回 None（不买粮）；卖货的小麦预留改成 0；**去掉两处最后一天清零雇工**（只保留 hour 23 不雇，因为新人要下一回合才出生）。改后第 29 天地里清空、挂卖 `MILK23 MELON3 WHEAT14 WOOL20 STRAWBERRY1`（改前只有 `MILK11 MELON3 WOOL20`）。

**剩余差距对账（我方全季净赚 74,529，对方 163,598，差 89,069）：**

| 时段 | 落后 | 占比 |
|---|---:|---:|
| 第 0–11 天 | 8,401 | 9% |
| 第 12–14 天 | 15,850 | 18% |
| **第 15–19 天** | **50,262** | **56%** |
| 第 20–28 天（我方净赚更多，反超） | −11,376 | — |
| 第 29 天 | 4,942 | 6% |

**第一主因（占 56%）：卖货「观察期」规则把第 15–19 天的收入全冻结了。** `EARLY_SELL_DAYS = 15`，第 15 天起进入 `watch_days(days_left)` = 剩余天数的 30%（剩 15 天 → 观察 4 天），观察期内**只记价不卖**，之后要现价回到峰值的 90% 才卖。实测这五天我方净赚 −1,267、−1,322、−1,322、−802、−887，卖出栏几乎全空；对方同期 +9,273、+9,345、+5,767、+9,763、+10,514。

仓库快照坐实了这一点：第 16 天起积压 `MELON10 MILK3`，到第 19 天涨到 `MELON25 MILK32`，第 20 天一次倒出 `MELON25 MILK56`。

**而且这期间价格是一直在涨的**，等峰值毫无道理：

| 中午报价 | 第 14 天 | 第 16 天 | 第 18 天 | 第 19 天 | 第 20 天 |
|---|---:|---:|---:|---:|---:|
| MILK | 254 | 261 | 277 | 279 | 272 |
| STRAWBERRY | 234 | 248 | 256 | 258 | 262 |
| MELON | 136 | 140 | 144 | 146 | **91** |

第 20 天瓜价从 146 崩到 91，正是我们自己一次倒 25 个砸出来的。所以观察期造成**双重损失**：五天没有现金进账（没钱就没法投资，复利吃亏），以及积压后一次倒货自己砸价。这条规则是 2026-09-20 用户定的「观察期 = 剩余整天数的 30%，之后达峰值 90% 才卖」，但当时还不知道**市场长期缺货、成交价普遍高于平衡价**（见前文 1,306 那节），所以「等好价」这个前提本身不成立。

**第二主因：第 12–14 天买地 + 买种 + 买畜集中花钱（15,850）。**第 12 天种子 2,010 + 买畜 800 + 买地，当天净亏 3,102。这是投资，未必是错，但和上面的现金冻结叠在一起就很痛。

**其他仍未解决的：**

- **买粮整季约 1 万**，每天 490–700，全靠买，还把麦价从 28 抬到 47 喂对手（对手常年 24–38 格小麦）。
- **后期旱死长草**：第 22 天收工 **21 格没浇**，第 24 天 9 格草、第 28 天 8 格草。人手在后期明显不够。
- **空地 5–16 格**仍然长期挂着。

**下一刀最该打的是卖货观察期**（对准 56%），而且证据很硬：市场缺货、价格在涨、积压还自己砸价。

### 2026-09-22：去掉卖货观察期，有货就卖（**已保留**）

用户要求删掉观察期。`sell_quantity` 现在在扣掉饲料小麦预留之后，当天有多少就卖多少，不再看 `EARLY_SELL_DAYS`、30% 观察窗、峰值 90%。`watch_days` 和 `EARLY_SELL_DAYS` 已从代码里删掉。最后一天本来就全卖，行为不变；饲料小麦预留仍在（清仓日由调用方把预留改成 0）。

**怎么验的：** `lab.test_route14_economy` 49 项全绿（删掉了观察窗单测，卖货单测改成第 16、20 天不论价钱都卖掉，小麦仍扣预留）。seed 0 对 `v45_base` 整局 720 步，`DONE/DONE`，26 秒。

**结果：终局 `82514:182376`，改前 `79638:183574`。我方现金 +2,876，对方 −1,198，终局差从 103,936 收到 99,862。**

按收工现金重算各段「对方比我多赚」（起点双方 3,000）：

| 时段 | 我方净赚 | 对方净赚 | 对方多赚 |
|---|---:|---:|---:|
| 第 0–11 天 | +6,794 | +15,321 | +8,527 |
| 第 12–14 天 | −3,150 | +12,738 | +15,888 |
| 第 15–19 天 | **+8,000** | +44,455 | +36,455 |
| 第 20–28 天 | +59,306 | +87,162 | +27,856 |
| 第 29 天 | +8,564 | +19,700 | +11,136 |

观察期那五天，我方从改前大约净亏 5,600 变成净赚 8,000，这段自己的进账大约多了 1.4 万，货不再堆到第 20 天一次倒。但第 20–28 天改前是我方反超约 1.1 万，现在变成再落后 2.8 万，把中段多卖的钱吃回去一大部分，所以全季只净增 2,876。**不要把观察期加回去**：终局更高，而且用户已经明确改了这条规则。

上一节把第 15–19 天的 5 万差距主要记在「货冻在仓库」。这次有货就卖之后，这五天我方确实在进账，对方仍多赚 3.6 万。所以那 5 万里**大部分是对方农场更大**（第 11 天起对方 75 格铺满，我方停在 50 格），不是没卖。

**下一位接手者：**观察期已删，不要恢复。50 格停住的原因见下一节，不是没钱。第 20–28 天为什么不再反超、买粮抬麦价、后期没浇长草，都还在。未报名、未提交。

### 2026-09-22：停在 50 格是因为还有空地，不是钱不够

用户问停在 50 格是觉得照顾不过来，还是金钱不够。同一局 seed 0（终局 `82514:182376`）把每个天亮的买地闸拆开看了。**钱不是原因。**

第三块地标价 **2,000**（`LAND_PRICES` 的第二档）。买地函数 `_land_option` 的第一行是：手里还有空地、不是天亮、或剩下不到 3 天，直接不买。第二块地是第 12 天早上买的，当时第一块 25 格是满的。从第 13 天起每个天亮都还有空地（1–12 格），所以根本走不到后面的钱和人手检查。扣掉当天口粮、工钱和照料预备之后，第 13 天早上还剩约 **5,900**，第 20 天约 **13,000**，第 24 天约 **33,000**。`keep_only`（口袋不够续命）从第 7 天起没再出现。

空地为什么补不满，三段不一样：

1. **第 13 天早上有 7 格空，但当天就安排补了 6 格。** 若没有「有空地就不买」这条规矩，第三块地那天的人手见证和现金都能过（`future_care_hires` 要 11 人，口袋够）。这天既不是没钱，也不是算出来照顾不了 75 格。
2. **第 14–17 天早上不补种**，是因为地里还有没收完的一次性作物，`planting_allowed` 在天亮直接停。同时在买牛。不是钱，也不是这几格算过照顾不过来。
3. **第 18 天起，12 个人照顾不完现在这 50 格再往后几天的活。** 第 18 天明天就有 2 组活排不进去，再种那 1 格空地会变成 3 组；第 22 天现有地在第 4 天后有 1 组排不进。第 20、24 天还能再多照顾 **1 格**，但一次补满剩下的洞就不行（第 20 天 6 格一起补，两天后有 5 组排不进）。口袋里这时有 9 千到 5 万。这才是照顾不过来，而且闸的是「现有农场加几格新种」，不是地价款。

第 15、16、17、20、21 天早上，第三块地的人手见证其实也能过，仍然被空地规矩挡住。所以两件套在一起：**规矩是有洞就不买；洞到后期补不满，是因为 12 个人的上限，不是因为买不起种子或买不起地。** 未改代码。未报名、未提交。

### 2026-09-22：V45 买地是写死的两个钟点；可以学买地，不能学铺满

用户问 V45 是不是固定在某天买几块地，以及我们能不能学这个钟点，但下种时按照顾得过来的格数来限。查了冻结磁带，**41 条路线的买地订单完全一样，不看空地满不满：**

- 第 6 天第 6 小时（step 150）买第二块，1000，变成 50 格。
- 第 11 天第 1 小时（step 265）买第三块，2000，变成 75 格。

第四块不在磁带里。羊群模块、番茄模块会在已经有 75 格、店和现金也满足时再买东南那块（4000）。seed 0 这局对方停在 75，第四块没买。第 6 天收工对方是 7 格空 / 50 格，第 11 天是 3 格空 / 75 格——**他们买的时候地并没有种满。** 空地本身不浇水、不长草，草只从没浇的苗来。

这个拆法是对的：买地看钟点和地价款，下种看 12 个人还能多照顾几格。不能把「买下来」做成「25 格都种上」。以前写死买到 75 格并把地铺满时，羊会跑、口袋会掉到几十。未改代码。若要做，下一刀只改买地闸（有空地也买，但口袋要够地价），下种仍走现有的人手见证，多出来的格留空。第 6 天早上这局我们只有 261，对方那个钟点的 1000 我们未必付得出，所以钟点到了也不要空手下单。未报名、未提交。

### 2026-09-22：按 V45 钟点买地（**已撤回**）

用户同意试：买地时间学 V45，不必卡死在第 6 天，前后都行；扣掉雇人、照料、撑到下一轮收获之后，剩的钱够地价就买。下种仍必须人手照顾得过来才种。

落地过一版：已有 1 块地时窗口是第 4–13 天（目标第 6 天，前 2 后 7），已有 2 块地时窗口是第 9–18 天（目标第 11 天）。窗口里不再要求「一块空地都没有」，也不再要求新地的 4 格先被人手见证。地价只从照料预备之后的余钱里扣。`_fill_plan` 的下种闸没动。

seed 0 对 V45，720 步，`DONE/DONE`，38 秒。**终局 `56562:172438`，改前 `82514:182376`。我方现金 −25,952。** 49 项单测在撤回后仍全绿。

实际买地：第 9 天早上口袋 1,515，买了第二块（第 4–8 天余钱不够 1,000，所以不是第 6 天）。第 11 天早上口袋 8,485、50 格里还空 17 格，买了第三块。之后收工空地一直是 19–53 / 75。种下去的还是大约 28 格草莓加十几格瓜，牛最后 12 头（改前 15 头）。2,000 的第三块地基本没种上，第 11 天现金从 8,485 掉到 2,254，后面的买牛和补种都慢了。

**撤回原因：**钟点和余钱闸是按用户说的做的，下种也确实没把 75 格铺满。亏在买了照顾不过来的地，钱被地价抽走，空地挂到收工。代码已回到买地前的版本。未报名、未提交。

### 2026-09-22：V45 能照顾 75 格，是因为隔天浇、而且只浇「再干一天就死」的（只读，未改代码）

用户问：我们买到 75 格就照顾不过来，V45 为什么照顾得过来。查的是冻结 V45 自己怎么干活，不是再改我们的买地。对局是 V45 对官方 starter、seed 0、720 步（避开 route14 每步重排，约 2 秒）。开局两家店是 `PIZZA_SHOP`、`FARMERS_MARKET`，第 6 天起走第 121 号磁带。没有改 `third_party/v45/main.py`。

**人并不比我们多。** 第 11 天买到 75 格之后，每天雇 9–11 人。我们的上限是 12。

**地是种满的，而且没有草。** 第 12 天收工：56 格苗、17 头畜、空地 1 格、草 0。这个满场一直维持到第 26 天（58 格苗、17 头、空地 0）。第 27 天才开始收尾留空。

**他们不是每格每天都浇。** 收工时当天没浇的苗，第 12 天 28/56，第 13 天 20/56，后面多数日子 20–30 格。但「连续没浇」的计数收工时只有 0 和 1，从来不到 2，所以草一直是 0。引擎规则是连续 2 天不浇才变草，而且这个计数要到第二天刷新才加。昨天浇过的今天可以跳过；昨天没浇的今天必须浇。第 12 天进来时有 8 格已经干了 1 天，这 8 格当天都浇了；跳过的 28 格都是昨天刚浇过的。

**畜没有隔天。** 17 头从第 12 天起几乎每天每头都喂、都照料（喂 17、照料 17）。收工没喂到的经常是 0。他们省下来的是浇水，不是喂牛。

**人每天仍从仓库门口出发。** 引擎每天把雇工重置在门口，磁带里也含这段路。75 格稳定之后，走路大约每天 100 步，10 个人一天一共约 240 个动作。路线是事先写死的：谁去哪一圈、哪格今天浇、哪格今天跳过。不是早上临时从门口把每一格都排一遍。

**这和我们以前试过的「隔天浇」不是同一件事。** 那五刀是临场改优先级：全场同一天到期，那天所有地一起要浇，而且「今天不浇就死」排在收货后面，草莓死了、牛也少了，已退回。V45 是磁带事先把地错开：一半今天浇、一半明天浇，已经干了一天的绝不跳过。草莓这种持续结果的，不浇也照样结果，浇水主要只防变草；所以跳过一半浇水，产量掉得少，人就够用。

我们的人手见证仍要求未来某一天 12 个人从门口出发，把每一格该浇的、每头该喂的全部排进去。按「每天全浇」去排 75 格加 17 头，排不进，于是不买、也不种满。V45 能照顾过来，是因为浇水只浇会死的那一半。未改 route14。不要把以前那五刀隔天浇原样再做一遍；若要学，得先有「昨天浇过的今天跳、昨天没浇的今天必浇」这种错开，而且不能抢喂牛。未报名、未提交。

### 2026-09-22：按坐标隔天浇（**已撤回**）

用户要求按土地坐标统计：昨天浇过的今天不浇，省人手。落地过一版：横纵相加是偶数的格在偶数天浇、奇数格在奇数天浇。已经干了一天的（再歇就变草）今天必浇。麦、萝卜、瓜在能靠浇水结产量的那几天不歇。浇水和收获拆开，免得草莓结果那天永远轮不到浇。人手见证也按同一套坐标算未来哪天要浇。

seed 0 对 V45，720 步，`DONE/DONE`，20 秒。**终局 `63990:184155`，改前 `82514:182376`。我方现金 −18,524。** 49 项单测在撤回后仍全绿。浇水从每天全浇降到全季 507 次，挖草 28 次。

实际局面：

1. **开局人雇少了，地没种满。** 第 0–6 天收工一直是 10 格瓜、13 格空，雇工掉到 1 人。坐标把「今天要浇的」算少了，雇人闸跟着少雇，空地补不上。
2. **第 9 天就买了第二块地。** 收工 20 格空 / 50 格，口袋 1,076，对方 3,284。人手见证以为隔天浇照顾得过来，地价先被抽走。
3. **第 22 天起出现草**（收工 1–2 格）。错开没有稳住，有的格连续两天没浇到。牛最后 9 头，比每天全浇那局少。

**和同一局 V45 比，问题不在「隔天浇」这句话。** 对方也不是每格每天都浇，75 格收到第 26 天仍然没有草。差在三处：

1. **少浇被拿去少雇人，空地就没人种。** 第 0–6 天我们收工一直是 10 格瓜、13 格空，雇工掉到 1。对方第 1 天 25 格已经种满（12 瓜 7 麦，随后补莓），每天 3–5 人。我们的雇人数跟着「今天要浇几格」走，隔天浇把这个数砍半，人就只够去浇「再干一天就死」的那些，种地排在浇水后面，13 格空地挂了六天。对方的路线是事先写好的：歇浇只是少停一格，人还在，沿路把该种的种上。
2. **少浇被拿去提前买地，新地没有种上。** 第 9 天我们买了第二块，收工 20 格空 / 50 格，口袋 1,076，对方 3,284。人手见证以为隔天浇照顾得过来，地价先付了。对方第 6 天买 50 格（当晚还空 7 格）、第 11 天买 75 格（当晚空 3 格），路线里已经写着第二天把洞补满：第 8 天 50 格是满的，第 12 天 75 格是满的。人仍是 7–11 个，不是因为浇水变少就少雇。
3. **该浇的格没有浇到。** 第 22 天起我们收工有 1–2 格草。对方从买到 75 格一直到第 26 天，草是 0：昨天歇的格，今天的路线一定会走到。我们是早上临时派人，浇水会输给收货和送仓库，隔天浇又没有第二天的余量，漏一格就变草。

**撤回原因：**浇水次数是少了，但省下来的人没有变成更多的收成。开局少种、提前买空地、后期死苗，三处一起把现金打下去。代码已回到每天全浇。未报名、未提交。

**用户要的不是这版。** 2026-09-22 用户追问是不是代码写不出他的意思。他的意思是：人还是那些人，只是按坐标把昨天浇过的格子今天跳过，省下来的时间去干别的。上一版额外改了两处他没要的：雇人数跟着「今天要浇几格」下降，买地的人手见证也按少浇来放行。这两处才是少掉 18,524 的原因。只改派工、不改雇人和买地的那一刀随后跑过，见下一节，终局更差，已撤回。

### 2026-09-22：歇浇只改出门的人，雇人和买地仍按每天全浇（**已撤回**）

用户原话授权这一刀：雇人和买地仍按每天都浇来留人，歇浇只发生在已经出门的工人身上，再干一天就会变草的格子今天照样浇。雇人闸、买地闸没动。出门的工人：昨天浇过（连续没浇是 0）的草莓今天不浇；已经干了一天的今天浇，优先级 97，排在普通收货前面、喂牛和倒瓜后面。麦、萝卜、瓜在靠浇水结产量的那几天仍浇。

seed 0 对 V45，720 步，`DONE/DONE`，23 秒。**终局 `46703:146548`，改前 `82514:182376`。我方现金 −35,811。** 49 项单测在撤回后仍全绿。浇水 432 次，挖草 13 次。买地仍是 1 次（第二块，第 12 天），没有提前买。

**亏在工钱和歇浇撞在同一天。** 第 0 天收工 10 格瓜都浇了，口袋 569，和每天全浇的开局一样。第 1 天这些瓜昨天浇过，出门的人就不浇了；雇人账却仍按「10 格都要浇」雇了 10 个人。人工资把口袋打到 370。第 2 天只雇得起 1 人，第 4–6 天雇工是 0，口袋掉到 69。第 3、5 天正是「再干一天就变草」的日子，地里没人，第 7 天收工有 1 格草，瓜从 11 格掉到 9 格。第 12 天才买到 50 格，后面牛只有 7 头。

**不要把这一刀再原样跑一遍。** 用户指出这是代码前后矛盾，不是规则本身做不到：雇人账按「今天 10 格都要浇」雇人，出门的人按「昨天浇过今天不浇」把活跳过。两套账同一天各算各的，工资照付，下一早该浇的时候已经雇不起。若再写，雇人、买地、出门浇水必须看同一份「今天哪格浇、哪格歇」；歇浇那天不要按全浇去发工资。代码已回到每天全浇。未报名、未提交。

### 2026-09-22：用户要的掌控结构（只记录，未写代码）

用户认为上一刀是部件不够细、前后各算各的。他要的结构还没写完，也还没说开始改。当前代码仍是每天全浇、终局 `82514:182376`。下一位不要按这个草图直接拆文件，除非用户说开始。

他点名的部件：

1. **总统计。** 现在的总金额，减去撑到下一次收获要花的钱，看还有没有余钱。
3. **每一格作物。** 收获倒计时、当天浇了几次、当天施了几次肥、再隔几天不浇会变草、预计卖掉的价钱、这块地的坐标。
4. **每一头畜。** 收获倒计时、当天喂了几次、当天照料了几次、再隔几天不喂会跑、预计卖掉的价钱、所在坐标。
5. **财务。** 算清余钱。一笔新开销来了，用现在的钱减去这笔，看周转会不会断，断就不要批。
6. **雇人与派遣。** 只根据上面传回来的情报决定雇几个人、每个人去哪一格。

2026-09-22 用户把第 2 个定为空土地，并要求用「agent：具有」这种条目写全。下面这份是当时对齐的表述，仍未说开始，不要照这个写代码。

1、总统计 agent：具有
	当前总金额
	撑到下次收获的花销
	扣完之后的余钱
	天亮刷新之后的今天这一份单子
	下一块地的标价和方位

2、空土地 agent：具有
	返回自身坐标
	离仓库门口多远
	是不是草，是草就要先挖
	今天会不会自己长草

3、作物 agent：具有
	收获倒计时
	腐烂倒计时
	身上已经结了多少
	这口水是收成，还是只为防草
	这一季还长不长得成
	当天浇水次数
	当天施肥次数
	肥料还剩几天
	变杂草倒计时
	预计收购货价（砸进自己这一笔之后）
	所在土地坐标

4、畜类 agent：具有
	收获倒计时
	当天喂食次数
	当天 care 次数
	逃走倒计时
	身上已经装了多少，装满是多少
	口粮要多少麦
	棚还在不在，棚空着才能再放一头
	预计收购货价
	所在土地坐标

5、财务与维持 agent：具有
	算清目前的余钱
	工资、饲料、种子、买畜、买地分开批
	批复新的消费
	当前金钱减去新的消费，会不会周转断掉
	还没进口袋的卖货不算余钱
	最后一天不再批照顾

6、工人雇佣 agent：具有
	只按今天的单子决定雇多少人
	决定派谁去哪个坐标
	雇来的人下一小时才出现在门口
	当天雇了不能退，工资已经付了
	每个时辰看谁在路上、手里有没有货
	两个工人不去同一格
	脚下有必须干的活先干
	四个门口这一小时各卸一趟

7、卖货 agent：具有
	仓库里有多少，上限 100
	这一小时进仓库多少
	喂畜的麦先扣下，剩下的才能卖
	市场上成交前有多少货
	小镇这一小时吃掉多少
	对方看得见的地和出货
	预计收购价按砸进自己这一笔来算
	货进仓库这个时辰就卖
	最后一天全部卖掉
	一个时辰最多 10 条市场命令

2026-09-22 用户接着问还可以设什么功能和 agent。记下的建议，未说开始，不要照这个去写代码：

- **今日单归总统计，不另设一个 agent。** 每格作物、每头畜把倒计时报上来，总统计印成今天这一份。雇人、财务、出门只读这一份。
- **再加卖货。** 货进仓库这个时辰就卖。价钱按市场上已经有的货、加上我们自己这一笔，算砸下去之后的价。喂畜的麦先扣下，最后一天全部卖掉。还没卖成的钱，财务不能当余钱花。
- **再加地块。** 管已经买下的空坐标，以及下一块地（1000、2000、4000）要不要向财务申请。空地不浇水。批下来之后才种，种下的格子当天变草倒计时已经走了一天，必须浇。不许买下来就铺满 25 格。
- **作物再加：** 这口水是收成（麦、萝、瓜的产量窗口）还是只为防草（草莓、番茄）；种下当天必须浇；季末前长不成的不种；没肥料，施肥次数就是 0。
- **畜再加：** 不喂也会产，但照料加成要当天又喂又照料；口粮是麦，卖麦前先扣；再喂一天就会跑的，排在浇水前面。
- **财务再加：** 工资、饲料、种子、买畜、买地分开批；最后一天不再批照顾。
- **雇人再加：** 人数只数今天单子上必须干的活；人每天从仓库四个门口出发；脚下有必须干的活先干；手里有货先卸；再干一天就变草或会跑的，优先于普通收货。

2026-09-22 用户追问完整运转里还该看什么。仍是设计，未说开始，不要照这个写代码。下面这些挂在已有部件上，不另设老板：

- **天亮先刷新，再印单。** 浇水、喂食、照料的「今天次数」归零。昨天没浇的，变草倒计时减一；减到头就变成草。昨天没喂的，逃走倒计时减一；减到头牛还在，棚还在，牛没了。草莓、番茄结满最后一茬之后会腐烂，果实掉光就变成草。麦、萝、瓜过了产量窗口还不收，也会烂成草。空地每天有很小的机会自己长草。总统计必须用刷新后的格子印单。
- **雇人是早上的决定，派遣是每个时辰。** 今天雇来的人要下一小时才出现在门口，最后一小时雇来的人干不了活。人一经雇下，当天不能退，工资已经付了。每个时辰更新：这格浇了没有、这人头上有没有货、谁还在路上。两个工人不能派去同一格。
- **货在三个地方，只有口袋里的钱算分。** 地里的果实、工人手里的货、仓库里的货都不算分。仓库最多 100。四个门口一个时辰最多卸四趟。手里有货先卸。喂畜的麦和要卖的麦是同一堆，卖之前先扣口粮；我们买麦喂牛，会把麦价顶高，对方地里的麦跟着卖得更贵。
- **价钱是共享的，而且每个时辰都在变。** 小镇商店大约每 4 小时吃掉一些库存，市中心大约每天吃一次，库存少了价会涨。对方和我们抢同一口价，报价看的是这一小时成交前的库存，对方仓库里有多少我们看不见，但对方的地、钱、牲口、人在哪，看得见。第 10 天两边都会倒瓜。作物报的预计卖价要用砸进自己这一笔之后的价，不能用价牌。一小时最多 10 条市场命令，雇人、卖货、买种、买畜、买地挤在这 10 条里。还没进口袋的卖货，财务不能花。
- **作物再加腐烂倒计时和身上结了多少。** 畜再加身上装了多少：鹅最多 4、牛和羊最多 6，装满还不挤奶、不剪毛，新产的就浪费了。肥料一次管 3 天，而且只有当天浇了水才有加成。
- **地块再加远近。** 新开的一块在仓库对角，走路比门口的地长。买下的是空坐标，不是已经能浇的苗。

这套结构要治的就是第 1 天那本矛盾账：作物先报「这 10 格昨天浇过，今天可以歇，变草倒计时还没到」，雇人和财务都看这份报。歇的格子不产生浇水工资。倒计时到了「今天不浇就变草」的格子，才雇人、才派人去浇。现在的中枢把雇人、买地、余钱放在一处，出门派工在另一处，两处对「今天浇不浇」各算各的。作物和畜也还没有按坐标逐格报倒计时。未改代码。未报名、未提交。

### 关键文件（当前最好版本：seed 0 `82514:182376`，49 tests）

- 容量门：`lab/route14_capacity.py`（`day_schedule` 当天路线、`future_care_hires` 未来关键日人手、`affordable_care_reserve` 养到首卖的现金）
- 执行器：`lab/route14_agent.py`（门口就卸；空地按金钱/天贴门口；第 10 天近圈先收；雇人/买地/买畜走中枢；PLACE 全员；每小时可卖）
- 协议：`docs/ROUTE14_PROTOCOL_V1.md`
- 跑一局：`.venv/bin/python -m lab.runner --left route14 --right v45_base --steps 720 --seed 0`（对照表在 agent telemetry `days`，用 `lab.route14_agent.format_daily_compare`；第 10 天小时账在 `day10_hours`）


## 1. 比赛事实（需在提交前重新确认）

- 比赛：[`Kaggriculture`](https://www.kaggle.com/competitions/kaggriculture)，ID `147734`，Featured Simulation，奖金 `$50,000`。
- 2026-09-17 通过 Kaggle MCP 查询：`team_count=9,285`；新参赛截止 `2026-09-23 23:59 UTC`（北京时间 9 月 24 日 07:59）；最终提交截止 `2026-09-30 23:59 UTC`（北京时间 10 月 1 日 07:59）。每日最多 5 次提交，队伍最多 5 人。
- 赛季 30 天 × 每天 24 回合，标准配置 `episodeSteps=720`。最终**银行现金**决定胜负；未卖出的 shed/手持库存不计分。
- 这是两名 agent 共用市场、各自农场的回合制模拟。对手私有 shed/seed/手持物品不可见，但对方现金、地块、作物、动物、位置是公开的。
- 正式评估期间不得网络 ingress/egress。提交包根目录必须有 `main.py`，暴露 `agent` 函数。多文件包可带 `model_weights.pkl`；这只是允许权重随包携带，并不表示现有高分方案使用 ML。
- 本机没有常规 Kaggle CLI 凭据；但 Codex 中的 Kaggle MCP 已经有独立登录态，能读公开比赛/Notebook。该 MCP 查询曾显示用户并未进入本比赛；需要时重新验证，不能据此替用户入赛。

## 2. 游戏与策略的关键理解

### 观测和动作

每回合 agent 从 `obs` 获得：

- 时间：`step`、`day`、`hour`。
- 双方公开农场：`money`、tiles、farmer/hands 坐标、已解锁土地、当日雇工数。
- 共享市场：各商品库存、当前报价；城镇已解锁商店。
- 自己私有状态：shed、种子、每位角色手持库存。

返回动作一般形如：

```python
{
    "farmer": ["..."],
    "hands": [["..."], ...],
    "market": [["BUY_SEED" | "SELL" | "BUY_PRODUCT" | "HIRE" | "BUY_LAND", item, qty], ...],
}
```

三个组可同回合同时行动。农夫/雇工可移动、种植、浇水、收获、施肥、建/放动物设施、喂养、照料、取放货等。固定 benchmark 的“13 条路线”只是作者自己压缩动作空间的方式，**不是游戏限制**。

### 市场结论

- 市场库存以 `I0=10,000` 为平衡点；买会降低库存并抬价，卖会增加库存并压价。
- 西瓜、羊毛、牛奶等高价品在库存超过均衡后会快速跌价；集中卖货、抢先卖和避开同类对手是重要博弈点。
- 同一回合双方市场订单交错、逐单位处理。订单顺序及对手的同步订单会影响成交报价。
- 官方环境源码中有完整 `MARKET_PARAMS`、作物/动物参数；见安装环境中的 `kaggriculture.py`。

## 3. 已完成的本地工程

工作目录：`/Users/pure_water/Desktop/竞赛/kaggriculture-lab`

```text
README.md                       本地运行说明
requirements.txt                固定 kaggle-environments==1.32.7
lab/agents.py                   透明的单格作物 smoke-test 策略族
lab/runner.py                   单局、固定 seed 运行器
lab/tournament.py               多 seed 统计评测器
lab/baselines.py                冻结 V45 开发基线的 loader
third_party/v45/main.py         SHA 已验证、不可直接编辑的 V45 基线源文件
third_party/v45/reconstruct.py  来源 Notebook 的原始重建 cell
third_party/v45/README.md       来源、SHA、许可证边界
```

环境：`.venv`，Python 3.12，`kaggle-environments==1.32.7`。

常用命令：

```bash
# 单局
.venv/bin/python -m lab.runner --left wheat --right starter --steps 720 --seed 7

# 多个连续 seed 的本地统计
.venv/bin/python -m lab.tournament --left melon --right v45 --count 3
```

`runner.py` 对 V45 每局重新加载，避免其内部记忆泄漏到独立对局。所有本地 match 都设置显式 `seed`，因此可复现。

### 2026-09-17：已归档的独立规则探索

以下改动均为用户已经授权的本地研究；没有报名、提交或改动 Kaggle 远端状态。它们曾用于验证实验管线，现已按用户决定归档，**不再是默认策略或后续搜索起点**：

```text
docs/V45_REFERENCE.md           将 V45 拆成可解释机制，明确不可复制边界
lab/own_agent.py                自写、参数化的 15 格西瓜生产/卖货策略
lab/search.py                   固定候选/对手/seed split 的两阶段搜索器
docs/EXPERIMENT_PROTOCOL.md     本轮可复审实验协议
experiments/melon_search_v1.jsonl  42 局逐局、可复算的结果
```

`own_agent.py` 的生产规则（与 V45 路线 tape 无共享代码）是：第 2–4 行各 5 格西瓜田；农夫和两名每日雇工各负责一行；第 0、12 天各买 15 粒种子；每小时按“收获 → 浇水 → 除草 → 种植”的优先级做本行最近任务。卖货规则是：shed 西瓜达到阈值时强制清仓；否则只在价格达到基准价比例时分批卖；最后两天无条件卖完。每局会记录雇工、种子、种/浇/收、卖货和紧急清仓 telemetry。

V45 只被拆解为“路线压缩、安全修复、市场顺序、资金预留、ROI、终局、公开对手信号”七类设计原则；详见 `docs/V45_REFERENCE.md`。不得把该参考中的路线 tape、开局市场攻击、wrapper 或未审查数据移入自有 agent。

### 已归档：第一轮卖货搜索结果与解释

协议固定了生产路线，只搜索 `sell_batch`、`min_price_ratio`、`emergency_shed_units` 三个卖货参数。对手池为 `starter`、单格 `melon`、本地 V45 参考；development seeds 为 `0, 1`，holdout seeds 为 `1000, 1001, 1002`。选择必须只看 development，再让前二进入 holdout。

| 阶段 | 候选 | 局数 | 平均本方终局现金 | 平均 margin | 胜负 |
|---|---|---:|---:|---:|---:|
| development | `b60-p0.70-e80` | 6 | 25,190.3 | -29,710.5 | 4–2 |
| development | `b90-p0.70-e80` | 6 | 25,158.0 | -29,667.2 | 4–2 |
| development | `b30-p0.50-e70` | 6 | 24,987.0 | -29,838.2 | 4–2 |
| development | `b45-p0.70-e70` | 6 | 24,987.0 | -29,838.2 | 4–2 |
| holdout | `b60-p0.70-e80` | 9 | 25,190.3 | -35,968.6 | 6–3 |
| holdout | `b90-p0.70-e80` | 9 | 25,158.0 | -35,925.2 | 6–3 |

候选排名按规则已在 development 固定为 `b60`、`b90`，然后才打开 holdout；没有基于留出结果返调参数。两版对 `starter`/基础 `melon` 的局部优势稳定（约 +25.6k 至 +27.7k margin），但对 V45 都明显落后：development 约 -142.5k，holdout 约 -161.2k。因此本轮只证明本地**自动筛选与留出验证管线可工作**，完全不支持“已有奖牌线”或“可超过 V45”的结论。

注意：在这一小田策略里，很多候选的实际卖货时机相同，参数间差异很小；不能把 25–200 的均值差当成有统计意义的胜负。下一轮应先增加能真正触发差异的情景/对手，再扩大样本，而不是继续微调这些阈值。

复跑命令：

```bash
# 先覆盖写 development，读取 selected_for_holdout；此结果选出 b60、b90
.venv/bin/python -m lab.search --stage development --output experiments/melon_search_v1.jsonl

# 只对上述已固定名称追加 holdout，禁止替换候选
.venv/bin/python -m lab.search --stage holdout --output experiments/melon_search_v1.jsonl \
  --selected own-melon-b60-p0.70-e80 own-melon-b90-p0.70-e80
```

本环境的单条命令运行时间有限，故必须分两阶段落盘；不要用会在环境结束时被回收的后台进程作为唯一实验记录。

### 2026-09-17：V45 现为冻结开发基线

用户明确决定“不从零自写西瓜基线，直接以 V45 为基础代码改进”。已执行的本地结构调整：

- `third_party/v45/main.py` 仍是 SHA-256 `2536d...210d94` 的逐字冻结副本；**不在此文件直接编辑**，以确保任何 variant 的差异可审计。
- `lab.baselines.load_v45_base_agent()` 每局重新加载上述源文件；新实验请用 agent 名称 `v45_base`。保留 `v45` 仅为旧记录兼容别名。
- `lab.runner` 与 `lab.tournament` 的左侧默认 agent 已改为 `v45_base`；`own_melon` 已从可选默认 agent 列表移除。
- `lab/own_agent.py`、`lab/search.py`、`docs/EXPERIMENT_PROTOCOL.md` 和 `experiments/melon_search_v1.jsonl` 被保留为历史证据，不能再被当成当前主线的结果或默认入口。
- `docs/V45_REFERENCE.md` 已同步为冻结基线的机制地图，明确 variant 的单一改动与 diff 规则。

从现在开始的改进约定：新增策略必须创建为具名 V45 variant（先定义唯一变更、假设、开发集、holdout 与回滚条件），再与冻结 `v45_base` 同 seed 对局。不要把“胜过 starter”写成进展；唯一的主要比较对象是冻结 V45 及其后续历史强版本。

**来源/提交门槛仍有效：**V45 是第三方衍生代码，不得宣称为用户原创。其 header 中列有多位上游作者和 Apache-2.0 lineage；在将来任何 Kaggle 打包或提交前，必须重新审查比赛规则、保留 notices、核验路线/replay 的完整来源链。当前仅完成本地开发基线切换，并未制作提交包。

### 用户明确要求：后期必须有创造性主线（暂不实现）

用户指出只调阈值、卖货批次或安全边界只是修补，不能是项目的最终竞争力；后续必须研究能改变决策能力边界的创造性方案。当前先用小变体建立可信 A/B 与留出验证流程，**不得把小变体的微小胜负包装成最终策略创新**。在基础评测可靠后，按以下优先级立项并各自建立独立实验设计：

1. **对手建模 + 自适应路线切换：**从公开田地、位置、市场库存变化和已解锁商店识别对方生产路线/卖货压力；在多个生产路线或销售计划之间切换，而不是只调整一个阈值。
2. **滚动反事实规划器：**从当前 obs 枚举少量高层动作（提前卖/囤货、扩张、动物投资、路线转产），用官方环境的短 horizon rollout 评估“我方动作 × 预测对手供给”的终局价值，再由规则层执行具体合法动作。
3. **联赛式自博弈与 exploiters：**维护主策略、历史版本和专门克制主策略的对手群；训练/搜索的目标不是击败固定 V45，而是在对手分布变化下仍稳健。
4. **高层上下文选择器：**用规则 rollout 产生训练数据，让小模型只选择路线、投资和卖货强度；底层移动/合法性仍由确定性规则负责。先验证离线选择器，只有在 holdout 联赛稳定提升后才考虑 PPO。

这些方向仍受来源审查和本地/正式榜单差异约束。恢复研究时，先写可审核方案、明确训练/算力需求和失败判据；不要直接启动大规模训练或租 GPU。

### 2026-09-17：多对手 benchmark 调研与第一批快照（兼容性验证前）

用户明确要求对手不能只用 V45，应从公开高分/有价值代码中建立多样对手池。调研结果见 `docs/BENCHMARK_POOL_RESEARCH.md`。核心结论：当前公共榜前 20 的分数约 `2947.6–3184.1`，而很多 Notebook 标题中的 `2600/2700/2900` 是历史记录、自报本地模拟或宣传，不能混为当前正式分数。

第一批候选为：sdy623 的 2842/2857 市场微结构版本、Ahmed V46、Indar 的历史 Top10 路线选择、Kaito V58 minimax continuation；第二批为 Nathan Pipe-7、nusrati 2715.6、Dmitrii 终局 rescue、Tetsutani 市场策略。候选的来源、证据强度、疑似 V43/V45 血缘和加载门槛已逐项记录。

用户于当日明确同意开始后，已下载第一批四份公开 `main.py` 快照：`sdy2842` SHA `944aa64c…20a56`、`v46` SHA `735c3703…cedb6`、`indar_top10` SHA `d39dba50…9f2e`、`kaito_v58` SHA `b041058e…ddcb9`。V46 hash 与作者 manifest 相符，其他三个 hash 与 Notebook 公开 log 相符；均通过 `.venv/bin/python -m py_compile`。每个目录有 README，明确“只作本地 opponent、不得提交/署为原创”。`lab.baselines.load_external_agent()` 每局用新 module namespace 载入；`lab.runner.AGENTS` 已登记四个名字，但这只是可运行测试入口，**不表示已进入固定联赛**。

初始验证已完成并记录于 `experiments/benchmark_pool_screen_v1.json`：四个 agent 均在本机 `kaggle-environments==1.32.7` 完成 720 回合；seed 7 对 starter 双跑 reward 完全一致，四个 action trace SHA 也均不同。随后固定 seeds 7、8 与两边座位、每个候选共 4 局对 `v45_base`：`sdy2842` 平均 margin `+2,049.5`，`v46` `+2,248.0`，`indar_top10` `-39,754.0`，`kaito_v58` `-18,248.5`，全部 `DONE/DONE`。这不是强度排名：仅 2 个 seed，不能外推到隐藏榜单或“谁更强”。但它证明四者行为不同、可执行，足够成为后续多样性对手的**候选**。

此前“尚未预注册固定联赛”的阻塞已解除，见下节的协议 v1。Kaito V58 原 Notebook 用环境 `1.29.3`、本机为 `1.32.7`，虽已完成本地兼容筛查，仍要在正式批量结果中单列性能/版本风险。只有在固定协议的留出验证通过后，才能宣称 variant 相对多对手池有改进。

### 2026-09-17：第二批多样对手快照与初筛

按用户明确指令“加载”，已加入候选测试入口的三份公开产物是 `nathan_pipe7`（Nathan Jacob Pipe-7，小麦市场微结构）、`boatlee_v16`（Boatlee V16 premium market route）和 `indar_pasture`（Indar E776 latent-pasture 多文件包）。来源/版本/SHA/声明性质记录在各自 `third_party/<name>/README.md`。前两份为 `main.py`，第三份原始公开产物为 `submission.tar.gz`，其 `agents/`、`e776_pkg/` 与根 `main.py` 均已完整保留；loader 每局清除该包的 module cache 后再加载，防止其状态跨局泄漏。

`experiments/benchmark_pool_screen_v2.json` 记录验证：三者均能在 Python 3.12、`kaggle-environments==1.32.7` 完成 720 回合；seed 7 对 starter 双跑完全一致，且动作 trace hash 各不相同、也不同于 V45。以 seeds 7、8、双座位各 4 局对 V45：Pipe-7 平均 margin `+234.0`，V16 `-40,631.5`，Pasture `-16,759.0`，全为 `DONE/DONE`。Pipe-7 在 starter seed 7 的现金与 V45 一样，但 trace 不同且对 V45 有小幅差距，故保留为近亲 market-microstructure 压力对手而不是重复 clone。样本仍只有 2 个 seed，禁止据此声称绝对强度或正式排名。

现在已有 7 个可运行的第三方候选：`sdy2842`、`v46`、`indar_top10`、`kaito_v58`、`nathan_pipe7`、`boatlee_v16`、`indar_pasture`。它们现已被确认纳入下节的固定联赛 v1；这不改变其“只作本地对手、不是提交物”的边界。

### 2026-09-17：七外部对手配对联赛 v1（已确认，校准完成）

用户确认“先用这七个对手作为测评基本盘”并随后明确授权开始。唯一主评测计划现为 `docs/SEVEN_OPPONENT_LEAGUE_PROTOCOL.md`：七个外部对手等权，冻结 `v45_base` 是每个 variant 的配对父本；同一对手/seed/座位下各跑 variant 与 V45 一局，比较终局现金差。development 固定 seeds `0–3`（56 配对条件、112 局），holdout 固定 seeds `1000–1007`（112 配对条件、224 局）；双座位均必需，holdout 前不得据其结果修改 variant。

实现为 `lab/league.py`。它以 append-only JSONL 保存逐局结果，默认每次只追加 8 个缺失局，并在每局结束时立即落盘；断点后重复同一命令可继续，不会丢失已完成局。每局对 stateful public policy 重建实例。结果汇总只配对完全相同的对手/seed/座位，防止 market stream 和座位扰动被误判为改进。最小 gate 是配对完整且全 `DONE/DONE`、等权均值为正、至少 4/7 对手非负；这只是进入下一阶段的最小证据，绝不代表正式 leaderboard 或奖牌。

下一验证门：先以 `v45_base` 作为 candidate 跑完 development 自校准，理论配对差必须为 0；这验证 runner、JSONL resume 与汇总逻辑，不能当策略结果。随后任何新、单一改动的 V45 variant 才能使用该已冻结协议进入 development。旧 feed-m1.15/m1.35 仍是已归档负结果，不得因为新联赛而重新调参或升级。

校准结果（已跑完但账本有审计注记）：`experiments/seven_opponent_league_v1.jsonl` 包含 120 行；按条件去重后是 protocol 所需的 112 局/56 个完整配对，全部 `DONE/DONE`，七个对手的 self-delta 均为 `0.0`。这证明同 seed/对手/座位的配对与汇总逻辑正确，且 V45 自校准没有数值漂移；`minimum_gate_met=false` 是预期的，因为它要求 variant 有正增益，校准值应为零。

审计发现原始账本多出 8 行：仅 V46 的 seeds 2、3、双座位和两种 evaluated policy；终局现金、margin、状态逐项相同，不会影响汇总（汇总按 condition key 配对，不累加重复行），也没有 conflicting duplicate。但 raw ledger 非 clean，不能再追加。根因是修复“每批末尾落盘”之前的一次短终端窗口；为防再发，`lab/league.py` 已改为每局即时 append，并在同一 candidate/split 有重复 key 时拒绝继续。后续 variant 必须使用新的 JSONL 路径；保留本校准原始账本作为 runner 审计证据，不删除或改写历史行。

从同一账本读取的 V45 **真实对手 margin**（只取每个 condition 的一份 V45 记录，不是 self-delta）给出首批候选的优先级。4 development seeds × 双座位共 8 条/对手：`sdy2842` 平均 `-2,001.1`（0–8），`v46` `-435.5`（1–7），`nathan_pipe7` `-236.5`（1–7）；但 V45 对 `indar_top10`、`kaito_v58`、`boatlee_v16`、`indar_pasture` 分别平均 `+38,148.6`、`+44,148.6`、`+39,732.6`、`+42,660.1`，均 8–0。该样本仅用于**定位弱点**，不是公开排名或泛化强度结论。它说明第一优先级应是同族的开局小麦/市场顺序/抢卖微结构，而不是为 V45 已大胜的异质路线堆叠复杂度。

### 2026-09-17：V46 晋级审计（development 未通过）

用户提出以 V46 作为新基准代码，并在说明“8 个条件不足以直接替换 V45”后明确授权开始。执行计划：V46 先作为 candidate，使用新账本 `experiments/v46_promotion_v1.jsonl` 完成七对手 development（112 局）；只有等权门和未见 holdout 都通过，才冻结 `v46_base`。V45 不会删除，继续作长期锚点；旧 V45 variant 不自动迁移。

因为 V46 是固定七对手之一，联赛汇总新增 `nonself_mean_delta_vs_v45` 和 `promotion_gate_met`：镜像 V46 对 V46 仍透明保留在七对手主表，但 V46 晋级额外要求移除该镜像条件后的六对手平均差也为正。此改动防止候选依靠自身镜像互动获得虚假晋级；协议已同步更新。

development 已完整运行：112/112 局、56/56 配对条件、全 `DONE/DONE`，账本 audit clean（无重复或冲突）。结果是 V46 相对冻结 V45 的七对手等权平均差 `-2,186.089`，不满足“均值为正”；去掉 V46 对 V46 镜像条件后的六对手平均差 `-3,821.104`，也不满足晋级 guard。按对手组均值：`sdy2842 +8,030.0`、`v46 +7,624.0`、`indar_top10 +3,306.125`、`indar_pasture +2.5`，但 `kaito_v58 -14,574.875`、`nathan_pipe7 -2,406.25`、`boatlee_v16 -17,284.125`。虽有 4/7 组非负，负组的量级更大。

**决定：不跑 holdout，不提升 V46 为 `v46_base`。**这是 development 的预注册淘汰，不是 V46 “整体很弱”的结论；它只说明在当前七对手等权基本盘中，直接用 V46 整包替换 V45 会降低稳健性。V45 继续是开发父本；V46 保留为强近亲对手和市场机制研究对象。后续源码核验显示 V45 已有很强的提前卖货/卖单前置层，因此该早期“优先提炼卖货机制”的建议已被下方 2026-09-18 诊断取代。

### 2026-09-17：`sdy2842` 基准晋级审计（development 未通过）

用户询问七个外部对手是否可能比 V45 更适合作为 baseline；根据 V45 自校准时对 `sdy2842` 的 8 个直接条件平均落后 `-2,001.1`，决定首先检验它，但明确该小样本不是强度排名。用户已审核并明确同意以下计划：以 `sdy2842` 为 candidate，冻结 V45 为永久配对锚点，按七对手等权、development seeds `0–3`、双座位运行 56 配对条件/112 局，写入新的 append-only 账本 `experiments/sdy2842_promotion_v1.jsonl`；仅当完整全 DONE/DONE、总平均差为正、至少 4/7 对手组非负，且因 candidate 同属对手池而**剔除 `sdy2842` 镜像后的六对手平均差仍为正**，才运行 holdout seeds `1000–1007`（112 条件/224 局）。holdout 通过后才可冻结 `sdy2842_base`；未通过则不替换 V45。全程只进行本地模拟，不报名、不提交、不创建凭据。

已验证新账本路径不存在，`lab/league.py`、加载器、runner 和 `third_party/sdy2842/main.py` 均已通过 `py_compile`。

development 已完整结束：`experiments/sdy2842_promotion_v1.jsonl` 恰有 112 行/112 局，对应 56/56 配对条件，全部 `DONE/DONE`；audit 为 clean（0 duplicate、0 conflict）。总体相对 V45 的平均现金差为 `+1,461.429`，去掉自身镜像后的六对手平均仍为 `+350.5`，但逐组只有 `sdy2842 +8,127.0` 与 `v46 +7,594.75` 非负。其余为 `indar_top10 -452.875`、`kaito_v58 -275.75`、`nathan_pipe7 -2,671.625`、`boatlee_v16 -1,255.875`、`indar_pasture -835.625`；即仅 `2/7` 组非负，未满足预注册的至少 `4/7` 门槛，`minimum_gate_met=false` 且 `promotion_gate_met=false`。

**决定：不跑 holdout，不冻结 `sdy2842_base`。**这不是说 `sdy2842` 没有直接交锋优势；它在自身/V46 条件上提升很大，导致总体均值为正。淘汰原因是这种收益无法横跨多数异质对手，不能证明它是比 V45 更稳健的通用开发父本。V45 保持当前 base，`sdy2842` 保持固定本地对手和研究对象；后续应做单机制 variant，而不是继续把完整公开 agent 逐个替换为 base。

### 2026-09-18：V45 与七对手池的根因诊断（已完成）

用户明确要求在再改策略前，先解释 V45 在固定七对手池中“为什么输、输在哪；为什么赢、赢在哪”，并审核后授权开始。该工作只做本地观测和分析，不改变任何 agent 策略、不报名、不提交。计划：实现一次性 trace runner，在相同 development 条件下重跑 `7 opponents × seeds 0–3 × 双座位 = 56` 局，逐回合保存双方公共农场/现金、各自私有 shed 与手持库存、市场价格/库存、双方市场订单、作物/动物/土地/雇工/位置及 V45 telemetry；随后按开局 `0–72`、发展 `73–287`、市场竞争 `288–647`、终局 `648–718` 聚合。每条 trace 的终局现金须与已冻结联赛的对应 V45 margin 一致，任何不一致 trace 不得用于结论。

产物预定为原始 append-only JSONL、可复算聚合 JSON 和一份报告，将明确区分“观测相关”与“可支持的机制假设”。重点优先解释 V45 对 `sdy2842`、`v46`、`nathan_pipe7` 的近亲劣势，也要解释对四个异质对手的大幅优势。风险：该诊断只适用于本地公开对手池和固定 seeds，且共享市场存在双向因果，不能外推为 Kaggle 官方排名或单一动作的绝对因果。

诊断实现为 `lab/diagnostics.py`。它不调用 agent 两次：从 Kaggle environment 的 post-turn state 采集上一动作、当前公共农场/市场和每名玩家自己的 private 状态，逐局 append 至 `experiments/v45_opponent_diagnosis_v1.jsonl`。单局烟测及最终批量均以旧联赛 `v45_base` candidate record 作 reference；全量 56/56 局各有 720 条 turn records（40,320 条）和 game end，终局现金/状态全部逐项匹配，0 条不完整或不一致。可复算汇总为 `experiments/v45_opponent_diagnosis_v1_summary.json`，人读报告为 `docs/V45_OPPONENT_DIAGNOSIS_V1.md`。

**诊断结论（不是单订单因果）：**近亲三组中，V45 的生产资产在第 287/647 回合边界几乎相同，劣势在现金路径：对 sdy 为开局 `-28`、发展 `-1,262.5`、市场 `-507.6`、终局 `-203`，合计 `-2,001.1`；对 Pipe-7 为 `-12/-224.5/0/0`，合计 `-236.5`；对 V46 为 `-14/-143/-404.8/+126.2`，合计 `-435.5`。sdy/Pipe-7 第 0 回合的 wheat round-trip 分别为 `10/10`、`5/5`，V45 是 `70/70`；但不可把全部差额归为该动作。尤其 sdy 局里 V45 的 clone/race 层已运行约 476 回合、卖货预留约 253 单位、且发生过一次 escalation，故“再扩大卖货提前窗口”没有证据支撑。

对四个异质对手，V45 到 step 287 通常多 `3.5–5` 头动物，随后市场/终局现金差扩大为约 `+38k–+44k`。这解释当前 base 的稳健强项是生产与终局兑现，而不是万能市场微结构。**当前下一门：**先做近亲三组的“分叉回合级”审计（首次现金差超过阈值时的双方订单、公共价格/库存与后续 1–4 回合现金），只在跨 seed/座位重复后才提出原创条件化规则；不得从这份相关性报告直接调参数或启用历史开关。

此前用户提出的“条件化提前卖货 + 卖单前置”**没有实现为 variant**。源码核验发现 V45 已有 R36：216–695 对非 wheat/fertilizer 的 post-unit shed 货，按自身未来 tape 预卖并在原回合 debt-suppress；288 后已有 `_v224_sales_first`，且 R37 在该区间已把预留窗口设为 4，clone/race 条件还能升至 8/24。因此 `2→3` 或泛化提前卖不是独立且有意义的新增机制；用户认可后停止该路线，未创建 `v45_r36_horizon3` 代码或实验账本。

### 2026-09-18：近亲现金分叉回合审计（已完成；未改策略）

用户在阅读阶段诊断后要求继续细查“哪一回合、什么订单、怎样改变现金路径”，并审核后明确授权开始。范围固定为现有 trace 的 24 条近亲条件（`sdy2842`、`v46`、`nathan_pipe7` 各 4 seeds × 双座位），不重跑、不改 agent。预注册判据：逐回合计算 `V45 money - opponent money`；找单回合差变化与随后至少 24 回合不反向消失的持久分叉；对每个候选提取前后 4 回合双方 market orders、共享市场库存/报价、shops、shed/hand 变化；按回合区间、商品、订单类型、现金方向聚类。仅同一对手至少 6/8 或跨多个近亲重复的模式可称稳定；同时以一个 V45 大胜对手作共享市场波动对照。产物为可复算事件 JSON、诊断报告增补和交接更新；不在本轮提出或实施策略改动。

实现为 `lab/divergence_audit.py`，输入只读的 `experiments/v45_opponent_diagnosis_v1.jsonl`，输出 `experiments/v45_near_peer_divergence_v1.json` 与 `docs/V45_NEAR_PEER_DIVERGENCE_V1.md`。脚本已 `py_compile` 并完整运行；其覆盖三名近亲和 Kaito 对照各 8/8 个已复现条件，使用冲击阈值 25、24-state persistence、稳定门槛 6/8。它列出 83 个稳定事件组（含 Kaito 对照），不把请求订单误作成交：只有双方均为单一、同商品 SELL，且双方现金变动和共享市场库存可由官方 per-unit lockstep 定价唯一反解时，才报告实际 fills。

**细查结果：**sdy 在 state 252 全 8/8 都显示双方请求 `SELL MELON 12`，但可严格反解为 V45 成交 `6`、sdy 成交 `12`，平均 V45 单回合 `-1,258`、24 状态后仍 `-1,258`。原因不是先后报价：官方 market 对同订单位的两名玩家先同价 quote 后提交；前 6 单位同价，sdy 额外完成 6 单位。事前聚合手持 melon 是 V45 24、sdy 30，足量但 trace 不保存每个 worker 的 item 分配或 `PLACE` 成败，故可确认“V45 可售库存交接少 6”，不能确认是哪一名 worker 或哪条路径失败。sdy 的另两个 8/8 市场事件在 362/386（sdy 多/前置 `SELL EGG`）分别为 `-115.5/-339.8` 与 `-336/-223.5`，但都属于复合订单，尚不可作因果归因。

Pipe-7 也出现同请求不同 fill：state 81 两边请求 `SELL WHEAT 3`，实际 V45 `2`、Pipe `3`，平均 `-29.5/-88.5`；state 101 两边请求 `SELL WHEAT 2`，实际 V45 `0`、Pipe `2`，平均 `-59/-59`（均为 8/8、前/后 24-state 指标）。这使“库存到 shed / 正确 worker 放货的可执行性”成为跨近亲重复的机制假设，而非泛化卖货时机问题。sdy state 1 的 70/70 对 10/10 wheat round-trip 为 `-108/-28`，Pipe 为 70/70 对 5/5、`-61/-41`，均只是已经存在的开局现金路径差，不能单独背负终局归因。

V46 的稳定核心在 state 3：V45 无市场单、V46 `SELL WHEAT 30`，市场库存实际 +30，V46 现金 +901，平均 V45 `-901/-891`。其前置状态是 V46 先保留 5 wheat、再买 30（state 2 曾只余 177 现金），随后售出；这是高现金暴露的完整 opening 序列，而非可安全抽取的一笔提前卖货。Kaito 对照也在 state 252 出现更大的同类 melon fill 差但 V45 整局大胜，说明单次市场节点不自动决定胜负。

**决定：不改 V45，不启用 R148，不复刻 V46，不调 R36/R37 提前卖货。**当前已完成的审计只证明应优先看 worker 级库存交接，并未证明任何修复规则会通过七对手 promotion。下一道验证门（若用户审核同意）应当仅扩展诊断 trace：保存每名 farmer/hand 的 inventory、当回合 unit action 的 success/no-op 和 market 实际 fills；用同一 24 条近亲条件确认 state 252/81/101 的失败 worker、手持物和目的地，再提出一个独立的“可执行库存”规则及预注册联赛验证。风险：现有 8 条条件/对手和公开池仍不足以外推官方 leaderboard，且 worker 级证据未采集前不可把 aggregate carried 当作可放货容量。

### 2026-09-18：worker 级库存/成交诊断（已完成；未改策略）

用户已审核并明确同意下一步仅扩展诊断。目标：用 Kaggriculture 默认解释器的只读运行时 instrumentation，重跑三近亲 `24` 条 development 条件和 Kaito 对照 `8` 条（合计 `32` 局），在不改变 agent 代码或动作的前提下保存每个 farmer/hand 的 action 前后 inventory、`PLACE`/`PICKUP` 对 shed 的实际变化、每个 market commit 的请求/报价/成功状态与成交量。主要取舍是多一次本地模拟和新增原始 trace，换取把聚合库存的相关性提升为 worker/action 级证据；不会报名、提交、创建凭据或修改 V45。质量门：32/32 终局必须与冻结 `seven_opponent_league_v1` 的 V45 记录一致，任何不一致的局不得用于结论；随后只核验 sdy state 252 与 Pipe state 81/101（并用 Kaito state 252 对照），先确认具体 worker 和失败原因，仍不提出/实施策略变体。风险：instrumentation 需保持官方解释器语义不变，因此还要对完整 trace 做终局复现 audit。

实现为 `lab/worker_diagnostics.py`，局内临时包装 `_apply_unit_action`/`_commit_unit` 并在每局结束前恢复；保存 worker action 前后物品、PLACE/PICKUP outcome 和 market unit commit，未修改任何 agent。`experiments/v45_worker_diagnosis_v1.jsonl` 的 32 个**唯一**条件全部 `DONE/DONE` 且终局奖励严格匹配冻结联赛；聚焦汇总 `experiments/v45_worker_diagnosis_v1_summary.json`。运行中第一个 32-game shell 调用在工具超时后仍在后台继续，和后续的分批续跑重叠，留下 52 行 raw records（20 个 duplicate keys）。这 20 个重复在去除 `elapsed_seconds` 后的完整 trace SHA-256 均一致，0 conflict；为保留原始事实没有删除或重写，汇总器显式标记 `ledger_clean=false` 并每条件只使用首条相同证据。后续运行不得把此 raw JSONL 当 append target；如需新 worker trace 必须使用新 protocol/path。

**结果一（sdy，8/8 精确链条）：**V45 `hand_0` 在 action 245 的 `WATER` 无效果，而 sdy 的同名 worker 改变 tile；action 246 V45 `HARVEST` 无效果，sdy 成功并得 6 melon；action 251 V45 `hand_0 PLACE MELON 6` 因手持 0 无效，仅 `hand_4` 成功放 6，sdy 的 `hand_0` 和 `hand_4` 都成功放 6。故两边虽请求 `SELL MELON 12`，V45 实卖 6（1 failed commit）、sdy 实卖12，少收 1,258。这证明 state252 不是市场报价先后，而是一个稳定的 worker 生产→配送无效动作链。尚未保存目标 tile 的完整字段：WATER 无效可由“非 PLANT/当天已浇水”造成，HARVEST 无效表示无可收 yield（或无 tile）；不可把它写成具体坐标/作物的确定因果。

**结果二（Pipe，8/8 现金可行性链条）：**action 31 双方均请求 `BUY_PRODUCT WHEAT 1`、报价 29；V45 现金 21，8/8 失败，Pipe 现金62，8/8 成功。该一单位缺口后续变为 state81 的相同 `SELL WHEAT 3` 请求（V45 实卖2、Pipe3）和 state101 的相同 `SELL WHEAT2`（V45 0、Pipe2）。在两个卖货 action 均无 PLACE WHEAT，故是既有 shed 库存差，不是当回合 worker 放货失败。开局 70/70 对 Pipe5/5 的现金路径与这个失败相连，但尚不可直接得出应改为某个固定开局数值。

Kaito step251 也存在 melon 交付量差但 V45 仍大胜，故单一成交节点不决定通用策略；V46 step2 稳定实卖 wheat30 是独立的高暴露开局库存路径。完整可读报告为 `docs/V45_WORKER_DIAGNOSIS_V1.md`。

**决定：仍不改 V45。**下一道验证门需要用户先选/审核一个独立假设：sdy 方向要先补充最小 tile snapshot，再验证“仅预约可执行的浇水/收获/卖货”；Pipe 方向做只截断不可支付 market buy 的现金安全变体。两者不可合并，任一候选须先作对手局验证，之后才有资格进入七对手 paired promotion。风险仍是公开近亲池和 8 条条件不能预测正式排行榜。

### 2026-09-18：sdy 无效动作的 tile 状态核验（已完成；未改策略）

用户选择先查 sdy，并明确同意开始。目标是用新的、独立的只读 trace 重跑 sdy 的 8 条固定 development 条件，仅在 action 245/246 对双方 `hand_0` 保存其脚下坐标、tile 的 crop/kind/`watered_today`/`yield_units`/种植日/肥料状态，以及 WATER/HARVEST 前后状态和动作是否改变 tile。此最小快照用来区分“空地/非植物”“当天已浇水”“无可收 yield/未成熟”等原因；不修改 V45、不提出规则、不重用已有 dirty worker JSONL。质量门：8/8 终局仍严格复现冻结联赛；只有 tile 原因跨 8/8 固定后，才可以草拟一个仅对该原因触发的 fallback 方案。取舍是多跑 8 局换取具体失败原因；风险是即使定位，也不等于 fallback 会提升总分。

实现为 `lab/sdy_tile_diagnostics.py`，原始证据为 `experiments/v45_sdy_tile_diagnosis_v1.jsonl`，报告为 `docs/V45_SDY_TILE_DIAGNOSIS_V1.md`。8/8 均 `DONE/DONE` 且终局严格匹配冻结联赛。action 245/246 的 tile 结论在 8/8 完全重复：V45 `hand_0` 坐标 `(0,4)` 的 tile 是 `None`（空地），故 WATER/HARVEST 均不改变状态；sdy 同坐标是 day0 种下的 MELON，step245 前 `yield_units=5`、`watered_today=false`，WATER 后为 `yield_units=6`、`watered_today=true`，step246 HARVEST 成功并清空地块。它随后正是 sdy 可在 step251 多放入的 6 melon。

**白话解释：**V45 不是“西瓜没熟却去收”，而是固定动作表还在照料一块自己早已没有西瓜的空地；sdy 那里真的有西瓜。把无效动作替换为 PASS 不会长出西瓜、不会补回 1,258 现金，故不是有效小修。真正待追踪的是更早的 `plant / harvest / dig` 分叉：V45 为什么没有在 `(0,4)` 保留这株西瓜、却仍给该格排了后续浇水/收获任务。若继续，先做只读的“该格从开局至 step246 的 tile 事件谱”，然后再审核是否存在可独立验证的“种植—任务表一致性”变体；不得直接加入 fallback 或调卖货规则。

### 2026-09-18：sdy `(0,4)` 种植—清除历史（已完成；未改策略）

用户明确要求继续查，并接受此前提出的下一步。将重跑 sdy 的相同 8 条条件，独立保存双方 `(0,4)` 从 step 0 至 246 的逐回合 tile 前后快照，以及任何角色在该格执行的动作和该动作前后 tile。目标是判定 V45 的空地来自“从未 PLANT”“HARVEST 后未再种”“DIG/其它清除”还是状态变化；不改 V45、不修改动作、不沿用现有原始 trace 路径。质量门仍为 8/8 严格复现冻结终局；只有该历史原因 8/8 固定后，才可提出单一的种植—任务表一致性候选。风险：即使定位到路线分叉，保持那株西瓜仍可能牺牲其他路线收益，必须独立联赛评估。

实现为 `lab/sdy_tile_history.py`，原始 trace 为 `experiments/v45_sdy_tile_history_v1.jsonl`，报告为 `docs/V45_SDY_TILE_HISTORY_V1.md`。8/8 `DONE/DONE` 且终局严格匹配冻结联赛。最早的、唯一关键分叉在 action step18：双方 `hand_4` 均在 `(0,4)`，V45 8/8 `PASS`、tile 仍空；sdy 8/8 `PLANT MELON`、tile 变为 day0 MELON。sdy 后续正常浇水、累积产量，在 step245 将 5 增至6、step246 收获；V45 一直为空地。故先前定位的 1,258 现金差来自 sdy 多种的一株西瓜，而不是 V45 意外清掉已种的作物、市场报价或随机 worker 故障。

**解释与决定：**在 V45 action18 硬加 `PLANT MELON` 虽是小代码改动，但策略上是在复制 sdy 的公开路线片段，不能称为原创或默认采纳；它也未证明能跨七对手获益。当前不改 V45。若用户希望继续策略研发，下一方案应从固定坐标/回合抽象为可验证的生产投资原则（空闲雇工 + 空地 + 可用 seed + 足够剩余生长期时，何时补种），明确其现金/市场风险和联赛门槛后再审核执行。不得因这一个 sdy 节点直接 copy plant action。

### 2026-09-18：空任务槽回填规则的旁观验证（已审核，第一轮已完成但未通过精确性门）

用户确认“空任务槽回填”的通用化思路，并同意先做旁观验证。目标：不改变 V45 返回的任何动作，只在本地 trace 中标记候选补种机会：当前角色位于空地且原动作 PASS、持有已有种子，并能从 V45 自身后续路径中找到同格的照料/收获服务窗口；再按剩余时间、种子、现金安全和保守出售价值筛掉不合格机会。先记录该规则在固定对手池中的触发数量、坐标、作物和后续是否实际拥有服务窗口，特别核验它能否识别 sdy `(0,4)` 而不是固定记忆该坐标。取舍是先增加一轮只读 trace/分析、不立即得到分数；影响是将路线差异抽象为可检验的规则而非复制公开单点；验证门是候选只读 trace 终局 100% 复现、候选确有明确服务窗口且不普遍误触发。只有用户再审核同意，才把通过的最小规则做成 V45 variant，并走七对手 paired 联赛。风险：未来路径基于原 V45，不保证在实际补种后仍完全相同；旁观验证不能证明收益。

第一轮已由 `lab/latent_slot_audit.py` 的 `v45-latent-slot-dryrun-v2` 完成，详见 `docs/V45_LATENT_SLOT_DRYRUN_V2.md`。覆盖七个固定对手、4 seed、左右座位共 56 条开发条件；56/56 终局现金精确复现冻结 V45 联赛，因此观察器没有改变 V45 动作。它在 `sdy2842` 的 8/8 条件中都自动找到第 18 步 `(0,4)` 的 MELON 机会：雇工 4 原本 `PASS`、有已有种子、原路线之后在该格浇水、246 收获、249 卖货。这证明规则没有硬编码该坐标或回合。

但该筛选不合格，不能制作 variant：它平均每局还标出 11–15 个槽，合计 STRAWBERRY 486、WHEAT 328、MELON 8 个候选。原因是“未来会经过并照料”只能说明可能可行，不能证明不挤占路线/现金且收益更高。结论是目前仅为机会探测器；没有实际种植、没有新的分数、也没有策略变更。`experiments/v45_latent_slot_dryrun_v1.jsonl` 是保留的失败尝试：只跑一局且漏记市场卖单，错误得出零候选，不能使用；v2 是有效数据。

下一步尚未获准：若用户审核同意，仍只做旁观验证，增加更严格的“种下后 1–2 步内同一雇工在同格浇水”等服务连续性筛选，测量能否保留 sdy 西瓜同时把泛滥候选压到极少数。只有该过滤通过，才另行提出最小实际种植 variant 与七对手联赛方案。风险不变：未来路线来自未改变的 V45，旁观命中不等于反事实下仍照料或赚钱。

### 2026-09-19：空任务槽即时照料筛选（已审核；部分通过，尚不能做 variant）

用户同意上一节的收窄验证。`lab/latent_slot_audit.py` 升为 `v45-latent-slot-dryrun-v3-immediate-service`：除 v2 的空地 PASS、已有种子、未来浇/收/卖窗口外，要求**同一角色**在接下来的 1–2 步中仍在同一坐标执行 WATER。仍只观察、不改变 V45 返回动作；有效 trace 为 `experiments/v45_latent_slot_dryrun_v3.jsonl`，汇总为 `experiments/v45_latent_slot_dryrun_v3_summary.json`，说明见 `docs/V45_LATENT_SLOT_DRYRUN_V3.md`。

验证通过：固定七对手 × 4 seed × 左右座位共 56/56 条件终局严格复现冻结联赛；sdy 的 `(0,4)` MELON 在 8/8 仍被发现，且确实是 hand4 step18 PASS 后、同一 hand4 step19 WATER。精确性有改善：v2 每局 11–15 槽、STRAWBERRY 486/WHEAT 328/MELON 8 候选，v3 降至每局 3.5–7 槽、STRAWBERRY 186/WHEAT 100/MELON 8。

但**未达到实际种植门**：sdy 每局仍稳定有 7 个槽（目标 MELON 1、STRAWBERRY 4、WHEAT 2），其余对手也有 0–7 个；这说明它们是固定路线空档而非随机假阳性，却仍不能证明哪项投入不挤路线、最值得投资或实际会赚钱。没有创建 V45 variant、没有新得分。若继续，需用户重新审核一个只读的“每局按通用资金/时间/预计卖价原则只选择一个最高价值机会”的比较方案；不得写作“优先 MELON”、不得以 sdy 坐标/步数为条件。风险仍是反事实种植会改变后续 V45 观察和路线。

### 2026-09-19：每局单一机会的通用排序（已审核，进行中）

用户在询问“最值得”如何判断后，同意开始。目标：不重跑、不改 V45、不种任何作物，只将 v3 的已验证候选在每局压成至多一个提名。资格门保持 v3（原动作为空地 PASS、已有种子、同一角色 1–2 步内同格 WATER、原路线还有成熟期收获与同类卖货窗口）。比较规则不含作物、坐标、回合、对手名的特例：主排序为 `保守预计卖货收入 − 种子机会成本`，同分才选更早的原计划卖货步，再按稳定位置顺序消除并列。取舍：先用可审计的、没有臆造未来价格/路线收益的简单账本，代价是它还不含反事实市场预测、劳动成本和再投资价值。验证门：输入必须是 v3 56 条严格复现的 trace；输出必须明确每局只选零或一项、报告按对手/作物分布，并核验 sdy 8/8 是否自然选中 `(0,4)` MELON。无论结果如何都不产生实际 agent 或分数；只有下一次另行审核才能进入 variant。

已完成为 `lab/latent_slot_selector.py`，输出 `experiments/v45_latent_slot_top1_v1.jsonl` 和 `experiments/v45_latent_slot_top1_v1_summary.json`，报告为 `docs/V45_LATENT_SLOT_TOP1_V1.md`。输入为 v3 的 56 条严格复现 trace；52/56 产生唯一提名、4/56 无合格候选。规则没有作物/对手/坐标特例，却在 sdy 8/8 选择 `(0,4)` MELON（预估净值 1,420、step19 即时同人浇水、246 收、249 卖）；其余 44 条选择 WHEAT（预估净值 40–90）。这证明单一规则可把候选压为每局零或一项，并自然保留目标 MELON。

**关键限制与决定：仍不能创建实际 variant。**上述排序是赛后旁观：它使用“V45 后来确实会浇/收/卖”的未来 trace，真实 agent 在种下时没有这些未来观察。因此这不是可直接部署的实时决策，也不是收益证明或新比赛分数。若用户希望继续，须先审核一个新的设计：从 V45 当下可读的路线/任务表中提取未来承诺，或以当下可见的现金、库存、市场和路线状态做保守预测；不得把赛后 trace 当作实时输入。之后才可讨论最小实际种植 variant 和七对手验证。

### 2026-09-19：V45 路线承诺提取与外层删改单审计（已审核，进行中）

用户同意开始“路线承诺提取器”。审计先确认 V45 核心为 719 步预先计算的 route tape，并在 step144 依据当时已解锁商店切换 route、step648 固定换 route2；因此不能直接把单条赛后 trace 冒充当前已知未来。已审核的实现是只读 wrapper：在每个 raw-tape `PLANT` 上，仅从当前 tape、V45 router 自己所有仍可能的 day6 route 分支以及固定 day27 route 计算同一 actor/同一格的 WATER、成熟期 HARVEST、足额 SELL 是否在**所有**静态分支存在；再观察真正的 V45 外层最终发出 PLANT 还是删成其它动作。路线图不读取未来环境观察、不看对手名称、不改变返回动作。

目标：验证 sdy `(0,4)` 的 MELON 是否是 V45 自己跨路线写好的任务承诺，并定位是否被外层安全层删掉；同时在固定 56 条 development 条件统计同类事件。取舍：它比赛后 trace 严格得多，但仍不能保证 reactive 层和共享市场未来不变；影响是把“可执行的当前已知路线任务”与“事后恰好发生”分开。质量门：56/56 终局严格复现冻结 V45；每个报告的静态承诺必须只来自 V45 route 数据；sdy 结论必须给出 raw PLANT、最终 emitted 命令和安全层 telemetry 的可核验链条。无论结果均不改 V45/不产生 variant；若发现可修复的删改单模式，仍须用户另行审核具体修复和七对手评测协议。

完整有效审计为 `v45-route-commitment-audit-v4-suppression-focus`（`lab/route_commitment_audit.py`、`experiments/v45_route_commitment_audit_v4.jsonl`、`experiments/v45_route_commitment_audit_v4_summary.json`，说明 `docs/V45_ROUTE_COMMITMENT_AUDIT_V4.md`）。56/56 条件 `DONE/DONE` 且终局严格复现。开发中的 v1/v2/v3 只各有一局或含已修正的提取范围，保留但不用于结论。

**sdy 结论已确认，且必须用白话说明：**8/8 条件中，step18 原始 V45 tape 同时要求两位雇工各 `PLANT MELON`，但当前只有 1 粒种子；手序靠前的 hand0 被保留，hand4 在 `(0,4)` 的 `PLANT MELON` 被最终改为 `PASS`。同一回合 `_r124_atomic` 的 `opening_atomic_dropped` telemetry 均增加 1；故这是 V45 开局原子安全层按动作顺序处理同作物种子不足，而非市场随机或 sdy 发现了 V45 未知的新地块。V46 也有同类开局 WHEAT 删种（step21 `(0,3)` hand4，8 次）；后期 step383 有 52 个 WHEAT 删种，但本轮没有越证据归因。

同时否定了一个过强主张：从 step18 可静态确定 hand4 在 step19 同格 WATER，但不能从当前就绝对保证 step246 的同格收获；step144 路线会依当时 shops 切换，且 V45 外层会改移动/雇工路径。故没有把“原始 tape 以后有 HARVEST”当作实时已知事实。全池 13,277 次当前有种子的 raw PLANT 中，13,209 照发、68 被删；只可得出通用问题类型：**同回合同作物请求多于现有种子时，V45 当前按工号顺序分配，未比较候选的后续价值。**

决定：仍不创建 variant。若用户希望继续，必须先审核独立的“稀缺种子分配”方案：只在种子不够时重排原本已有的同作物 PLANT 请求，以当前可观察的即时照料、现金与短期路线证据排序；不得按 sdy/坐标/西瓜特判。之后再固定七对手 paired league 验证。

### 2026-09-19：稀缺种子分配器的实时信息影子验证（已完成，不产生 variant）

用户明确同意开始该策略。第一步仍不创建实际 variant，而是验证“只用当前就知道的信息”能否真正做出分配：仅在同回合同作物 raw `PLANT` 请求数大于当前该作物种子数时触发；候选必须当前空地可种。对每项候选只读取当前 V45 route tape 至当天结束（不跨未来 day6 route 选择），分数依次为“同一人 1–2 步内同格 WATER”与“当天同格 WATER 次数”。同 crop 的现金/种子成本相同，不纳入人为差异；分数并列则明确 abstain，保持 V45 原动作，绝不按对手、坐标、西瓜或雇工顺序破平局。

目标：检验这种真正实时、通用的短期证据能否在 56 条固定条件中唯一选出一个种植点，特别是能否不靠坐标地识别 sdy step18 的 MELON；取舍是宁愿大量 abstain，也不把赛后收获/卖货坐标伪装成已知事实。影响：若无法区分，则证明当前短期信息不足，不能直接做策略；若能唯一分配，才另行审核最小 wrapper variant。验证门：56/56 终局严格复现冻结 V45；所有 recommendation 可由当前 observation + 当天 route tape 复算；并列时零修改。

**结果：质量门全部通过，但策略门明确失败。**`lab/seed_conflict_shadow.py` 运行固定 56 条开发条件，全部 `DONE/DONE`、56/56 终局现金严格复现冻结 V45，故观察器没有改变动作。它只找到 16 次真正冲突：sdy step18 的 MELON 8 次、v46 step21 的 WHEAT 8 次；**16/16 都是并列、0 次唯一推荐。**sdy 的两块候选地都由各自雇工在下一步浇水一次、当天同格浇水一次，短期分数均为 `[1,1]`；所以不能诚实地说 `(0,4)` 比另一颗西瓜更值得这粒种子。没有创建 variant、没有跑新分数。完整说明见 `docs/V45_SEED_CONFLICT_SHADOW_V1.md`，有效记录为 `experiments/v45_seed_conflict_shadow_v1.jsonl` 和 `experiments/v45_seed_conflict_shadow_v1_summary.json`。

**下一位接手者不得跳过的结论：**“即时浇水 + 当天路线”不能为稀缺种子重排提供证据，不能加一个坐标/西瓜/雇工序号特判来强行修复。若用户要继续，先重新审核一个全新的受限短期模拟预测协议（目标、分叉步骤、可见信息、计算预算、预测校准、development/holdout 验证）；该模拟是预测，不是读取未来 trace。获批准并先完成预测校准前，不得创建实际 variant。

### 2026-09-19：动态 70/30 经营组合与每日规划器（已审核，第一阶段进行中）

用户提出并明确同意开始新策略：将**新增的自由生产能力**（不是全部已有资产，也不是每一回合强制比例）最多约 70% 投给当前综合价值最高的生产线，约 30% 投给第二名以分散市场/生产风险。生产线分数必须基于当前可见的商店需求、市场价格与库存、对手公开生产、启动成本、照料工时和剩余回本时间；现金、饲料、仓库空间和已有作物/动物的照料任务必须先预留，不够就少投或不投。当前官方引擎的 Town Center 每天固定消耗一份每种产品；不得复用旧代码的“后期自动 2 倍/4 倍 Town Center 需求”假设。已查公开相近实现会依据商店/价格/对手混种，但未发现明确、经过本地验证的固定 70/30 机制；这不是原创性或收益保证。

已审核的第一阶段只做独立、只读的影子经营大脑，不编辑 `third_party/v45/main.py`、不创建 Kaggle 提交、也不让建议变成游戏动作。已新增：`lab/portfolio_model.py` 以产品线（作物、蛋、奶、羊毛）计算可审计的优先级；`lab/daily_planner.py` 在每天开始列出必须的喂养/收获/关键浇水/清理任务并预留小麦、仓库和理论行动容量；`lab/portfolio_shadow.py` 包住冻结 V45，记录每日 70/30 建议但原样返回 V45 行动。验证门：先通过静态编译与单局严格复现；随后固定 56 条开发条件必须全部 `DONE/DONE` 且终局现金严格等于冻结 V45。该验证只证明观察器无副作用、评分能由当下 obs 复算；不证明策略有效、更不产生新分数。

实现过程发现并已纠正一个建模错误：最初的影子记录 `portfolio-daily-shadow-v1` 对动物只计第一份产出，却扣除了剩余赛季的全部饲料，导致羊/牛/鹅在评分中被不合理压低。v1 的 **56/56 终局严格复现** 仍可作为“观察器没有碰 V45 动作”的证据，但其推荐统计不允许再用于经营判断。原记录保留，不覆盖、不混入新结果。现用 `portfolio-daily-shadow-v2-recurrent-lines` 重新记录：动物按剩余天数内可发生的多次产出计算，并扣对应的持续饲料；作物的反复产出也同样按周期计算。下一验证门是重新通过静态编译、单局和完整 56 条影子复现，再检查推荐是否确实随公开的商店/价格/库存/对手状态变化；即使通过，也仍不是比赛分数或可执行 agent。

**v2 已完成。**`lab/portfolio_model.py`、`lab/daily_planner.py`、`lab/portfolio_shadow.py` 已通过编译；先单局、后补齐固定 56 条开发条件。全部 `DONE/DONE`，56/56 终局现金严格复现冻结 V45，记录 1,680 份日计划到 `experiments/portfolio_daily_shadow_v2.jsonl`，汇总为 `experiments/portfolio_daily_shadow_v2_summary.json`。它不是新比赛分数：V45 动作完全没有被替换。主线计数为 tomato 684、strawberry 488、carrot 242、wheat 154、无可回本线 112；同一组 shops 在不同公开市场/对手状态下会得到不同建议，故它不是固定路线表。

**v2 后的真实规则审计已完成，且否定了 v2 的经济排名。**已直接读本机官方引擎 1.32.7 及其 README，并用官方 `_daily_refresh_animals` 做 30 天小核对（不改 V45、无提交）。关键事实：番茄/草莓的 `max_yield=4` 意味着整株最多发生 4 次生产，绝不是每次生产 4 份；而动物每天 `FEED` + `CARE` 会把 CARE 奖励累计到下一次生产。day0 放下、每天都喂且及时收货时，鹅/牛/羊的 30 天总产量分别为基础 27/12/9、每天 CARE 后 56/39/38。当前 `portfolio_model.py` 将番茄/草莓的 4 当作每次产出并按周期累积，又遗漏动物 CARE 奖励，故 v2 的生产线排序（包括“动物 0 次前二”）**全部降级为无效经营结论**；唯有 56/56 非干扰验证仍有效。

详细事实表、测试假设和下一方案见 `docs/PRODUCTION_WORKLOAD_FACTS_V1.md`。用户随后明确要求先**修复重跑**，故当前已批准的范围是 v2.1 公式修复，不是 v3 新策略：番茄/草莓的每次基础产出改为 1，并限制整株最多 4 次；动物暂时仍是每天 FEED、但不额外 CARE 的保守基线。旧 v2 原始记录绝不覆盖，v2.1 使用独立 protocol/输出重新跑固定 56 条影子条件。修复后仍不得出动作或依据结果调分；CARE、建筑/送货和走路时间的完整比较仍须另行审核。

**v2.1 已完成。**`portfolio_model.py` 将 `units_per_yield_event` 与 `max_yield_events` 分开，番茄/草莓分别为每次 1、最多 4 次；开局 unit check 均为基础总产量 4。`portfolio_shadow.py` protocol 是 `portfolio-daily-shadow-v2.1-capped-recurring-crops`，输出独立保存为 `experiments/portfolio_daily_shadow_v2_1.jsonl` 和 `_summary.json`。固定 56 条开发条件全部 `DONE/DONE`，56/56 终局严格复现 V45，记录 1,680 份每日建议。修复明显改变推荐：v2 的 tomato/strawberry 主线 684/488 变为 24/0；v2.1 主线为 melon 576、wheat 557、carrot 409、tomato 24、sheep_wool 2、无可回本线 112，次线含 cow_milk 278、sheep_wool 34、goose_eggs 12。该变化只是从错误公式回到正确的基础产出计数，**绝非对局得分或策略胜率证据**。完整报告为 `docs/PORTFOLIO_DAILY_SHADOW_V2_1.md`。

**下一道未获授权的门：**不能因羊毛/牛奶终于出现就直接发动作或任调阈值。要继续，先审核“显式行动成本 + 动物 CARE 方式比较”：把建棚、取放、每天 FEED/可选 CARE、收货、送货、走路、现金/格子/仓库冲突列为可复算事实，并先另跑影子验证。此前冻结 V45 仍是唯一可对局基线。

### 2026-09-19：v2.2 显式 CARE 与田间动作下限（已审核并完成，仍为影子）

用户明确要求继续。已实现的范围：不发游戏动作，只在 70/30 影子评分里把每个动物分为 `*_basic`（FEED）和 `*_cared`（FEED + CARE）候选；动物产出按官方 CARE 累积规则逐次计算，最低田间动作数显式计入建结构、取动物、放动物、取小麦、每日 FEED、可选每日 CARE 和为避免 `max_held` 截断所需收货。作物也按种/浇/收（反复作物额外 DIG）计最低田间动作。饲料成本改为“剩余每天 1 小麦 × 当前小麦市场价”，不是种子价。明确取舍：这些是无走路的**下限**，不能假称可执行日程。

新增 `lab/test_portfolio_model.py`；其 2 个回归测试通过：番茄/草莓均 4 次基础生产；开局 30 天动物结果和田间动作下限为鹅基础 27/41、鹅 CARE 56/78、牛基础 12/36、牛 CARE 39/71、羊基础 9/36、羊 CARE 38/73。`portfolio-daily-shadow-v2.2-care-and-field-actions` 的 56 条固定开发条件全部 `DONE/DONE`，56/56 终局现金严格复现 V45，记录 1,680 份计划到 `experiments/portfolio_daily_shadow_v2_2.jsonl` / `_summary.json`。主建议中 cared sheep 604、cared cow 244、carrot 364、melon 148、wheat 136、tomato 68、cared goose 4、无可回本线 112；Yarn Store 在场时 cared sheep 是主建议 370 次。

**结果解释与下一门：**高照料羊/牛出现，证明 CARE 不再被当成免费，也不代表应实际养它们。v2.2 还没有走路，亦没有证明同一日能完成建棚/取放/CARE；这会尤其高估每天两次田间动作的 CARE 动物。完整报告 `docs/PORTFOLIO_DAILY_SHADOW_V2_2.md`。若继续，必须先审核“具体每日任务包 + 工人当前位置 + 曼哈顿移动 + 当天剩余回合”的可行性影子检查；仍不接管 V45，先只输出 feasible / infeasible 证据，不能按本结果直接调动物分数。

### 2026-09-19：v2.3 当前维护路线 witness（已运行；解释无效，必须修正）

用户要求继续计算后，已创建 `lab/task_router.py`，并把它接入每日影子计划：对**当前 observation 中已经存在的** mandatory FEED/HARVEST/WATER 任务，按真实曼哈顿移动（锁地可通行）和每个田间操作 1 回合，优先级优先地构造一条具体路线。成功仅证明这条路线可走；失败只报告贪心顺序失败。对应的第三个单测通过，且 `portfolio-daily-shadow-v2.3-mandatory-route-witness` 56/56 `DONE/DONE`、终局现金严格复现 V45，记录为 `experiments/portfolio_daily_shadow_v2_3.jsonl` / `_summary.json`。

**但 v2.3 的负载统计不得解释。**它只有 day0 的 56/1,680 份计划能 witness，其余 1,624 份排不下；原因是每天 hour0 observation 中雇工已经在前一日结束消失，而当天 V45 会在这一个 callback 的 market `HIRE` 订单中重新雇工。引擎顺序已核对：本回合先执行现有单位动作，之后处理 market `HIRE`；新手在该回合后出生，故从下一 callback 起（hour1、剩 23 回合）可用。v2.3 仅看 hour0 的农夫，错误地把几十个任务压给一人；这不是 V45 真的无法维护的证据。

下一修正会改变可行性检查范围，尚待用户确认：允许影子 wrapper 在拿到**本回合 V45 已经返回的 action**后，只读取其中实际 `HIRE` 订单，并按官方 NWSE/最低占位 spawn 规则虚拟加入这些已知当日雇工；从 hour1、23 回合开始为现有任务排路线。不得预测未来 callback、未来 HIRE 或市场成功以外的状态；若 market 中 HIRE 因现金不足，会按实际可付的顺序截断。先用引擎小测试核对 spawn/时序，再另用 56 局复现。未获此范围同意前，不修改 v2.3 的解释或出动作。

用户明确授权开始重算后，实际采用了更严格且更简单的方式：不虚拟 HIRE，而是在每个 day 的 **hour1** 读取引擎已经结算 HIRE 后的真实 observation，包含实际雇工数、出生位置与 farmer 首个动作后的农场，余 23 回合；这避免了市场现金/订单顺序预测。v2.4 56/56 仍严格复现 V45，但其固定坐标的同优先级顺序仅 witness 724/1,680 天。所有未 witness 日的任务动作总数仍低于“实际雇工数 × 23”，故不能称为纯人手数量不够。

### 2026-09-19：v2.5 动态同优先级路线 witness（已完成，仍只读）

用户要求继续计算后，`task_router.py` 改为在每一步从当前最高优先级任务中选择**全局最早完成**的工人—任务组合，而非按坐标固定顺序分配。该范围不改变雇工、任务定义、产品评分或 V45 动作。编译、3 个单测、单局后重跑 56 条：全部 `DONE/DONE`、56/56 终局严格复现，原始结果 `experiments/portfolio_daily_shadow_v2_5.jsonl` / `_summary.json`。可给出具体维护路线的日计划从 v2.4 的 724 增为 **924/1,680**，增加 200、零回退；其余 756 个贪心未 witness 集中 day15–28。868 个“有任务且已 witness”的日子完成后剩余行动格中位数 66。

**必须保留的解释边界：**924 不是 V45 真实维护成功率，756 也不是维护不可能；所有未 witness 条件在动作计数上都有足够总容量，剩余问题可能是贪心分配，而非总动作不足。当前没有把剩余格用于新增生产，也没有发动作。完整说明 `docs/DAILY_ROUTE_WITNESS_V2_5.md`。若用户要继续，应先审核多种确定性路线构造（动态最早完成、空间分区、最近邻）作为只读对照：任一成功即为路线证据，全部失败也只称这些构造均未找到；先分离路线算法局限与真实地图/任务密度，再谈把投资任务加入。

### 2026-09-19：独立可执行 agent v0（作物安全底座，第一阶段完成）

用户明确同意进入下一阶段并授权开始。已新建 `lab/portfolio_agent.py`，注册 `lab.runner` 名称 `portfolio_executor`，每局通过 `make_portfolio_agent()` 生成全新状态；不编辑/调用 V45 路线。v0 每天 hour0 有限 HIRE 和买 seed/卖 shed，逐回合对已有 FEED、HARVEST、WATER、DIG 优先分配并持久化目标；只在种子/空地允许时按 70/30 排名最高的**作物**种植。植物请求按当前种子数预约，避免原子全失败；每回合重验目标。

本机功能验证（720 steps，seeds 0–3）：对 starter 全部 `DONE/DONE`，平均 `27,185.0 : 3,533.5`，telemetry 确认实际种/浇/收/卖；对冻结 V45 也全 `DONE/DONE`，但平均 `15,587.5 : 171,658.0`、margin `-156,070.5`。这不是七对手结果或 Kaggle 预测；v0 远弱于 V45 是诚实的当前起点。完整说明 `docs/PORTFOLIO_EXECUTOR_V0.md`。

随后用户继续授权。动物任务包现已接入独立 `portfolio_executor`：BUILD 对应结构、市场 BUY_ANIMAL、shed PICKUP、结构 PLACE、每天先取小麦再 FEED、只对实际已喂动物 CARE。逐回合 trace 先发现喂养者未取小麦，修复后又发现第二头已买动物会因目标数量 cap 被永久留在 shed；现按“已购未放也必须继续 PLACE”修正。最终重跑 seeds 0–3：对 starter 全部 `DONE/DONE`，均有 2 次真实 PLACE 且 44–45 次 FEED、41–44 次 CARE，平均 `36,791.25 : 3,503.75`、margin `+33,287.5`；对 V45 全部 `DONE/DONE`，均有 2 次 PLACE、44–48 次 FEED、43–48 次 CARE，平均 `21,299.5 : 166,139.75`、margin `-144,840.25`。这只说明动物生命周期实际运行，且相同四 seed 上较作物版缩小了部分对 V45 的差距；它依旧严重落后，不能称改进成功或推断排名。更新说明见 `docs/PORTFOLIO_EXECUTOR_V0.md`。

用户已批准下一门：以新 append-only 文件运行固定 development 的七外部对手 paired league（7 opponents × seeds 0–3 × 双座位 = 56 条配对条件，候选与 V45 各跑一次，共 112 局）。目的是测量同条件下候选相对 V45 的现金差；若开发集没有清楚正向结果，不跑 holdout、不报名、不提交。风险：agent 尚无土地扩张、市场择时和日内全局路径优化，预期会显著落后强参考。

该联赛现已完成，原始 append-only 账本为 `experiments/portfolio_executor_v0_league_v1.jsonl`。112 条 raw records 恰为 112 个唯一条件记录、无重复；56/56 配对条件完成。`portfolio_executor` 对冻结 V45 的平均终局现金差为 **`-80,740.411`**；按对手均为负：sdy2842 `-66,131.25`、v46 `-66,551.25`、indar_top10 `-73,278.75`、kaito_v58 `-88,768.375`、nathan_pipe7 `-77,321.125`、boatlee_v16 `-107,172.75`、indar_pasture `-85,959.375`，无任何非负对手。因此 promotion gate 明确失败：**不跑 holdout、不替换任何基线、不报名、不提交。**此负结果比 starter/V45 四 seed 功能门更有价值：它证实动物链能跑，但当前独立执行器的土地扩张、路线、市场与终局兑现能力仍远不够。下一步应先只读诊断相对 V45 的现金/资产/动作时间线，定位最大缺口；不得据此直接调参数或堆叠多个机制。

### 2026-09-19：主线改为 V45 父本 + 组合投资层（已审核，第一步进行中）

用户明确指出：从零执行器不可能合理地与成熟 benchmark 竞争；应把我们的创意叠在 V45 基线上，而不是试图替换 V45。计划因此更新为唯一行动基线：冻结 `third_party/v45/main.py` 不编辑；所有候选均以独立加载的 V45 module 为父本，保留路线、移动、卖货、库存和终局层；只在经过证实的“自由新增投资机会”上加可撤销组合层。`portfolio_executor` 及其负联赛结果保留为独立能力验证，不再作为冲分父本。

源码定位已完成：V45 在 step 144 按前两个店铺选择路线、step 648 强制终局 route 2；除此以外它已有动态番茄扩张（day 18 起）和羊群扩张（day 12 起）模块，二者都包含买地、买投入、雇工与具体后续劳动链。因而原路线中的 `BUY_SEED` / `BUY_ANIMAL` / `BUY_LAND` 与这些既有扩张都不能被 70/30 规则粗暴替换，否则会使后续种植、取放、照料或卖货路线断裂。第一步是只读机会审计：返回 V45 原动作不变，记录最终投资订单与当时 70/30 建议，再分类哪些是路线承诺、既有扩张或真正自由槽位；严格逐局复现是质量门。只有发现实际空槽，才另立一个单一覆盖机制，先小样本后七对手 paired development；开发集不通过不跑 holdout。详细协议：`docs/V45_PORTFOLIO_VARIANT_PROTOCOL.md`。

机会审计已完成：新增 `lab/v45_portfolio_audit.py`，记录 final V45 action 中的 BUY_LAND/BUY_SEED/BUY_ANIMAL/HIRE 及当时 70/30 建议；原始账本 `experiments/v45_portfolio_opportunity_audit_v1.jsonl`、汇总 `_summary.json`。56/56 条件均 `DONE/DONE` 且终局逐局严格复现 V45，证明观察器无副作用。总计 13,388 个含投资订单 callback，主要订单量为 HIRE 15,850、BUY_SEED WHEAT 9,112、STRAWBERRY 1,848、CARROT 1,724、MELON 664、三类动物 1,012、BUY_LAND 141。观察到的产品买入 14,550 单位中只有 3,054 与当时组合模型 primary 相同；这只说明两个系统经常不同，绝不说明其余订单可替换。最终 action 不能可靠归因内部 wrapper，但已确认绝大多数订单处在开局、day6 路线切换、day12 羊群、day18 番茄、day24–27 终局等承诺链中。**决定：不在最终订单层改动作。**下一道已批准的只读门是 step144 路线画像与“前 144 步兼容路由集合”审计：若没有安全备选 route，放弃该切口；若有，才能制定组合驱动 route-selector 的单一 variant。报告 `docs/V45_PORTFOLIO_OPPORTUNITY_AUDIT_V1.md`。

路线审计已完成：新增 `lab/v45_route_portfolio_audit.py`，静态读取冻结 route tapes，产物 `experiments/v45_portfolio_route_audit_v1.json`。41 条 route 中，route 0 是开局实际使用的前缀；除 route 1 外的 **40 条**与 route0 的 step0–143 action 完全相同，故存在可在 step144 切换的结构接口。它们后段作物骨架几乎固定（约 138 WHEAT、29 STRAWBERRY、29 CARROT plants），差异主要在鹅/牛/羊的组合；Yarn 相关路线约 7–9 羊，常规组合约 2–6 羊和 2–5 牛/3–5 鹅。结论不是“任意切 route 安全或会赚钱”，而是：首个组合候选若继续，应只在 step144、只从这 40 条中选择，并只在 70/30 primary/secondary 为动物产品时尝试改变动物组合；作物 primary 时保持 V45 原选择。下一步需要用户审核这一**具体候选机制与保护条件**，再实现 no-op 同分 smoke、最小合法性样本及开发集联赛。报告 `docs/V45_PORTFOLIO_ROUTE_AUDIT_V1.md`。

用户进一步澄清并明确授权开始：70/30 的正确对象是**种植与养殖共同争夺的新增土地/生产槽位**，不是只在动物内部配比。例：10 块新增可用地、羊毛第一、草莓第二时目标为 7 羊/3 草莓；若饲料、现金、工时、仓库或剩余时间只支持 5 羊，则实际应为 5 羊/5 草莓，剩余槽位继续依次流向第二、第三产品，均不安全才留空。已据此将近期第一步改为独立纯函数的容量分配器：产品排名必须去重，同一产品的 basic/cared 不可同时占两份；主产品最多拿 70% 理想槽位且受安全上限截断，余量按后续产品填充。先用固定 `5+5`、`7+3` 等单测验证，不发 V45 动作；随后才审计如何从 V45 实际 future commitments 计算“真实安全上限”。详细协议 `docs/PORTFOLIO_CAPACITY_ALLOCATOR_V1.md`。

容量分配器现已实现于 `lab/portfolio_model.py`：`choose_portfolio` 的 secondary 改为必须是不同产品；`allocate_ranked_capacity` 是不读环境、不发动作的纯函数；`allocate_new_capacity` 将当前 observation 的去重产品排序接入该函数。`lab/test_portfolio_model.py` 从 3 增至 6 个回归测试，全部通过：确认不同产品的第一/第二名；10 槽、WOOL 上限 5 时为 `WOOL 5 + STRAWBERRY 5`；上限足够时为 `7 + 3`；第二名受限时依次流向第三名，仍不足则明确 `unallocated_slots`。这证明分配规则符合用户说明，**尚未证明任何 V45 真实地块/现金/饲料/工时上限已经算对，更未修改 V45 action。**下一道执行门是只读地把 V45 在某日的既有 future commitments 扣除，分别计算各产品的 land/cash/feed/labor/shed/time 上限；不可把当前抽象 `available_slots` 直接接入 route 或 market。

当前日资源扣除审计已完成，脚本 `lab/v45_portfolio_capacity_audit.py`。首次 v1 只跑了 1 条后发现 HIRE 成本表在第 6 个工人后没有继续按官方 Fibonacci 增长；该 append-only 原始文件 `experiments/v45_portfolio_capacity_audit_v1.jsonl` 保留但**无效、不得引用**。修复为与 V45 `_fib` 相同公式后以新 protocol `v45-portfolio-capacity-audit-v2-fib-cost` 全量重跑，原始账本 `experiments/v45_portfolio_capacity_audit_v2.jsonl`：56/56 `DONE/DONE` 且逐局严格复现 V45，记录 1,680 个 day/hour1 快照。它在扣除当前强制 FEED/HARVEST/WATER 与当前 parent 市场订单后，只能得到**当前日残余**，不是整季安全上限：空闲已拥有地块中位数仅 1（最小 0）；可用小麦（shed+手持−当日既有喂养预留）中位数 13、最小 `-23`；当前强制任务 route witness 仅 924/1,680 日成功，成功日剩余工人回合中位数 45、最小 4。结论：不可把“10 块抽象槽位”或这些中位数直接接入 V45；V45 有不少日子连现有维护的贪心路线都未 witness，且会有饲料短缺。下一门必须做更强的 future-commitment/多日路线扣除，至少把 V45 已计划的土地、饲料、工人和 tile 使用按候选地块逐日占用算进去；否则只能保留容量分配器为纯模型。

多日地块/维护扣除现已完成：容量审计升级为 `v45-portfolio-capacity-audit-v3-multiday-tiles`，有效原始账本 `experiments/v45_portfolio_capacity_audit_v3.jsonl`，56/56 `DONE/DONE` 且严格复现。它在每个 day/hour1 保存具体空闲 owned tile 坐标，并事后只接受“同一 tile 在整段生产期都始终为空、每天 mandatory route witness 成功、每天剩余工人回合至少覆盖新增维护、动物每天还有 1 wheat 超过既有 feed reserve”的极保守窗口。结果：CARROT（需连续 4 天）只有 46 个窗口、每个都只剩 1 格；MELON（13 天）、STRAWBERRY（11 天）、以及从当日照料到终局的 cared SHEEP 全部 **0** 窗口。故当前结论不再是抽象的“可能没空间”：在不改 V45 既有路线/土地使用/维护的前提下，根本没有支持 `7+3` 新增种养组合的连续余量。容量分配器本身仍正确，但不能作为 V45 的“外挂扩张层”。下一项若继续必须改变 V45 的 route 本身，在完整 route 之间做组合选择或重写一条完整兼容 route；不能继续尝试在原路线旁边塞新羊/新作物。

### 2026-09-20：V45 day-6 动物路线选择器 v1（已开始；仅局部冒烟）

用户明确授权“开始吧，做动物”。实现选择的是 V45 的真实高层接口，而非替换单笔动物订单：新增 `lab.v45_variants.make_animal_route_selector_agent()`，在 step 144 将 V45 router 原本已经选出的 route 与所有“route0 前 144 步字节等价、且排除 route1”的兼容 route 比较。每条候选的完整 tape 自带建棚、买动物、买小麦、雇工、取放、照料、收货和销售链；selector 绝不自行改 `BUY_ANIMAL`、`HIRE` 或工人动作。当前 observation 用既有的 `evaluate_lines` 对 EGG/MILK/WOOL 排名（商店需求、价格/库存、对手公开生产、剩余时间），取前二动物为 70/30 目标；对每条 route 统计 step144–647 的既定动物购买量，加上当前已摆放动物，计算与 70/30 的距离，第三种动物计为偏离。只有**唯一**候选严格优于 V45 原 route 才切换；并列、原 route 不差、缺少两种动物候选或任何异常都 abstain。协议和取舍详见 `docs/V45_ANIMAL_ROUTE_SELECTOR_V1.md`。

先实现了 `v45-animal-route-identity` 模式：完全安装同一个 router hook，但必定返回 V45 原 route。`.venv/bin/python -m unittest lab.test_portfolio_model` 的 6 项既有测试通过；对 starter 的 seed 0、1 的 720 回合 identity A/B 都 `DONE/DONE`、终局现金逐局完全相同（seed0 `181415:3506`，seed1 `146016:4043`），且各只在 step144 调一次、零 switch。这只证明 hook 的两个局部烟测无副作用，**不等于 56 条严格复现已经完成**。

真实 selector 对 starter 的 seed0–3 小样本均 `DONE/DONE`：seed0 `-841`、seed1 `0`（最佳候选 route 并列而 abstain）、seed2 `+4632`、seed3 `+4751` 对 V45。三个实际 switch 均为当前 MILK 第一、WOOL 第二，从原 route 121/107/105 切到 route123；该 route 的静态后续购买由 `MILK4,WOOL4,EGG3` 变为 `MILK5,WOOL3,EGG3`，当时已摆放 `MILK4,WOOL2`。这 4 局是连通性与 telemetry 检查，starter 不是七对手开发集，样本也太少，**不得称为收益提升或进入 holdout/提交**。下一验证门：先补齐 identity 在固定七对手 development 条件的复现，再对真实 selector 运行同一 56 条 paired 条件；若任一未完成、异常或未过既定联赛门槛，即停止，不报名不提交。

identity 联赛的首次运行账本 `experiments/v45_animal_route_identity_league_v1.jsonl` 在工具超时后后台进程仍继续时，被一次误重试并发写入，形成 41 行、28 个 unique key、8 个完全相同的重复 key；`lab.league` 的重复保护已正确拒绝继续追加。该账本**无效且不得引用**，按 append-only 纪律保留，不覆盖、不去重。后台进程已停止。后续 identity 验证必须使用新的干净账本 `experiments/v45_animal_route_identity_league_v2.jsonl`，并只保持一个运行实例；这是运行编排错误，不是 selector 或 V45 行为差异。

干净的 identity 开发集已完成：`experiments/v45_animal_route_identity_league_v2.jsonl` 恰有 112 条唯一 raw records（identity 与 V45 各 56），无重复、无冲突；56/56 paired conditions 全部 `DONE/DONE`。每个对手的平均差、以及全部条件的平均差均为 **0.0**。因此“在 step144 内部 router 挂入 selector、但返回原 route”可逐局严格复现冻结 V45，hook 层已通过质量门。注意 league 的 `minimum_gate_met=false` 只是其设计要求严格**正**均值，identity 的 0 是正确的预期而非失败。现在才允许运行真实 `v45-animal-route-v1` 的同一 112 局开发集；在其完成前，无收益判断、无 holdout、无报名、无提交。

真实 `v45-animal-route-v1` 开发集现已完成，干净 append-only 账本为 `experiments/v45_animal_route_v1_league_v1.jsonl`：112/112 unique raw records、无重复/冲突，56/56 paired conditions 全 `DONE/DONE`。平均相对冻结 V45 为 **`-1,114.286`**，逐对手为 sdy `-38.5`、v46 `-286.0`、indar_top10 `-141.0`、kaito `0.0`、pipe7 `-648.25`、boatlee `-6,686.25`、indar_pasture `0.0`；仅 2/7 非负，promotion gate 明确失败。故 v1 **停止**：不跑 holdout、不将其合入基线、不报名、不提交。

失败原因已有精确定位，不能被总体均值掩盖：selector 在 42/56 条件 abstain，故这 42 条严格同分；仅 14 条实际切换，合计 `-62,400`。其中 `105 -> 123` 10 条、平均 `-1,805.4`；`7 -> 123` 2 条、各 `-22,779`（都在 boatlee seed3 双座位）；`108 -> 123` 2 条、各 `+606`。切换一律因为 day6 当前排序为 MILK 第一、WOOL 第二，现有动物是 MILK4/WOOL2，静态购买画像让 route123（MILK5/WOOL3/EGG3）比原 route 的比例更接近 70/30。结论是**比例相近不等于整条路线相近**：route7/105/108 和 route123 虽可共享前六天，却仍携带不同的商店适配、作物、雇工、输入、卖货时序；只数动物购买量的 selector 漏掉这些经济耦合，不能仅调权重或解除并列来“修好”。若继续，下一份方案必须先做只读的 route7/105/108 vs 123 全路线差异/现金时间线审计，解释这几条 route 的差异，再提出受该证据约束的新选择规则；当前不存在获准的 v2。

用户要求“查查差异原因”后，该只读审计已经完成。新增 `lab/animal_route_diff_audit.py` 和可复算 `experiments/v45_animal_route_diff_audit_v1.json`；脚本静态比较 route 7/105/108/123 的 raw tape，并重放 Boatlee 的三个实际 switch（seed1 `108→123 +606`、seed2 `105→123 -4572`、seed3 `7→123 -22779`），每次终局严格复现联赛账本。报告为 `docs/V45_ANIMAL_ROUTE_DIFF_AUDIT_V1.md`。

关键事实：route7 不是普通“羊多一点”的路线，而是 V45 对 `PIZZA_SHOP + YARN_STORE` 的专门回应，静态为 `COW2+SHEEP9`；route123 是 `PIZZA_SHOP + PIZZA_SHOP` 的 `COW5+SHEEP3+GOOSE3`。在 seed3 day6，组合模型仍因 MILK 当前价格相对其基础价较高而给 MILK `121.69`、WOOL `112.14`，尽管 Yarn 的 WOOL 日需求为 13、Pizza 的 MILK 日需求为 7。切换后 candidate 最终为 `EGG3/MILK9/WOOL11`、V45 为 `MILK6/WOOL17`，作物相同；day20 candidate 的 MILK 价 `7` vs V45 `63`，终局 `5` vs `89`，现金从 day20 `-7975` 扩大至 `-22779`。seed2 的 105→123 只多一牛少一羊，raw tape 大部差异仅 day6–11，但早期市场/现金不同会经 V45 反应层放大，终局 MILK `1` vs `13`、损失 `4572`。结论更精确：当前模型过度信任 day6 的相对价格和静态最终比例，未验证商店能吸收 route 的未来累计产量；**不得直接改比例/阈值出 v2**。若继续，需先审核“未来累计产量 vs 已知商店需求”的硬拒绝门能否先排除 Pizza+Yarn 误切换。

用户明确要求把后续更复杂路线的考虑写入长期规划；已更新唯一主计划 `docs/V45_PORTFOLIO_VARIANT_PROTOCOL.md` 的“2026-09-20 起的长期路线规划”。优先级现为：已开放商店每天具体购买量（最高）→ 自己当前+候选 route 的未来可兑现产量 → 对手**公开**生产 → 当前价格/库存（仅短期权重）→ 现金、雇工、饲料、土地、仓库、移动和剩余时间 → 未开放商店的保守风险情景。明确禁令：不得把未知未来店铺或对手私有计划当作事实；不得由一个当前价格信号推翻 Yarn 等已知购买量。首个未来硬门是“当前已知商店在剩余天数内能买多少 vs route 将产多少”，必须先在失败的 Pizza+Yarn 反例中拒绝 7→123，才可提出新 selector 并请求用户实现授权；当前仍无 v2。

用户随后纠正了 70/30 的对象，长期计划已同步修正：**70% 不是单一第一名商品，是真正紧俏商品的整个集合。**若牛奶和羊毛都紧俏，牛+羊合计应占 70%，而不是牛 70%、羊 30%；这 70% 内部未来须按各自已开店每日购买量、自己已有/候选产量、对手公开产量和资源上限细分。余下 30% 才给不属于紧俏集合、但价值最高的生产线；紧俏品有三种时三者共同分 70%。历史 `v45-animal-route-v1` 的“MILK 第一 70 / WOOL 第二 30”实现因此是对用户策略的错误理解，保留为失败实验，不得作为后续方案基础。

用户再次精确纠正了上述“紧俏品集合占70%”的表述，该表述也**不正确**，已立即替换为最终定义：70% 不是土地/生产槽位份额，而是**每一种紧俏商品分别覆盖其当前已开店剩余购买量的70%**。公式为 `目标产量=已知剩余购买量×0.70`，`缺口=目标−(已有生产+已承诺路线未来可收获生产)`。例如 Pizza+Yarn day6 的已知剩余购买量约 MILK168、WOOL312，故各自目标约 MILK118、WOOL218；牛奶已有/承诺产量已达118就停止加牛，把新增地/钱/工人转给仍未达目标的羊毛或其他有缺口商品，全部紧俏目标已填后才选非紧俏高价值线。历史 v1 不只是把 MILK/WOOl 排成70/30错误，更根本地把70%错当作产品组合份额；不得复用。唯一主计划 `docs/V45_PORTFOLIO_VARIANT_PROTOCOL.md` 已同步覆盖旧说法。

用户随后明确授权按长期六项判断开始，当前已完成第一项的只读供需审计：新增 `lab/route_supply_demand_audit.py` 与说明 `docs/V45_ROUTE_SUPPLY_DEMAND_AUDIT_V1.md`。它重放 Boatlee seed1/2/3 的既有实际 switch，day6 只用当时已开店的每日购买量×剩余24天作确定需求，并从真实 `HARVEST` 与**下一帧**同工人 inventory delta 计实际收获；三局终局严格复现原联赛。首次输出 `experiments/v45_route_supply_demand_audit_v1.json` 因 action/状态帧错位导致收获全0，**无效不得引用**；v2 虽对齐修复但 protocol 标签陈旧，最终可引用的是 `experiments/v45_route_supply_demand_audit_v3.json`（protocol `known-shop-supply-demand-audit-v3-transition-aligned`）。

最关键的 Pizza+Yarn seed3 数据：candidate 123 收获 MILK228（Pizza已知剩余可买168，超60）、EGG77（已知24，超53）、WOOL235（Yarn已知312，少77）；原 V45 route7 收获 MILK179（超11）、WOOL369（超57），终局原 route 多 `22779`。这支持“候选把更多货放入购买较少的牛奶/鸡蛋、同时减少 Yarn 明确高购买量羊毛”是失败方向；但同时证明原先绝对硬门不成立，因为 route7 的羊毛也略超过312却仍胜。因此长期计划已修改：不是“任一超量即拒绝”，而是下一道**相对父本新增过剩门**——候选若增加购买较少商品的超量、同时减少购买较多商品的供给，才拒绝；该门必须先离线拒绝 `7→123` 而保留 route7，才可提出 v2。当前没有实现 v2 或跑新联赛的授权。

按用户最终“每种紧俏商品分别覆盖已知购买量70%”定义，已新增只读 `lab/target_coverage_audit.py` 和说明 `docs/V45_TARGET_COVERAGE_AUDIT_V1.md`。它读取有效 transition-aligned v3 harvest 重放，目标=`known shop remaining capacity×0.70`；只把每日购买超过 Town Center 基础1份的已开店商品视作紧俏。candidate 只有在填补新的70%缺口、且不增加另一种已达标紧俏货超额生产时才可通过。输出 `experiments/v45_target_coverage_audit_v1.json`；三个历史 switch 的结果均为拒绝：seed1 `108→123 +606`（只增加已达标 STRAWBERRY 超额，未补缺口）、seed2 `105→123 -4572`（增加已达标 MILK）、seed3 `7→123 -22779`（增加已达标 WHEAT/MILK）。seed1 的小赢也拒绝是符合用户规则的安全行为，不是测试失败。

这个检查**仅是赛后规则验证，不能装进 agent**：它使用真实未来 HARVEST。下一道未完成门是只读 day6 生产预测器，输入只能是当时地图、已有生产、候选 route 已写明动作和剩余时间；必须先在这三局对实际收获做误差审计，才可提出任何新 selector/v2。当前无 v2、无新联赛、无报名或提交。

用户再次授权“开始”后，day6 动物预测第一版已完成，代码 `lab/day6_animal_forecast_audit.py`、说明 `docs/V45_DAY6_ANIMAL_FORECAST_AUDIT_V1.md`、结果 `experiments/v45_day6_animal_forecast_audit_v1.json`。它仅从 day6 observable 已有动物和 raw candidate route 后续 `PLACE` 动作、官方 first-yield/interval/max-held、假定每日 FEED+CARE+及时收货，给出动物产量乐观上界；三局都重新生成 day6 observation 并对 effective route 的真实 harvest 作误差表，未改 V45/未选 route。

关键结果：它抓到 candidate123 牛奶明显偏多（seed3 预测270、实际228，Pizza已知70%目标约118；V45 route7预测192、实际179），故“不要再加牛”的方向有用；但羊毛尚不可用，route7预测284 vs 实际369、candidate123预测144 vs 实际235。原因是 raw tape 外 V45 有条件性羊群扩张等 overlay；v1 也假定完全照料/收获。结论：v1 只能作明显过产预警，**不能接 route selector**。下一道未完成的只读门是将番茄/羊群 overlay 分类为 day6 已确定触发、已确定不触发、依赖未来价格/资金的风险，并核对加入已确定项是否降低三局羊毛误差；未经该审计不得实现 v2。

该 overlay 分类门现已完成（用户授权“开始”后的只读审计）：新增 `lab/v45_overlay_classification_audit.py`、`experiments/v45_overlay_classification_audit_v1.json` 和说明 `docs/V45_OVERLAY_CLASSIFICATION_AUDIT_V1.md`。它对三条既有 Boatlee 反例，在 day6 分别检查 raw route 是否已因自身日程而让 V233 六羊或 V219 番茄扩张不可能；若没有静态冲突，明确列出后续才会知道的门槛，并完整重放取得 V45 telemetry 验证实际是否触发。原 route 108/105/7 与错误候选 route123 全部没有静态冲突，故六羊（day12）和番茄（day18）在 day6 均是**未来不确定**，不能当作确定产量，也不能一律按零处理。实际回放恰好覆盖三种情况：seed1 两边均触发番茄（10株、确认收获80）而不触发六羊；seed2 两种都不触发；seed3 两边均触发六羊（额外羊毛收获108）而不触发番茄。这解释 seed3 raw forecast 漏羊毛约108，却不允许把108固定补到所有局。唯一主计划 `docs/V45_PORTFOLIO_VARIANT_PROTOCOL.md` 已同步：day6 只能有 raw-route 的保守情景，overlay 只能作为写明 day12/day18触发条件的上行情景；不得混为一个确定生产数字。下一道**未完成、仍仅只读**的门是设计双情景预测表，并验证它不会把未来店铺、价格、现金或土地当作当前已知；此门前无 v2 selector、无新联赛、无报名或提交。

用户随后纠正：即使“未来店铺、价格、现金”可做预测，也不应因为随机未来 Yarn 可能出现而在第6天为羊毛路线加分；应直接选当前已开店条件下最好的种养，未来店真的开了再当场响应。此前提出的“六羊机会准备度加权”因此**明确废弃，未实现**。用户授权“开始”后，已改做只读 `lab/day6_known_demand_route_audit.py`，结果 `experiments/v45_day6_known_demand_route_audit_v1.json`，说明 `docs/V45_DAY6_KNOWN_DEMAND_ROUTE_AUDIT_V1.md`。它枚举64种 V45 已知两店组合，严格复刻 router 的分支：有 Yarn 则走 V39/Yarn branch，否则走 EXP240/non-Yarn branch；只统计 raw route 的种植和动物摆放承诺，绝不把未来店、未来价格或 overlay 算入。关键结果：Pizza+Yarn 的 V45 route7 是2牛+9羊，所有单 Yarn 组合基本也是2牛+9羊，双 Yarn 为12羊；route123 则是 Pizza+Pizza 的5牛+3羊+3鹅。因此 V45 已对当前 Yarn 需求做了专门动物选择，历史 selector 用 route123 替 route7 是拿错了整条店铺路线，不是可修的小比例问题。主计划已同步：下一道未完成、仍只读的门是**在 V45 已选定的当前店铺完整 route 内**审计是否有不破坏土地/工人/饲料/仓库/卖货链的自由新增投资槽；只有存在这种槽，才可按各商品当前已开店70%覆盖缺口决定投向。没有槽就不改 route；无 v2、无新联赛、无报名或提交。

在继续该门前已复核项目已有的 `lab/v45_portfolio_capacity_audit.py` 有效 v3 结果，故没有重复运行：`experiments/v45_portfolio_capacity_audit_v3.jsonl` 对七对手×四seed×双座位的56条条件均严格复现 V45，并在“同一格贯穿生产期始终空闲、既有维护每天都能执行、饲料与工时均有余量”的严格实时安全标准下，得到草莓、西瓜和照料羊的安全窗口全为0；胡萝卜只有46个赛后确认的单格窗口，且不构成通用实时扩张能力。这使当前结论更明确：虽然 V45 第6天已经按当前店铺选整条种养路线，但我们没有证据能安全把新作物/动物外挂到该路线；不能为了70%规则硬塞。改变生产结构只能比较或重写完整 route。用户 2026-09-20 要求冲前 10% 后，已实现下一刀 `v45-animal-route-v2`，见下节。

### 2026-09-20：冲前 10%；v2 同店铺族 70% 覆盖选择器（开发集未通过，已停止）

止损建议被否决。判断未变：外挂空地不可行，v1 跨店铺换路线会把 Yarn 局切成双 Pizza 路线。冲榜第一刀仍是第 6 天换一条完整路线，不是从零执行器，也不是抄 sdy 单格西瓜。

已实现 `v45-animal-route-v2`：只在原路线所属 Yarn 表或非 Yarn 表内比较；产量 = 当前已有动物 + 候选路线写明放畜的乐观上界；每种紧俏动物产品目标 = 已知剩余购买量 × 70%；唯一多补缺口且不增加另一种已达标货超量时才切换。Pizza+Yarn 的 route 7 候选集不含 route 123。协议 `docs/V45_ANIMAL_ROUTE_SELECTOR_V2.md`。不编辑冻结 V45。身份 hook 沿用已通过的 v1 identity，不重跑 112 局 identity。

单测 2 项通过：Pizza+Yarn 的 route 7 不能选 123；双 Pizza 的非 Yarn 族可以考虑 123。对 starter、seed 0、720 回合连通性 `DONE/DONE`，终局现金 `181415:3506`（与历史 V45/identity 同局相同）。

七对手 development 已完整结束：账本 `experiments/v45_animal_route_v2_league_v1.jsonl` 112/112 局、56/56 配对、全 `DONE/DONE`、audit clean。相对冻结 V45 的等权平均差 **0.0**，七个对手组均为 0。56 条候选局全部 abstain、0 次切换、0 error。原因与 starter 烟测相同：第 6 天乐观动物产量通常已经超过已知紧俏货的 70% 目标，同店铺族里没有“再补缺口且不增加超量”的完整路线。`minimum_gate_met=false` 是预期的：门槛要求严格正均值，打平不算晋级。

**决定：不跑 holdout，不把 v2 合入基线，不报名、不提交。** 这一刀证明规则能避免 v1 的 Yarn→Pizza 误切换，但不能多赚钱。下一刀若继续冲前 10%，按预注册只做独立的“现金不够不下买单”，不得与 v2 叠加。报名仍需用户另说“报名”。

### 已完成但未通过：首个可归因 variant（R85 feed multiplier）

为了先验证 variant 管线，已创建 `lab/v45_variants.py`、`lab/v45_feed_search.py`、`variants/v45_feed_multiplier/README.md` 和 `docs/V45_FEED_VARIANT_PROTOCOL.md`。该 variant 不编辑冻结 V45 文件：每局加载隔离的 V45 module，只替换 R85 `_r85_feed` 中的一个经济判断系数。父本为 `1.25`，候选为 `1.15`（更倾向跳过喂养）和 `1.35`（更倾向保留喂养）；路线、市场、工人、其他 safety layer 均保持父本逻辑。

已做不变量 smoke test：把 override 设回 `1.25` 后，`seed=7` 对 `starter` 与冻结 `v45_base` 的终局现金均为 `172,566`，`feed_skips=5`，双方状态 `DONE/DONE`。这证明 override 没有在父本参数下改变行为。

预注册 development 为 18 局（`v45_base`、`starter`、`melon` × seeds 0/1/2 × 两候选）。结果：

| 候选 | 对 V45 平均 margin | 对 starter 平均 margin | 对 melon 平均 margin | 结论 |
|---|---:|---:|---:|---|
| `v45-feed-m1.15` | -190.3 | +162,265.3 | +161,172.3 | 两候选中较好，按预注册规则进入 holdout |
| `v45-feed-m1.35` | -238.7 | +162,265.3 | +161,172.3 | 淘汰 |

holdout 固定为 seeds 1000/1001/1002，仅运行 `m1.15` 共 9 局：对 V45 平均 margin `+28.0`，对 starter `+150,590.0`，对 melon `+149,137.0`。27 局均为 `DONE/DONE`，逐局记录在 `experiments/v45_feed_multiplier_v1.jsonl`。

**判定：未通过，不升级。**development 为负、holdout 为正但只有 28，而每局终局现金量级为数万到十几万，方向不一致且效应量不足以证明改进；弱对手上的完全相同表现也不能算胜利。禁止基于这次 holdout 的 +28 再反向加密阈值搜索。此 variant 与协议保留为实验管线的成功验证和负结果；冻结 `v45_base` 仍是主基线。下一个小变体应另立协议，优先考虑实际触发但更独立的 R95 小麦安全库存；创造性主线仍按上一节单独立项。

### 已通过的验证

1. 官方环境可以本地创建 `make("kaggriculture")` 并完整跑完 720 回合。
2. `wheat` vs `starter` 相同 seed 连跑两次，reward 完全一致。
3. 简单 agent 的完整对局耗时约 `0.55s/局`；V45 对局约 `1.5s/局`。批量模拟是 CPU 为主的任务，尚不需要 GPU。
4. V45 已按其公开 Notebook 的重建 cell 写出，且 SHA-256 为：

   ```text
   2536d41ed5a00c75204b6350f1c76c54259c774cb065ba2a3a0072eedf210d94
   ```

5. V45 在本地环境中可编译、导入、完整运行，无动作错误。
6. 自有西瓜 agent 对 `starter` 的 seed 7 完整 720 回合 smoke test：`31,078` vs `3,460`，状态均为 `DONE`；telemetry 为雇工请求 58、种子订单 2（30 粒）、种植 30、浇水 300、收获 30、卖货 2（150 单位）。
7. 卖货搜索的 42 局均有 `DONE/DONE` 状态并已落在 JSONL；这只是批量本地验证完成，不是整项比赛目标完成。

## 4. 已观测的实验结果（不要过度外推）

| 对局 | 样本 | 本地结果 | 结论 |
|---|---:|---|---|
| `wheat` vs 官方 `starter` | 20 seeds | 小麦平均落后 285.1，20/20 输 | 运行器能稳定区分弱策略 |
| `melon` vs 官方 `starter` | 20 seeds | 西瓜平均领先 2321.7，20/20 赢 | 作物经济差异会被评测器捕捉 |
| `melon` vs `v45` | 3 seeds | 西瓜平均落后 156,197，3/3 输 | 不能只打弱对手；强参考远高于玩具策略 |
| `v45` vs `starter` | seed 7 | raw terminal cash `172,566` vs `3,484` | V45 能运行，但 raw cash 与公开榜单展示分数存在巨大量纲差异 |

**重要风险：**公开 Notebook 搜索中 V45 的历史/公开分数约为 2,765，而本地 raw terminal cash 可达十几万。尚未证明正式 simulation leaderboard 如何聚合 episode、对手配对和分数。因此：

- 本地 raw cash 可用于**同一套 seed/对手/环境下的相对比较**；
- 不能把 raw cash 直接映射为正式榜单分数或奖牌概率；
- 后续必须以多对手、留出 seed 的相对结果为主要门槛；若用户最终入赛，正式分数只是最后外部验证。

短批量运行不要设置过大：当前 Codex 单条命令约 30 秒会被截断；一次大约 20 个简单局或 3–10 个 V45 局较稳。更大批量应写出可恢复的分片 runner/结果文件。

## 5. 公开高分基线的已知策略

### V45（冻结开发基线）

公开 Notebook：[`ahmedberatozer/kaggriculture-v45-first-turn-wheat-round-trip`](https://www.kaggle.com/code/ahmedberatozer/kaggriculture-v45-first-turn-wheat-round-trip)。同源 clone `degnonguidi/kaggriculture-cloning-agent` 不应当当作独立策略。

它不是 LLM、不是神经网络，也没有 `.pkl/.pt` 权重；它是巨大的纯 Python 规则/路线系统：

- 把 13 条左右的预计算路线 tape 映射到开局商店组合；
- `Chassis` 在路线外添加 weed repair、卖货抢先、预算保护、仓库保护、dead stock、终局清仓等动态层；
- 依靠公开对手位置/行为做 clone/race 探测，在同类产品抢卖时调整卖货时间窗；
- 保留对小麦、种子、饲料、肥料和未来雇工的现金/库存预留；
- 对动物喂养、肥料 ROI、仓库容量及最后 7 回合货物送达做专门修复；
- V45 特有开局：第 0 回合大额买入并立即卖出小麦，试图影响使用相似公共路线的对手的后续报价。它对特定同类路线是博弈改动，不是无条件经济收益。
- 75 格怎么照顾过来（2026-09-22 实测，详见上文同日一节）：seed 0 对 starter 走第 121 号磁带，买到 75 格后每天只有 9–11 人。苗不是每天全浇，只浇「再干一天就变草」的那一半，连续没浇从不到 2，草是 0；17 头畜则每天都喂、都照料。人仍每天从仓库门口出发，路线事先写在磁带里。

源码路线入口大约在 `third_party/v45/main.py:223`、`_ROUTES` 在约 945 行；后面叠了大量实验性 wrapper。阅读时不要把每一层都当作已独立验证有效。

### 许可证/出处

V45 源码含 Apache 2.0 upstream notices，但其 Notebook 自身也提示上游 replay/路线数据链仍值得审查。当前副本用于：

- 本地开发基线与同 seed 回归对照；
- 行为/模块研究对象；
- 派生 variant 的冻结父本（variant 必须另存、可 diff）。

在未完成来源审查前，不应将其或其 variant 直接打包提交，更不得声称为用户原创。

## 6. 已归档策略方案：独立规则优先，ML 为可选加分

此节保留以解释先前的西瓜实验，已被“V45 冻结父本 + 可 diff variant”主线取代。不得按本节继续把自写西瓜策略当作主策略。

### 目标

不是复刻全规模 AlphaStar。现实版本是：强规则底座 + 多对手评测联赛 + 少量高层决策优化。

### 规则主线（优先做）

1. **先建自己的可解释生产基线**
   - 不从公开 V45 直接复制；先实现明确可读的路线/工人/作物生产逻辑。
   - 最低要求：移动、种/浇/收、shed 取放、市场卖货、终局清仓都可记录和测试。
2. **现金与容量保护**
   - 为未来种子、雇工、小麦饲料预留现金；
   - shed 接近满时优先清理，防止溢出和后续路线断裂。
3. **动态卖货**
   - 不按固定日期卖；使用剩余时间、自己库存、当前市场库存/价格、对手公开成熟同类作物决定卖出比例。
4. **投资 ROI gate**
   - 对买地、雇工、动物、肥料、大额种子比较“可在剩余回合兑现的预期收益 - 成本 - 路径/容量风险”。
5. **对手公开行为适应**
   - 先用可解释特征：作物/动物构成、成熟度、市场方向、资金、位置；后续再考虑聚类。

### 自动化而非人工调参

不要每次手改阈值。把规则写成参数化策略，例如：

```text
价格阈值 P、shed 压力 C、剩余天数 D、对手同类供给 R、卖出比例 S
```

自动生成候选参数 → 相同 seed/对手池配对 → 淘汰 → 给少数优胜参数增大样本 → 留出 seed 验证。

这适合随机搜索/进化搜索/贝叶斯优化。结构性新想法仍应先由 agent 写成独立模块，再让自动评测决定是否保留；不要让自动程序随意改 Python 源码。

### 对手联赛（借 AlphaStar 的可迁移部分）

维护并冻结：官方 starter、多个简单作物/动物类型、公开强参考、自己的历史版本、专门克制当前主力的版本。主力策略不应只打赢单一 benchmark。

评价指标：

- 多 seed、多对手的平均终局现金；
- 相对当前主力的平均 margin；
- 方差和最差局；
- 动作错误数、超时与 shed overflow。

新版本只有在**从未参与调参**的 held-out seed + opponent 组合中稳定变强，才可以取代主力。

## 7. 小模型方案（第二阶段，尚未实现）

### 不使用 LLM

LLM 不适合、也不需要参与评测。`.pkl` 若使用，装的是小型策略/价值模型的权重或参数字典。

### 推荐 MVP

输入 100–300 个结构化特征：

- day/hour/剩余回合；
- 现金、土地、shed 利用率、种子/产品/动物/工人数；
- 作物成熟度与需要处理的工作；
- 市场价、库存、价格变化；
- 对手公开农场、生产结构、移动/卖货迹象；
- 当前路线与已承诺成本。

输出只做高层离散选择，不控制逐格移动：

```text
开局路线（13 选 1）
卖货强度（不卖/少量/正常/提前清仓）
投资（不投/雇工/买地/动物/肥料）
转产倾向
```

规则层负责将建议转成合法具体动作，并兜底资金、仓库、商店解锁条件。

### 训练顺序

1. 从 benchmark/自有规则 rollout 的行为做 imitation，先学不犯基础错误；
2. 用自动化对局结果训练上下文选择器/价值模型，预测当前局面下哪种高层选择更好；
3. 只有此模型在 held-out 联赛中稳定优于规则版，才考虑 PPO/self-play 微调。

`MLP` 是小网络结构；`PPO` 是训练这类网络的方法；两者都不是大语言模型。PPO 最终可用，但不应作为当前第一步。

### 算力判断

- 规则搜索和环境 rollout 主要吃 CPU，GPU 不会明显加快 720 回合环境本身。
- 小树模型/小 MLP 可先 CPU 训练；若进入 PPO，租 AutoDL 单张 RTX 4090 24GB + 至少 8 CPU 核/32GB 内存已足够原型。
- 不需要多卡或 A100。不要在本地评测/规则基线尚未证明有价值前租 GPU。
- 当前在 AutoDL 查到的 4090 示例价约 ¥1.88/h，仅供估算，租用时需重新核价和确认 CPU/RAM。

## 8. 当前推荐的下一步执行顺序

1. 用户要求空地自动雇人/买种/种/浇，钱花在能下种的格子上。已用 `plan_fill`：按空地和浇水工作量雇人买种；先浇已有作物再种；种完当天再浇新苗。seed 0 对 V45：`8769:125276`。第 1 天早上 25 格已铺满（萝7 瓜4 莓6 茄6 麦2），第 0 天雇 4 人。第 11 天仍被对方 12 瓜甩开（我方按店 70% 只种了 4 瓜）。未获准不跑联赛。
2. 未获准前不跑七对手 112 局、不报名、不提交。
2. 用户否决 A/B 类开局小麦、现金买单等小处找补作为主线。真正要做的是 **第 14 条路线**：不在 V45 现成 13/40 条 tape 里挑选；按每种紧俏货「已知剩余购买量 × 70%」看当前+已承诺产量，不够就继续加产，已经够了就把新增地/钱/工人改投还没够的货，全部够了再投非紧俏高价值线。v1/v2 只是在已超额的旧菜谱里换本，所以会 abstain 或切错店；第 14 条必须能**停掉超额、改产**，因此是完整新路线（可挂在 V45 的移动/卖货/安全外壳上），不是外挂空地、也不是选 route123。
用户随后用三句话钉死第 14 条的种法，纠正了「重排」的歧义：

1. **重排**不是把正在长的作物拔掉重来；只是「这一茬收完、下一茬还没下种时，按此刻已开门的店和 70% 缺口决定下一茬种什么」。新店只影响还没种的下一茬。
2. **停产**只取消还没做的种/买，已种下的长完再换。
3. **换什么**：不到 70% 就继续补这一种；满了就换下一种。不要再拆成「紧俏集合 / 非紧俏第三名」的复杂说法。

用户 2026-09-20 进一步确认实验态度：**先别管 V45 那 13 条固定种植日程**；每一茬种什么都必须先看当前 70% 缺口再下种。用户认为这在方向上收益最高。已对齐的规则：熟了才换、只改还没做的、不到 70% 继续补、满了换下一种。

用户 2026-09-20 已把第 14 条关键数字钉死（仍未获准写代码）：

1. 店没开也轮种。麦收完可先种草莓进仓库，店开了再卖；整季都没店则走下面卖货规则。
2. 土地 70% = **现有已解锁格子数**。先尽量满足店铺收购 70%；若因此超过现有格子的 70%，减产到只占 70% 格子。店货若只用掉更少格子，多出来的并入 30% 轮种其他作物。
3. 种下/放畜时把这一茬浇水、收货、照料订死；订不完就不种。下一茬等收完再看缺口。
4. 已放下的动物不拆；未买的只有缺口大且仍留得轮作地才买。
5. 买地：当前价估算「从能收获起到第 30 天」收入，减去买地（1000/2000/4000）和为新地多雇人的费用，净值为正才买。若当天加人后走路仍排不完，则**少种，并且少买/不买地**。
6. 雇人：每天收工解散、次日重雇。先雇够已订死的浇/收/喂（含农夫）；只有 30% 轮种或新地「不加人就会旱死」才多雇；加人后仍排不完则少种、少买地，不无限雇。
7. 卖货：观察期 = 到比赛结束剩余整天数的 **30%**（20 天剩则看 6 天，不是封顶 3 天）；观察期内只记最高价。之后现价达到该最高价的 90% 就卖。达不到 90% 则到期清仓（最后一天必须卖掉，仓库不计分）。

用户 2026-09-20 明确说「开始」。已落地独立执行器 `route14`（`lab/route14_economy.py`、`lab/route14_agent.py`、协议 `docs/ROUTE14_PROTOCOL_V1.md`），不编辑 V45、不报名。

用户 2026-09-20 要求学 V45：先把现有地种满、现金砸进能下种的格子，人管得过来才买地。已加 `seed_fill_plan`（按缺口买第一种，再买得起的下一种把当天能种的空地填上；有种子时先种）。不抄开局买麦 70 卖麦 70。

`9941:142895` 那局失败在开局没铺满：第 0 天现金 996、只有 4 瓜、空地 20+；4 格瓜把店铺缺口盖住后改点草莓，钱不够就停种。种 250 次主要是后期短茬麦。第 25 天空地 11→30，又买了一块种不满的地。

用户点明：对方卖高价瓜，我们卖萝卜草莓茄子小麦。这正是 70% 按店缺口分地的结果——瓜没有店，地被分给有店的货；那些货单价/茬期都比不上开局价约 250 的瓜。未改规则、未开联赛。

用户 2026-09-20 进一步问：瓜是不是天生能赚大钱，应否改成 70% 最高经济作物、30% 次优，价值按卖出金钱/天。判断：开局价下是；不要写死永远种瓜。

用户随后明确重设：**70% 不要紧俏，要赚钱；赚钱 = 金钱/天。** 已改选种、雇人、前15天即卖。

用户指出收瓜比对方晚是成熟期估错。官方 `first_yield_day` 瓜=10、麦=2、萝=2、茄=8、莓=10；瓜浇水窗口第6–12天，未施肥第10天即可满产6。我们误用占格12天才收。已改为 `crop_ready_to_harvest`：一次性作物产量到顶或到 `max_yield_day` 再收，进行中作物有货就摘；瓜占格/金钱天改为10。单测 11/11。seed 0 对 V45：`26234:140327`。第 10 天瓜 17→4，第 11 天卖出约 1.18 万。

用户追问第 11 天为何仍是 `11834:18296`。该日现金差约 6500，不是没卖到瓜：对方第 10 天先把 12 格瓜倒进**共享市场**（2691→17932），我们第 11 天早上才卖（599→11834）。同样一茬瓜，对方吃高价、我们吃砸价后的低价。用户要求像对方一样为倾销加人：卖货当天雇平常两倍人手先把货卖掉；能收就收，不凑满 6 再浇再收。已改。seed 0 前 15 天对 V45：半场 `13495:28707`；第 10 天 `8027:17265`（我雇 6、对方 11，地里还剩 13 瓜）；第 11 天 `12550:16851`。

用户问 V45 是否每条路线第 11 天都雇 11 人抢先卖瓜。静态 41 条 tape：**第 10 天** 39 条雇 11、2 条雇 12（瓜当天成熟）；第 11 天仍 10 或 11 人（扫尾 12 个瓜 + 大农场）。route 0 第 10 天 hour 0 雇 9、hour 1 再雇 2。完整脉络已写在 **§0.5**。未获准不跑联赛、不报名。

用户 2026-09-20 改卖法：收到 60% 卖一包、收完再卖。seed 0 第 11 天 `10964:16626`，比进仓就卖的 `13782:16700` 差。60% 那包 14 点才凑齐，对方 11 点已经倒完。未获准不跑联赛、不报名。
3. 开发集过门才跑 holdout。holdout 过门后把证据和出处风险交给用户，由用户决定是否说“报名”。
4. 不改冻结 `third_party/v45/main.py`；不把 V46/sdy 整包换父本；不从零执行器冲榜。
5. 报名截止 `2026-09-23 23:59 UTC`。未获“报名”授权前不得加入比赛或提交。

## 9. 给后续 agent 的注意事项

- 不要把“13 路线”误说成游戏只允许 13 种行动，它只是公开 benchmark 的路线库。
- 不要把公开 Notebook 的 `best_public_score` 视为实时排名或可复制的保证分数；模拟赛的对手生态会变化。
- 不要把 single seed、官方 starter、或 raw local money 当作奖牌预测。
- 对手会变化，因此训练/规则评测必须是 pool，而不是固定一个 bot。
- 在提交前，须测试包在无网络、限定依赖环境中能通过 `main.py` 加载；PyTorch/NumPy 是否可用不能只凭本机假设。
- 不要运行 `third_party/v45/reconstruct.py` 去覆盖或改写用户的 agent；它只负责重建本地参考 `third_party/v45/main.py`，且带内部 SHA 断言。
- 本项目尚无 Git 仓库；若初始化/提交，遵守 Conventional Commits。
